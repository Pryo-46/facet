# M24 申し送り: ツリーのノード幅固定——幅を導出しない、高さは3行で止める

> **追記専用。** そのとき何が起きたかの記録であり、以後変えない。**いま開いている残件は [`../open-issues.md`](../open-issues.md) を見ること**——本書に書かれた残件は当時の状態を指す。

M24 は「**課題ツリー・ロジックツリーのノード幅を一律 320px に固定し、閉じた箱に出る文章を3行で打ち切る**」マイルストーン。UI ノート（[`../facet-UI設計ノート.md`](../facet-UI設計ノート.md)）の実施優先順位 §8 の **優先6＝D（レイアウト固定。D3）**にあたる。**D3 は2本柱を持つが、実施したのは後半（ツリー）だけで、前半（テーブルの行高固定＋2行省略）は「変えない」と決着させた**——これは先送りではなく判断である（決定1。人間の裁定）。

実装計画は [`../superpowers/plans/2026-08-25-m24-node-width-lock.md`](../superpowers/plans/2026-08-25-m24-node-width-lock.md)（`0578ae7`。このブランチの2番目のコミット）、設計の正は [`../superpowers/plans/2026-08-25-m24-node-width-lock-design.md`](../superpowers/plans/2026-08-25-m24-node-width-lock-design.md)（`b1f7c39`。ブランチ最初のコミット）。**設計は 2026-08-25 に人間との対話で確定しており、4つの裁定（テーブルの行高は変えない／幅は両ツリーとも一律 320／高さは行数上限で止める／打ち切りの合図は出さない）がすべての決定の根拠になっている。**

コミット範囲: `b1f7c39`（スペック）〜本コミット。実装は5コミット——`f7d9332`（Task 2。番人の観測点を幅から高さへ）・`85374d1`（Task 3。`wrapWithin` に `maxLines`）・`281285c`（Task 4。ロジックツリー固定幅）・`57199ff`（Task 6。`CellInput` の `scrollTop`）・`aa6f29f`（Task 5。課題ツリー固定幅）。**Task 1（着手前スキャン）と Task 8（通しの検証）はコミットを産んでいない。** **`57199ff` が `aa6f29f` より前にあるのは実行順を入れ替えたためで、事故ではない**（下の「実装で確定した事項」参照）。

テストは **1774 → 1785 件**（全件緑）。`npx tsc -b` は exit 0、`npx oxlint` は指摘なし。**実機確認は人間の作業として未了のまま残す**（[`../open-issues.md`](../open-issues.md) の「次に手を付ける候補」7件目）。

---

## 何を作ったか

- **幅を内容から導出するのをやめた。** 課題ツリー・ロジックツリーとも、全ノードが **320px 固定**になった。表現は `wrapWithin` の **`minWidth === maxWidth`**——新しい書き方ではなく、シーケンスの答えセル（`ANSWER_WRAP`）が既に使っていた同じ手である。これで右上のバッジは列ごとに縦一列に揃う（rev.3 が言う「スキャン性」の主張そのもの）。
- **閉じた箱に出る文章を3行で打ち切る。** 対象は**課題のタイトル・見送りの理由・ロジックのノード**の3つ。**展開パネルの中（由来・根拠・FB・以前の判断）には上限をかけない**——そこが「詳細を読む場所」そのものであり、D3 の「詳細は別パネルで表示」が指しているのがこの場所である。
- **省略記号は出さない。** `text-overflow: ellipsis` も `-webkit-line-clamp` も `textarea` には効かない（フォームコントロールであるため）。溢れは `overflow-hidden` で切り、編集中はキャレット移動で全文に届く。`overflow-y: auto` は採らない——Chromium/Windows のスクロールバーがレイアウト幅を食い、測定の前提より狭い内容幅になって**測定より多く折り返して文字が切れる**ため。
- **`CellInput` の `onBlur` で `scrollTop` を 0 に戻す。** 戻さないと、長文を編集して抜けた箱だけが途中の行から表示されたまま残る。1箇所で済み、`autoSize` の欄（テーブルのセル）は内容が常に収まるので影響しない。
- **`wrapWithin` に任意の `maxLines`。** **既定は無制限**なので、シーケンスと展開パネルは無変更で通る。落とした行は**幅の算出にも入らない**（見えない行が箱の幅を決めるのは筋が通らない）。
- **消えた定数3つ**——`ISSUE_MAX_WIDTH`(320)・`ISSUE_TITLE_MIN_WIDTH`(128)・`NODE_MIN_WIDTH`(96)。**改名1つ**——`NODE_MAX_WIDTH` → **`NODE_WIDTH`**（上限ではなく固定値になり、名前が嘘になったため）。**新設3つ**——`NODE_MAX_LINES`(3)・`BOX_TEXT_MAX_LINES`(3)・`WrapOptions.maxLines?`。**3つの 320（`BOX_WIDTH` / `NODE_WIDTH` / `LABEL_MAX_WIDTH`）は共有定数に束ねていない**——2つは固定値・1つは上限で、意味の違うものが同じ名前になる方が危険である。
- **`layout.ts` から幅の分岐そのものが消えた。** `if (rows.length > 0 || deferred)` の if/else と、それに付いていた 25 行の JSDoc（「箱の下限から枠のぶんを引くと、空けた枠が文章を食って入力欄が数 px になる」という実機の欠陥の説明）が、**構造ごと消えた**——固定幅では枠は常に固定幅の中から取られるので、枠が文章を食って下限を割る経路が成立しない。同じ理由で **`wrapWithin` の `minWidth <= maxWidth` 依存**（食い違うと `maxWidth` を採るので、逆転するとタイトルが黙って下限を割る）も課題ツリーからは消えた。**`wrap.ts` 側の注意書きは残してある**——シーケンスがまだこの依存の上にいる。
- **文書。** [`../overview-rev.md`](../overview-rev.md) 9章の「測定層と描画層が同一のフォントトークンを参照しなければならない」の項に、**フォントに追従するのは高さ（行数）だけになった**という帰結を足した（シーケンスだけ扱いが違うことを含む）。UI ノート D3 に **M24 での実施結果**の追記小節を置き、§8 の表の直後に**実施状況**の1行を足した（表そのものには列を足していない——この表は順位を示すもので、完了を記す様式を持たない）。[`../open-issues.md`](../open-issues.md) は**消1・書き換え1・足す1**。

---

## 実装で確定した事項

### `LogicTreeEditor.font.dom.test.tsx` の番人が巻き添えで死ぬところだった——観測点を幅から高さへ移した

このテストは「Web フォントの読み込み完了（`document.fonts.ready`）でノードが測り直されるか」を、**ノード矩形の `style.width`（142px → 262px）**で観測していた。測定器を「呼ばれるたびに太く測る」偽物に差し替え、幅が動くことで測り直しを証明する形である。**幅が定数 320 になると、測定器を差し替えても幅は動かない**——番人が黙って何も検証しなくなる。

**観測点を `style.height` へ移した**（決定4）。高さは行数を通じてフォントに依存し続けるので、太さが倍になれば折り返しが増えて高さが動く。**期待値はピクセルのリテラルで書いていない**——実測した1行ぶんの高さから `inset + oneLine * 2` の形で導いている。段や `leading` が変わっても黙って取り残されないためである。

**文言長を12文字から20文字へ変えた。** 内容幅 298px（320 − `NODE_INSET_X` 11×2）に対し `L × perChar ≦ 298` が `perChar` 10 で真・20 で偽になる L の範囲は **15 以上 29 以下**で、その中央付近の 20 を採った。**12文字のままでは 12×20＝240 ≦ 298 で2回目も1行に収まり、高さが動かない**——赤くならず、番人が黙って死ぬ。この導出はテスト本文のコメントに残してある。

**番人が本当に番をしていることを、破壊テストで実証した**（Task 2 Step 3）。`measurerKey` から `fontGeneration` を一時的に外すと高さが 38px のまま（期待 62px）で FAIL し、戻すと PASS した。**「不等式なので自動追従する」という思い込みが M23 決定1 で実際に外れている**ので、今回は緑を見るだけで済ませていない。

**この作り替えを、幅の固定より前に置いた**（Task 2 が Task 4・5 より前）。**現行コード（可変幅）でも緑になる形で書き換えたので、「幅の固定を入れたついでに 320 へ書き換えて緑にした」という退化が構造的に起きない。** 実際、幅を固定した後もこのテストは緑のままだった。

### `ISSUE_TITLE_MIN_WIDTH` が守っていた不変条件は、定数からテストへ移った

`ISSUE_TITLE_MIN_WIDTH`（128）は「箱の下限ではなく文章の下限」という概念を持つ定数で、固定幅では出番が無くなる。**ただし「一番広い枠を引いてもタイトルが痩せすぎない」こと自体は要る**——`BOX_WIDTH` を縮めるか、バッジの語を伸ばした将来の変更に対する門番である。

これを `layout.test.ts` のアサーションへ移した（「一番広い枠（仮説なしバッジ）を引いても、タイトルは日本語8字ぶんを残す」）。一番広い枠は「仮説なし」バッジ（`text-sm` 14px で4字 ≒ 56 ＋ `BADGE_PADDING_X` 6×2 ＋ `BADGE_BORDER` 1×2 ＝ 70）である。**下限をリテラルで書いていない**——`fonts.title.measure('あ'.repeat(8))` とテストの測定器から導いている。段が変われば「8字ぶん」の px は動くので、リテラルで書くと M23 決定1 と同じ隠れリテラルになる。

### 実行順を1つ入れ替えた——Task 6（`CellInput`）を Task 5（課題ツリー）より前に実行した

計画は Task 5 → Task 6 の順だった。そのとおりに実行すると、Task 5 が書く `BOX_TEXT_MAX_LINES` の JSDoc が「抜けたときは `CellInput` が `scrollTop` を 0 に戻す」と、**まだ存在しない挙動を現在形で断言する窓**ができる。

**M23 が「旧段を現在形で断言するコメント7行が残っていた」を実装で確定した事項として記録している**ので、同じ形を自分で作らないことにした。**成果物は同一**で、コミットの順序（`57199ff` が `aa6f29f` より前）だけが計画と違う。

### 計画の誤りが2件——どちらも実装者が辻褄を合わせずに報告した

1. **Task 4 の「旧名 `NODE_MAX_WIDTH` / `NODE_MIN_WIDTH` の参照は3ファイルだけ」は不正確だった。** `src/modules/issue-tree/measure.ts:17,22` のコメントにも残っていた。**ただしその2行は `ISSUE_MAX_WIDTH` / `ISSUE_TITLE_MIN_WIDTH` の JSDoc の中にあり、Task 5 の定数削除で構造的に消えた**——結果として追加の手当ては要らなかった。
2. **Task 5 の「旧定数名の grep で残るのは `wrap.test.ts` の歴史コメント1件だけ」も不正確だった。** `layout.test.ts:184` にも「**M24 より前は** `ISSUE_TITLE_MIN_WIDTH` が持っていた不変条件」という**過去形の歴史記述**がある。**残すと裁定した**——`wrap.test.ts:43` の「**当時の** `NODE_MAX_WIDTH`／`NODE_MIN_WIDTH`」と同じ形で、消すと「なぜこのテストが要るのか」の由来が失われるためである。

### 文書反映（Task 7）で、UI ノート §8 の帰属の食い違いが1件見つかった

計画 Task 7 Step 6 は「優先 1〜6・9 は消化済み（**1・3・4 ＝ M21・M22、2・9 ＝ M22**、5 ＝ M23、6 ＝ M24）」と書くよう指示したうえで、**その帰属は検証していない**と自ら断っていた。[`m21-core-design-tokens-v2.md`](m21-core-design-tokens-v2.md) と [`m22-core-missing-semantics.md`](m22-core-missing-semantics.md) に当たると食い違っていたので、**正しい方を書いた**:

- **優先2（D9。数字の等幅化＋数値列の右揃え）は M22 ではなく M21。** M21 の「何を作ったか」に「`@layer base` の `body` へ `font-variant-numeric: tabular-nums` を一括適用し、No 列は右揃えにした」と明記されている（M22 が用語集に No 列そのものを足したのは事実だが、D9 の作業ではない）。
- **優先1（D15。判断軸の配色）・優先4（D2・D16・D19。色の役割整理）も M21 単独。** M22 は D15 に触れていない。
- **優先3（D1・D14）と優先9（D4）は M22。** ただし**優先9 に含まれる D5「警告は問題のあるセルだけを染める」は M21 が片付けている**（シーケンスの行全体を染める帯を廃し、セルの状態を面から輪郭へ移した）ので、その但し書きも UI ノートに残した。

書いた行は **1・2・4 ＝ M21 ／ 3・9 ＝ M22 ／ 5 ＝ M23 ／ 6 ＝ M24** である。

### 計画との差分が無かったところ

- **決定2 の「消える定数・消える分岐」は、実物と一致していた。** `ISSUE_MAX_WIDTH` / `ISSUE_TITLE_MIN_WIDTH` / `NODE_MIN_WIDTH` の3つと、`layout.ts` の if/else 分岐＋25 行の JSDoc は、いずれも計画が指したとおりの場所にあり、そのまま消えた。
- **`maxLines` の既定が無制限であることで、シーケンスと展開パネルは1行も触らずに通った**（決定3）。`ANSWER_WRAP`（`maxWidth === minWidth`）も `LABEL_MAX_WIDTH`（320）・`ACTOR_MAX_WIDTH`（240）も据え置きである。
- **決定5 の「削除するテスト」も指示どおりだった。** `layout.test.ts` の「仮説を持たない課題の箱はタイトルの自然幅」は主張が反転したので消し、「空のタイトルでもバッジのぶん箱が広がり、入力欄は下限を割らない」は箱が広がる構造ごと消えたので、残す側（タイトルが下限を割らない）だけに書き直した。

---

## 直さずに残したもの

**このマイルストーンの中では直さないと決めたもの**で、台帳（[`../open-issues.md`](../open-issues.md)）にも項を起こしていない。

- **バッジ右端揃いのテストは、幅ロック自体を弁別しない。** 決定5 は「同じ深さの箱で見送りバッジの右端が一致すること」を門番に据えたが、両フィクスチャが見送り済みで、**旧コードでも `BOX_WIDTH` だったため、幅を固定する前でもこのテストは緑になる**。**ただし穴にはなっていない**——レイアウトが矩形を組むのは見送りバッジだけで、「仮説なし」バッジと見送りトグルは `IssueBox` が CSS の `right: ISSUE_PADDING_X` で右寄せする。どちらも右端は「箱の右端 − `ISSUE_INSET_X`」に落ちるので、**幅の一様性を押さえることがそのままバッジの縦一列を押さえる**。幅一様性のテスト（RED で 228 ≠ 320 と落ちた）と対で D3 の主張を守る形になっている。**弁別力を上げるなら、見送り済みでないフィクスチャを混ぜた3件目が要る。**
- **`layout.test.ts:184` の過去形の歴史記述**（上記「計画の誤り」2件目）。**残すと裁定した**もので、次に旧定数名を grep した人はこの1件に当たる。
- **`BOX_TEXT_MAX_LINES` と `NODE_MAX_LINES` は、値が同じ 3 でも別々に持っている。** 2つの木が互いの寸法に縛られないためで、`BOX_WIDTH` と `NODE_WIDTH` を束ねなかったのと同じ扱い。相互参照のコメントで結んである。

---

## 実機確認について

**未実施である。** サブエージェントは Tauri の GUI を操作できないため、実機確認は人間の作業として残っている。以下は設計スペック「検証」11項目を、**空のまま**写したものである。**通ったかどうかの記録ではない。**

```bash
npm install        # 省略しない
npm run tauri dev
```

- [ ] 1. 課題ツリーで、**同じ列の箱のバッジが縦一列に揃っている**（D3 rev.3 の主張そのもの）
- [ ] 2. 短いタイトルの箱と長いタイトルの箱が同じ幅で、木が階段状に見えない
- [ ] 3. 空の課題を新規作成したとき、タイトルの入力欄に十分な幅がある（1字も打てない箱ができていない）
- [ ] 4. ホバーで出る見送りトグルが、タイトルの文字に被らない（枠の予約が固定幅でも効いている）
- [ ] 5. ロジックツリーで、長文ノードだけ幅が3倍になる現象が消えている
- [ ] 6. ロジックツリーの木が横に広がったことで、初期表示（`INITIAL_TRANSFORM`）から根が画面外に出ていないか
- [ ] 7. **4行以上の文言を課題タイトルに打ったとき、キャレットを動かして4行目以降が読めるか**（決定3 の未検証の前提。ここが false なら設計判断のやり直し）
- [ ] 8. 長文を編集して箱を抜けたとき、表示が先頭行に戻っている（`scrollTop` のリセット）
- [ ] 9. 見送りの理由が3行を超えたときの見え方
- [ ] 10. 展開パネルの中（由来・根拠・FB）は従来どおり全文が伸びる
- [ ] 11. 用語集・エラーカタログの行高が M23 から変わっていない（触っていないことの確認）

**項目7 を先に見ること。** 「`overflow: hidden` の `textarea` で、キャレット移動時にブラウザが内部スクロールする」という**未検証の前提**の上に、「省略記号を出さず打ち切るだけ」という決定3 が乗っている。**追従しないなら4行目以降が編集で到達不能になり、設計判断のやり直しになる**——そのときの逃げ道は、非フォーカス時のフェード（`mask-image` を `:not(:focus)` に当てる）か、スクロールの許可である。

確認後は次で後片付けする（[`../../CLAUDE.md`](../../CLAUDE.md)「マージ後の後片付け」1）:

```bash
git checkout -- sample-project/ && git clean -fdx sample-project/
git status --short          # 空になること
```

**実機確認とこのタスクは束ねていない。** チェックリストは空のままで、消し込みの管理は [`../open-issues.md`](../open-issues.md) の「次に手を付ける候補」7件目が持つ。確認で出た変更要求は計画外修正として扱う（症状と人間の言葉を分けて記録すること——[`../lessons-for-planning.md`](../lessons-for-planning.md)「タスク分割」節）。

---

## 次へ

**UI ノートの残りは D の前半（判断として閉じた）・E・F。**

- **D（レイアウト固定）** ——ツリー側は本マイルストーンで閉じた。**テーブルの行高固定＋2行省略は「変えない」と決着した**ので、蒸し返すなら (1) `CellInput` の `MAX_ROWS = 8` に付いた「行の高さが揃わなくなるが、読めないよりよい」（M10 決定17）を反転させる理由と、(2)「省略した全文をどこで読むか」の答え——テーブルには展開パネルに相当する場所が無い——の2点に答えを持ってくること。
- **E（フォント同梱。U1 未決）** ——ウェイト階層（D8）の全面展開は引き続き E 待ち。M24 は触っていない。
- **F（見送り集計ほか。D17・D18、§1.1 の select 置換・角丸統一）** ——手つかず。**U4（破線エッジの意味）は実装が既に答えを持っている**ので、問いとしては閉じている（[`../open-issues.md`](../open-issues.md) の F の項を参照）。
- **ダークの吟味**は M21 から持ち越したままで、M24 も触っていない。

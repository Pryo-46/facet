# M25 実装計画: UI ノート F の決着（見送りの別枠集計・U4・select 置換・角丸統一）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 課題ツリーの帯に見送りの別枠「見送り N」を出し（要対応の集計は既に見送り配下を除外済み——今回は「消えている在庫」を見えるようにする側）、帯の `⚠` を `CircleAlert` アイコンへ替え、ネイティブ `<select>` 2箇所を styled なドロップダウンへ置換し、角丸を2段＋円（`rounded-sm` / `rounded-md` / `rounded-full`）に統一して機械検査で固定する。あわせて D18・U4 を文書として決着させる。

**Architecture:** 集計の導出は `derive.ts`（Skill へバイト一致コピーされる——変更後は `cp` で追従し、smoke テストが逐語一致を縛る）。巡回は `open-targets.ts` の既存の剰余ロジックと同型。select 置換は既存の Radix `DropdownMenu`（`KindMenu` の先例）を使った新部品 `CellSelect` で、素の ↑↓ の値切り替え（操作言語）を部品が所有する。角丸は `conventions.test.ts` に許可リスト検査を足す（M23 の `leading-*` と同じ形。ただし走査は ui/ を**含む**別母集合）。

**Tech Stack:** React + TypeScript + Tailwind v4 + Radix (shadcn DropdownMenu) + lucide-react + vitest (jsdom)。

**Spec:** `docs/superpowers/plans/2026-08-25-m25-ui-note-f-design.md`（同ブランチのコミット `a2dc99c`〜`ac89b01`。決定1〜8 と実測済みの実効値はすべてそこにある）

## Global Constraints

- **作業場所は worktree `worktree-m25-ui-note-f`**（`C:\Dev\Projects\facet\.claude\worktrees\worktree-m25-ui-note-f`）。基底は `73a7d95`（`issue-tree/main`）で確認済み。主チェックアウトでファイルを作らないこと
- **計画の指示が実物と矛盾していたら、辻褄を合わせずに「計画の矛盾」として報告する。** ただし**既存実装と一致すべきもの（文言・クラス名・テストの形）は実物が正**
- **`derive.ts` を変えたら必ず `cp` で Skill 側コピーを更新する**（手で写さない）: `cp src/modules/issue-tree/derive.ts .claude/skills/issue-tree-register/scripts/derive.ts`
- **`derive.ts` に値 import・相対 import・`enum` を足さないこと**（バイト一致コピーが Node の型ストリップで動く前提。`skill-copy.test.ts` が検査する）
- テストの期待値にリテラルの px を書かない（この計画では該当なしのはずだが、迷ったらこの原則へ戻る）
- コミットメッセージは既存の流儀（`feat(m25): …` / `test(m25): …` / `docs(m25): …`、末尾に `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`）
- Rust（`src-tauri/`）には触らない

---

### Task 1: 準備と着手前スキャン（コミット無し）

**Files:**
- 変更なし（読むだけ＋ `npm install`）

**Interfaces:**
- Consumes: なし
- Produces: 後続タスクが使う「現状の確認結果」。食い違いが出たら**計画の矛盾として報告**し、該当タスクを止める

- [ ] **Step 1: worktree と基底の確認**

```bash
cd C:/Dev/Projects/facet/.claude/worktrees/worktree-m25-ui-note-f
git log --oneline -3
```

Expected: 先頭付近に `ac89b01`（スペック訂正）と `a2dc99c`（スペック）、その下に `73a7d95` が見えること。

- [ ] **Step 2: 依存を入れ、ベースラインが緑であることを確認**

```bash
npm install
npm test
npx tsc -b
```

Expected: 全件緑・exit 0。**worktree には `node_modules` が無い**ので `npm install` を飛ばさないこと（CLAUDE.md の教訓——飛ばすと後のエラーを誤診する）。

- [ ] **Step 3: 前提の確認スキャン（結果をメモし、食い違いは報告）**

以下を確認する。**計画は 2026-08-25 時点の実測に基づいており、ここで違ったら該当タスクの前提が崩れている:**

1. `src/modules/issue-tree/derive.ts` の `tallyQuestions` が抑制配下を数えないこと（`poseQuestions` の `!off &&`）——D17 前半は実装済み、という本計画の前提
2. `rounded` 系の全出現（テスト・`.ts` を含む）:
   ```bash
   grep -rnoE "rounded(-[A-Za-z0-9\[\]().,%*-]+)?" src/ | grep -v ".test." | sort
   grep -rn "rounded" src/ --include="*.test.*" | grep -v "rounded-full"
   ```
   計画 Task 8 が列挙する箇所（`badge-styles.ts:30`・`HypothesisRow.tsx:245`・`IssueTreeEditor.tsx` の `DEFER_TRIGGER_FACE`・`ui/button.tsx`・`ui/dropdown-menu.tsx`・`ui/alert-dialog.tsx`）と突き合わせる。**テストが `rounded` の字面をアサートしている箇所があれば列挙する**（既知: `IssueTreeEditor.dom.test.tsx:229,759` は `rounded-full` のセレクタで、`rounded-full` は据え置きなので影響なし）
3. select セルの既存 DOM テスト（Task 7 で書き換える対象の列挙）:
   ```bash
   grep -rn "fireEvent.change\|種別\|resolutionLevel\|対応" src/modules/glossary/*.dom.test.tsx src/modules/error-catalog/*.dom.test.tsx | grep -in "select\|change\|種別\|レベル"
   ```
4. `IssueTreeEditor.dom.test.tsx` で判断ピッカー（`KindMenu`）のメニューを**どう開いているか**（`fireEvent.click` / `keyDown` / `pointerDown`）——Task 6 の CellSelect テストは同じ開き方に倣う
5. `openFile()`（`IssueTreeEditor.dom.test.tsx` のフィクスチャ）に見送りを掲げた課題が**無い**こと（Task 5 の「0件ではチップが出ない」がこのフィクスチャで書けるか）
6. **select セルでの Enter と Space の現在の行き先**——`GlossaryEditor.tsx` / `ErrorCatalogEditor.tsx` の `resolveCommand`（キー言語）が、select セルの context（`editing: false` ほか）で Enter / Space をどう解決するか実物で読む。**Task 6 は「Enter はセルの操作言語へ渡す（開かない。ネイティブ select も Windows では Enter で開かない）／Space とクリックで開く」を前提にしている**——Enter が操作言語で意味を持たないと分かったら、それはそれで前提どおり（渡しても何も起きない＝ネイティブと同じ）。**Space が操作言語で意味を持っていたら計画の矛盾として報告**（ネイティブ select は Space で開くので、現状もその Space は select が食っているはず——食われていない形跡があれば前提が崩れている）

---

### Task 2: `derive.ts` の見送り集計と Skill への反映

**Files:**
- Modify: `src/modules/issue-tree/derive.ts`（末尾、`ISSUE_DEFERRED_LABEL` の後）
- Modify: `src/modules/issue-tree/derive.test.ts`
- Modify: `.claude/skills/issue-tree-register/scripts/derive.ts`（`cp` で更新）
- Modify: `.claude/skills/issue-tree-register/scripts/issue-tree-write.mjs:279,304` 付近
- Modify: `.claude/skills/issue-tree-register/SKILL.md:199` 付近
- Test: `src/modules/issue-tree/skill-write.smoke.test.ts`

**Interfaces:**
- Consumes: `latestKind`・`ISSUE_DEFERRED_LABEL`・`TALLY_TOTAL_LABEL`（いずれも `derive.ts` に既存）
- Produces: `deferredIssueCount(issues: readonly Pick<IssueNode, 'events'>[]): number`／`deferralLine(count: number): string`（`見送り N`）／`DEFERRAL_NOTE: string`。Task 5 のチップと Skill の報告が使う

- [ ] **Step 1: 失敗するテストを書く**

`derive.test.ts` の import に `deferralLine, deferredIssueCount, DEFERRAL_NOTE` を足し、末尾に追加:

```ts
describe('deferredIssueCount / deferralLine（D17 の別枠）', () => {
  const issue = (
    id: string,
    parentId: string | null,
    events: { kind: 'deferred'; note: string }[] = [],
  ) => ({ id, parentId, text: '課題', events })

  it('見送りを掲げた課題だけを数える（配下の抑制は数えない）', () => {
    const issues = [
      issue('issue_AAAAAAAAAA', null, [{ kind: 'deferred', note: '今回は追わない' }]),
      issue('issue_BBBBBBBBBB', 'issue_AAAAAAAAAA'), // 抑制されるが、自分は掲げていない
      issue('issue_CCCCCCCCCC', null),
    ]
    expect(deferredIssueCount(issues)).toBe(1)
  })

  it('入れ子の見送りはそれぞれ1と数える', () => {
    const issues = [
      issue('issue_AAAAAAAAAA', null, [{ kind: 'deferred', note: '枝ごと' }]),
      issue('issue_BBBBBBBBBB', 'issue_AAAAAAAAAA', [{ kind: 'deferred', note: '個別にも' }]),
    ]
    expect(deferredIssueCount(issues)).toBe(2)
  })

  it('deferralLine はチップと Skill の報告が共有する字面', () => {
    expect(deferralLine(2)).toBe('見送り 2')
    expect(DEFERRAL_NOTE).toBe('見送り配下の問いは要対応に数えません')
  })
})
```

`issue()` の戻り値の形が `IssueNode` と合わない型エラーが出たら、`src/types/issue-tree.ts` の実物を開いて**実物に合わせる**（実物が正）。

- [ ] **Step 2: 落ちることを確認**

Run: `npx vitest run src/modules/issue-tree/derive.test.ts`
Expected: FAIL（`deferredIssueCount` が未定義）

- [ ] **Step 3: `derive.ts` に実装（末尾、`ISSUE_DEFERRED_LABEL` の直後）**

```ts
/**
 * 見送りを掲げた課題の数（UI ノート D17 の別枠「見送り N」）。
 *
 * 数えるのは**自分自身が見送りを掲げている課題**だけ——配下の抑制
 * （`suppressedIssueIds`）は数えない。別枠は「誰が何を落としたか」の台帳
 * なので、入れ子の見送りもそれぞれ1と数える。課題のイベントは見送りしか
 * 無い（スキーマ）ので、判定は「events が空でない」と同値である。
 *
 * **配下に眠る凍結中の問いの数は導出しない**——出す画面が無い（人間の裁定。
 * 別枠は件数だけ）。必要が出たら poseQuestions を抑制なしで回す形で足せる
 */
export function deferredIssueCount(issues: readonly Pick<IssueNode, 'events'>[]): number {
  let count = 0
  for (const node of issues) if (latestKind(node.events) !== null) count += 1
  return count
}

/** 別枠の1行。アプリのチップと Skill の報告が逐語で同じ文字列を出す。0件のときは呼び出し側が行ごと出さない（チップも描かない） */
export function deferralLine(count: number): string {
  return `${ISSUE_DEFERRED_LABEL} ${count}`
}

/** 別枠の注意書き。チップの title と Skill の報告の補足が同じ文を出す */
export const DEFERRAL_NOTE = `見送り配下の問いは${TALLY_TOTAL_LABEL}に数えません`
```

- [ ] **Step 4: テストが通ることを確認**

Run: `npx vitest run src/modules/issue-tree/derive.test.ts`
Expected: PASS

- [ ] **Step 5: Skill 側コピーを `cp` で更新し、バイト一致検査を通す**

```bash
cp src/modules/issue-tree/derive.ts .claude/skills/issue-tree-register/scripts/derive.ts
npx vitest run src/modules/issue-tree/skill-copy.test.ts
```

Expected: PASS

- [ ] **Step 6: Skill の報告に「見送り N」の行を足す**

`issue-tree-write.mjs` の `const tally = D.tallyQuestions(posed);`（:279）の直後に:

```js
const deferredCount = D.deferredIssueCount(normalized.issues);
```

`if (openAt.length) console.log(...)`（:304）の直後に:

```js
if (deferredCount > 0) console.log(`  ${D.deferralLine(deferredCount)}（${D.DEFERRAL_NOTE}）`);
```

- [ ] **Step 7: smoke テストに失敗するテストを足して、通す**

`skill-write.smoke.test.ts` の import を `import { deferralLine, deferredIssueCount, ISSUE_DEFERRED_LABEL, poseQuestions, tallyLine, tallyQuestions } from './derive'` に広げ、describe 内に追加:

```ts
  it('見送りを掲げた課題があると「見送り N」の行が出て、無ければ出ない', () => {
    const deferred = {
      schemaVersion: 2,
      type: 'issueTree',
      title: '検証用',
      issues: [
        {
          id: 'issue_AAAAAAAAAA',
          parentId: null,
          text: '需要検証',
          events: [{ kind: 'deferred', note: '今回は追わない' }],
        },
        { id: 'issue_BBBBBBBBBB', parentId: 'issue_AAAAAAAAAA', text: '認知', events: [] },
      ],
      hypotheses: [],
    }
    const withDeferral = check(deferred)
    expect(withDeferral.status).toBe(0)
    // アプリの導出と逐語で同じ行（「集計行がアプリと一致する」の見送り版）
    expect(withDeferral.stdout).toContain(deferralLine(deferredIssueCount(deferred.issues as never)))

    const none = {
      ...deferred,
      issues: deferred.issues.map((i) => ({ ...i, events: [] })),
    }
    expect(check(none).stdout).not.toContain(ISSUE_DEFERRED_LABEL)
  }, 20000)
```

Run: `npx vitest run src/modules/issue-tree/skill-write.smoke.test.ts`
Expected: 全部 PASS（既存3本＋新1本）。**既存の fixture（FIXTURE）は触らない**——警告6ブロックの検査が崩れる。

- [ ] **Step 8: SKILL.md の報告の説明に1行足す**

`.claude/skills/issue-tree-register/SKILL.md` の `- \`⚠ 要対応 N（仮説なし N ／ 未決 N ／ 保留 N ／ 未判断 N）\`（0 の内訳は出ない）`（:199）の直後に:

```markdown
- 見送りを掲げた課題があるときだけ `見送り N（見送り配下の問いは要対応に数えません）` の行が続く（0件では出ない。字面の正は `scripts/derive.ts` の `deferralLine` / `DEFERRAL_NOTE`）
```

- [ ] **Step 9: コミット**

```bash
npx vitest run src/modules/issue-tree/
git add src/modules/issue-tree/derive.ts src/modules/issue-tree/derive.test.ts .claude/skills/issue-tree-register/scripts/derive.ts .claude/skills/issue-tree-register/scripts/issue-tree-write.mjs .claude/skills/issue-tree-register/SKILL.md src/modules/issue-tree/skill-write.smoke.test.ts
git commit -m "feat(m25): 見送りの別枠集計 deferredIssueCount / deferralLine——Skill の報告にも同じ行を出す"
```

---

### Task 3: 見送りの巡回（open-targets）

**Files:**
- Modify: `src/modules/issue-tree/open-targets.ts`
- Test: `src/modules/issue-tree/open-targets.test.ts`

**Interfaces:**
- Consumes: `latestKind`（`./derive`。値 import になるがアプリ側なので可）・`sameFocus`（同ファイル内 private のまま使う）・`FocusTarget`（`./commands`）
- Produces: `listDeferredTargets(data: Pick<IssueTreeSchemaVersion2, 'issues'>): FocusTarget[]`／`nextDeferredTarget(targets: readonly FocusTarget[], current: FocusTarget | null): FocusTarget | null`。Task 5 が使う

- [ ] **Step 1: 失敗するテストを書く**

`open-targets.test.ts` に追加（import に `listDeferredTargets, nextDeferredTarget` を足す）:

```ts
describe('listDeferredTargets / nextDeferredTarget（見送りの巡回）', () => {
  const issue = (id: string, parentId: string | null, deferred = false) => ({
    id,
    parentId,
    text: '課題',
    events: deferred ? [{ kind: 'deferred' as const, note: '今回は追わない' }] : [],
  })

  it('見送りを掲げた課題だけが行き先（配下の抑制は入らない）', () => {
    const data = {
      issues: [
        issue('issue_AAAAAAAAAA', null, true),
        issue('issue_BBBBBBBBBB', 'issue_AAAAAAAAAA'), // 抑制されるが掲げていない
        issue('issue_CCCCCCCCCC', 'issue_AAAAAAAAAA', true), // 入れ子でも入る
      ],
    }
    expect(listDeferredTargets(data as never)).toEqual([
      { cell: 'issue', index: 0 },
      { cell: 'issue', index: 2 },
    ])
  })

  it('末尾の次は先頭へ戻り、current が列に無ければ先頭、空なら null', () => {
    const targets = [
      { cell: 'issue' as const, index: 0 },
      { cell: 'issue' as const, index: 2 },
    ]
    expect(nextDeferredTarget(targets, null)).toEqual({ cell: 'issue', index: 0 })
    expect(nextDeferredTarget(targets, { cell: 'issue', index: 0 })).toEqual({ cell: 'issue', index: 2 })
    expect(nextDeferredTarget(targets, { cell: 'issue', index: 2 })).toEqual({ cell: 'issue', index: 0 })
    expect(nextDeferredTarget([], null)).toBeNull()
  })
})
```

既存テストのフィクスチャの作り方（`as never` を使うか、完全なオブジェクトを組むか）が上と違ったら**既存の形に合わせる**。

- [ ] **Step 2: 落ちることを確認** — `npx vitest run src/modules/issue-tree/open-targets.test.ts` → FAIL

- [ ] **Step 3: 実装**

`open-targets.ts` の import を `import { latestKind, type PosedQuestions } from './derive'` にし、末尾に追加:

```ts
/**
 * 「次の見送りへ」の巡回列（帯のグレーのチップの行き先。M25 D17）。
 *
 * **見送りを掲げた課題**だけが行き先で、配下（抑制）は入らない。条件は
 * `deferredIssueCount`（derive.ts）と同じ `latestKind` から引く——チップの
 * 数と列の長さが同じ条件から出るので、「見送り 2」と言いながら1件にしか
 * 飛べない、が起きない（`listOpenTargets` と `tallyQuestions` の関係と同じ）
 */
export function listDeferredTargets(
  data: Pick<IssueTreeSchemaVersion2, 'issues'>,
): FocusTarget[] {
  const out: FocusTarget[] = []
  data.issues.forEach((node, index) => {
    if (latestKind(node.events) !== null) out.push({ cell: 'issue', index })
  })
  return out
}

/** `nextOpenTarget` と同じ剰余の巡回。kind の絞り込みが無いだけ */
export function nextDeferredTarget(
  targets: readonly FocusTarget[],
  current: FocusTarget | null,
): FocusTarget | null {
  if (targets.length === 0) return null
  const at = current === null ? -1 : targets.findIndex((t) => sameFocus(t, current))
  return targets[(at + 1) % targets.length]
}
```

- [ ] **Step 4: 通ることを確認** — 同コマンド → PASS

- [ ] **Step 5: コミット**

```bash
git add src/modules/issue-tree/open-targets.ts src/modules/issue-tree/open-targets.test.ts
git commit -m "feat(m25): 見送りの巡回 listDeferredTargets / nextDeferredTarget"
```

---

### Task 4: 帯の `⚠` を `CircleAlert` アイコンへ（決定8）

**Files:**
- Modify: `src/components/MissingTally.tsx`
- Test: `src/components/MissingTally.dom.test.tsx`
- Modify: `src/modules/issue-tree/IssueTreeEditor.dom.test.tsx:607-625`（`⚠` の縛りテスト）

**Interfaces:**
- Consumes: `lucide-react` の `CircleAlert`
- Produces: 帯の見た目の変更のみ（部品の props は不変）。**`tallyLine`（`core/missing-tally.ts` / `derive.ts`）は触らない**——端末に出す文字列は `⚠` のまま

- [ ] **Step 1: `MissingTally.dom.test.tsx` を書き換えて RED にする**

1本目と2本目の it を差し替え:

```tsx
  it('合計と内訳を出す（警告はアイコンで、絵文字は出さない）', () => {
    const { container } = render(<MissingTally tally={TALLY} />)
    expect(screen.getByText('要対応 3')).toBeDefined()
    // ⚠ の絵文字は画面に出さない（M25 決定8。端末に出す tallyLine 側は ⚠ のまま）
    expect(container.textContent).not.toContain('⚠')
    expect(container.querySelector('svg')).not.toBeNull()
    expect(screen.getByText('未定義 2')).toBeDefined()
    expect(screen.getByText('未分類 1')).toBeDefined()
  })

  it('0 件ならアイコン無しの合計だけ', () => {
    const { container } = render(<MissingTally tally={{ total: 0, parts: [] }} />)
    expect(screen.getByText('要対応 0')).toBeDefined()
    expect(container.querySelector('svg')).toBeNull()
    expect(screen.queryByRole('button')).toBeNull()
  })
```

Run: `npx vitest run src/components/MissingTally.dom.test.tsx` → FAIL

- [ ] **Step 2: `MissingTally.tsx` を実装**

import に `import { CircleAlert } from 'lucide-react'` を足し、`:27-29` の `<span>` を差し替え:

```tsx
      <span className="flex items-center gap-1">
        {/* `⚠` の絵文字は使わない（M25 決定8）。OS のカラー絵文字は色が
            rev 9章「色を持つのは意味だけ」の管理の外にあり、SVG なら欠落軸の
            `text-missing` を明示できる。**`tallyLine`（Skill が端末に出す
            文字列）は `⚠` のまま**——端末に SVG は出せない。0 件はアイコンも
            出さない（`⚠` を付けない流儀と同じ）。大きさは帯の文字（text-base
            16px）に合わせて size-4 */}
        {tally.total > 0 && (
          <CircleAlert aria-hidden="true" className="size-4 shrink-0 text-missing" />
        )}
        {`${TALLY_TOTAL_LABEL} ${tally.total}`}
      </span>
```

Run: 同コマンド → PASS

- [ ] **Step 3: `IssueTreeEditor.dom.test.tsx` の縛りテストを分岐の形へ書き換える**

`:616-622` 付近（「⚠ を打ち直さない」のコメントと `getByText(head)`）を差し替え:

```ts
    // **語を打ち直さない。** 帯と `tallyLine`（Skill の報告）は同じ語
    //（`TALLY_TOTAL_LABEL` / `QUESTION_LABELS`）を出すが、接頭辞は M25 決定8 で
    // **意図的に分岐した**——画面は CircleAlert のアイコン、端末は `⚠`
    //（SVG は端末に出せない）。ここでは両側をそれぞれ縛る
    const [head] = tallyLine(t).split('（')
    expect(head).toBe(`⚠ ${TALLY_TOTAL_LABEL} ${t.total}`) // 端末側の字面（据え置き）
    expect(screen.getByText(`${TALLY_TOTAL_LABEL} ${t.total}`)).toBeTruthy() // 画面側は語と数だけ
```

Run: `npx vitest run src/modules/issue-tree/IssueTreeEditor.dom.test.tsx` → PASS

- [ ] **Step 4: 全体を回してコミット**

```bash
npm test
git add src/components/MissingTally.tsx src/components/MissingTally.dom.test.tsx src/modules/issue-tree/IssueTreeEditor.dom.test.tsx
git commit -m "feat(m25): 帯の警告を ⚠ 絵文字から CircleAlert アイコンへ——端末の tallyLine は ⚠ のまま"
```

`npm test` で他モジュールの帯のテストが `⚠` を期待して落ちたら、その一覧を報告してから同じ形（語と数だけを見る）へ直す（Task 1 Step 3 の確認では帯側の `⚠` アサートは `IssueTreeEditor.dom.test.tsx` と `MissingTally.dom.test.tsx` だけのはず）。

---

### Task 5: 帯の見送りチップ（グレー・`StickyNoteOff`・押すと巡回）

**Files:**
- Modify: `src/modules/issue-tree/IssueTreeEditor.tsx`（import・`goToNextOpen` の隣・帯の JSX `:899-903` 付近）
- Test: `src/modules/issue-tree/IssueTreeEditor.dom.test.tsx`

**Interfaces:**
- Consumes: `deferredIssueCount` / `deferralLine` / `DEFERRAL_NOTE`（Task 2）、`listDeferredTargets` / `nextDeferredTarget`（Task 3）、`badgeClass`（既存 import 済み）、`goTo` / `lastFocus`（既存）
- Produces: 帯のチップ（`aria-label="次の見送りへ"`）。実機確認の項目1〜4 の対象

- [ ] **Step 1: 失敗する DOM テストを書く**

`IssueTreeEditor.dom.test.tsx` にフィクスチャと2本を追加（import に `deferralLine, DEFERRAL_NOTE` を足す。型名・`Harness`・`openFile` は同ファイルの既存の形に合わせる）:

```tsx
/** 見送りを掲げた課題が2件（うち1件は入れ子）あるファイル */
const deferredFile = () => ({
  schemaVersion: 2 as const,
  type: 'issueTree' as const,
  title: '見送りの帯',
  issues: [
    { id: 'issue_AAAAAAAAAA', parentId: null, text: '決済', events: [] },
    {
      id: 'issue_BBBBBBBBBB',
      parentId: 'issue_AAAAAAAAAA',
      text: '需要',
      events: [{ kind: 'deferred' as const, note: '今回は追わない' }],
    },
    {
      id: 'issue_CCCCCCCCCC',
      parentId: 'issue_AAAAAAAAAA',
      text: '性能',
      events: [{ kind: 'deferred' as const, note: '機材が無い' }],
    },
  ],
  hypotheses: [],
})

describe('IssueTreeEditor（見送りの別枠チップ。M25 D17）', () => {
  it('見送りを掲げた課題の数を出し、押すとその課題の欄へ視点が飛んで巡回する', () => {
    render(<Harness initial={deferredFile()} />)
    const chip = screen.getByRole('button', { name: '次の見送りへ' })
    expect(chip.textContent).toBe(deferralLine(2))
    expect(chip.title).toBe(DEFERRAL_NOTE)
    fireEvent.click(chip)
    // 行き先の検証は既存テスト「帯のチップを押すと、その種類の次の要対応へ
    // フォーカスが移る」と同じ書き方（document.activeElement の data-cell）に
    // 合わせること。1回目は issues[1]（需要）、もう1回押すと issues[2]（性能）、
    // さらに押すと issues[1] へ戻る
  })

  it('見送りを掲げた課題が無ければチップは出ない', () => {
    render(<Harness initial={openFile()} />)
    expect(screen.queryByRole('button', { name: '次の見送りへ' })).toBeNull()
  })
})
```

Run: `npx vitest run src/modules/issue-tree/IssueTreeEditor.dom.test.tsx` → FAIL（チップが無い）

- [ ] **Step 2: 実装**

`IssueTreeEditor.tsx`:

1. `:1` の lucide import を `import { Plus, StickyNoteOff } from 'lucide-react'` に
2. `./derive` の import に `DEFERRAL_NOTE, deferralLine, deferredIssueCount` を足す
3. `./open-targets` の import に `listDeferredTargets, nextDeferredTarget` を足す
4. `goToNextOpen`（`:818`）の直後に:

```tsx
  const deferredCount = deferredIssueCount(data.issues)

  /** 帯のグレーのチップ。押すと次の「見送りを掲げた課題」へ視点が飛ぶ（末尾なら先頭へ） */
  const goToNextDeferred = (): void => {
    const next = nextDeferredTarget(listDeferredTargets(data), lastFocus)
    if (next !== null) goTo(next)
  }
```

5. 帯の `<MissingTally ... />`（`:899-902`）の直後・`<KeyHints ...>` の前に:

```tsx
          {/* 見送りの別枠（UI ノート D17）。**要対応の外**——`MissingTally` の
              parts は「total の内訳」という契約なので、そこへ混ぜると合計と
              内訳が合わない帯になる。見送りを**掲げた課題の数**だけを出し
              （配下の凍結中の問いは数えない。人間の裁定）、0件なら描かない。
              面は rev 9章の見送りの描き方そのもの（badgeClass('deferred')＝
              surface-muted の面・rule の枠・ink-muted の文字） */}
          {deferredCount > 0 && (
            <button
              type="button"
              className={`shrink-0 transition-colors ${badgeClass('deferred')}`}
              aria-label="次の見送りへ"
              title={DEFERRAL_NOTE}
              onClick={goToNextDeferred}
            >
              <StickyNoteOff aria-hidden="true" className="mr-1 size-3.5 shrink-0" />
              {deferralLine(deferredCount)}
            </button>
          )}
```

アイコンの大きさはチップの文字（`text-sm` 14px）に合わせて `size-3.5`（14px）。

- [ ] **Step 3: 通ることを確認** — `npx vitest run src/modules/issue-tree/IssueTreeEditor.dom.test.tsx` → PASS。`chip.textContent` がアイコンのぶんズレて落ちたら、`textContent` は SVG を含まないので `deferralLine(2)` と一致するはず——落ちる場合は実際の値を確認して**アサーションの側**を `toContain(deferralLine(2))` に緩めず、原因を確認してから直す

- [ ] **Step 4: コミット**

```bash
git add src/modules/issue-tree/IssueTreeEditor.tsx src/modules/issue-tree/IssueTreeEditor.dom.test.tsx
git commit -m "feat(m25): 帯に見送りの別枠チップ——押すと見送りを掲げた課題を巡回（D17）"
```

---

### Task 6: `CellSelect` 部品（ネイティブ select の置換の本体）

**Files:**
- Create: `src/components/CellSelect.tsx`
- Test: `src/components/CellSelect.dom.test.tsx`

**Interfaces:**
- Consumes: `@/components/ui/dropdown-menu` の `DropdownMenu` / `DropdownMenuContent` / `DropdownMenuRadioGroup` / `DropdownMenuRadioItem` / `DropdownMenuTrigger`
- Produces: `CellSelect`（props: `value: string` / `options: readonly string[]` / `labelOf: (v: string) => string` / `onPick: (v: string) => void` / `'aria-label': string` / `'data-cell': string` / `className: string` / `onKeyDown?: (e: React.KeyboardEvent<HTMLButtonElement>) => void`）。Task 7 が2つのエディタから使う

- [ ] **Step 1: 失敗する DOM テストを書く**

`src/components/CellSelect.dom.test.tsx` を新規作成:

```tsx
// @vitest-environment jsdom
import { afterEach, describe, expect, it, vi } from 'vitest'
import { cleanup, fireEvent, render, screen } from '@testing-library/react'
import { CellSelect } from './CellSelect'

afterEach(cleanup)

const OPTIONS = ['screen', 'data', 'undecided'] as const
const LABELS: Record<string, string> = { screen: '画面', data: 'データ', undecided: '未分類' }

function renderSelect(value: string) {
  const onPick = vi.fn()
  const onKeyDown = vi.fn()
  render(
    <CellSelect
      value={value}
      options={OPTIONS}
      labelOf={(v) => LABELS[v] ?? v}
      onPick={onPick}
      aria-label="種別（1行目）"
      data-cell="row1:kind"
      className="w-full"
      onKeyDown={onKeyDown}
    />,
  )
  return { onPick, onKeyDown, trigger: screen.getByRole('button', { name: '種別（1行目）' }) }
}

describe('CellSelect', () => {
  it('トリガーは現在値のラベルと data-cell を持つ', () => {
    const { trigger } = renderSelect('screen')
    expect(trigger.textContent).toBe('画面')
    expect(trigger.getAttribute('data-cell')).toBe('row1:kind')
  })

  it('閉じたまま ↓ で次の値、↑ で前の値。メニューは開かない', () => {
    const { onPick, trigger } = renderSelect('data')
    fireEvent.keyDown(trigger, { key: 'ArrowDown' })
    expect(onPick).toHaveBeenLastCalledWith('undecided')
    fireEvent.keyDown(trigger, { key: 'ArrowUp' })
    expect(onPick).toHaveBeenLastCalledWith('screen')
    expect(screen.queryByRole('menuitemradio')).toBeNull()
  })

  it('端で止まる（ネイティブ select と同じ。循環しない）', () => {
    const { onPick, trigger } = renderSelect('screen')
    fireEvent.keyDown(trigger, { key: 'ArrowUp' })
    expect(onPick).not.toHaveBeenCalled()
  })

  it('Space で開いて項目を選ぶと onPick が走り、現在値に印が付いている', () => {
    const { onPick, trigger } = renderSelect('screen')
    fireEvent.keyDown(trigger, { key: ' ' })
    const current = screen.getByRole('menuitemradio', { name: '画面' })
    expect(current.getAttribute('aria-checked')).toBe('true')
    fireEvent.click(screen.getByRole('menuitemradio', { name: 'データ' }))
    expect(onPick).toHaveBeenCalledWith('data')
  })

  it('Enter は開かずにセルの操作言語へ渡る（ネイティブ select と同じ）', () => {
    const { onKeyDown, trigger } = renderSelect('screen')
    fireEvent.keyDown(trigger, { key: 'Enter' })
    expect(onKeyDown).toHaveBeenCalledTimes(1)
    expect(screen.queryByRole('menuitemradio')).toBeNull()
  })

  it('Alt+↑↓ と Tab は親のセル操作へ渡り、素の ↑↓ は渡らない', () => {
    const { onKeyDown, trigger } = renderSelect('screen')
    fireEvent.keyDown(trigger, { key: 'ArrowDown', altKey: true })
    fireEvent.keyDown(trigger, { key: 'Tab' })
    expect(onKeyDown).toHaveBeenCalledTimes(2)
    fireEvent.keyDown(trigger, { key: 'ArrowDown' })
    expect(onKeyDown).toHaveBeenCalledTimes(2)
  })
})
```

**「Space で開く」が jsdom で開かない場合**: Task 1 Step 3-4 で確認した「既存テストが `KindMenu` をどう開いているか」と同じイベント（`fireEvent.click` / `pointerDown` 等）に合わせる——Radix の開き方の癖は既存テストが正。開き方をイベントごと変えても、「Enter では開かない」のテストは残すこと。

- [ ] **Step 2: 落ちることを確認** — `npx vitest run src/components/CellSelect.dom.test.tsx` → FAIL（ファイルが無い）

- [ ] **Step 3: 実装**

`src/components/CellSelect.tsx` を新規作成:

```tsx
import { useState } from 'react'
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuRadioGroup,
  DropdownMenuRadioItem,
  DropdownMenuTrigger,
} from '@/components/ui/dropdown-menu'

export interface CellSelectProps {
  value: string
  options: readonly string[]
  /** 値 → 表示ラベル（用語集の kindLabel / エラーカタログの resolutionLabel をそのまま渡す） */
  labelOf: (value: string) => string
  onPick: (value: string) => void
  'aria-label': string
  'data-cell': string
  /** トリガーの面。呼び出し側の `cellInput` を渡す（足すのではなく差し替える） */
  className: string
  /**
   * セルの操作言語（Tab・Alt+↑↓ 等）。**素の ↑↓ はこの部品が消費し、
   * ここへは渡らない**——部品が意味を与えた打鍵は親へ渡さない（sequence M1 の教訓）
   */
  onKeyDown?: (e: React.KeyboardEvent<HTMLButtonElement>) => void
}

/**
 * テーブルセルの選択肢（ネイティブ `<select>` の置換。M25 決定4・UI ノート §1.1）。
 *
 * ネイティブをやめたのは**開いたときのリストが OS 描画で styled にできない**ため
 * （閉じた見た目は appearance-none で既にカスタムだった）。開閉は `KindMenu`
 * （IssueTreeEditor.tsx）と同じ Radix の DropdownMenu。あちらは「同時に1つ」を
 * 親の開閉 state で保証するが、こちらはメニューがモーダル（Radix の既定）なので
 * 2つ同時には開けず、state は部品が自分で持てば足りる。
 *
 * **キーボード契約（ネイティブ select の挙動を維持する）:**
 * - **閉じたまま素の ↑↓ ＝ 値切り替え**（端で止まる——循環しない）。
 *   `preventDefault` が Radix の「ArrowDown で開く」既定とページスクロールの
 *   両方を抑える（Radix はユーザーの onKeyDown を先に呼び、defaultPrevented を
 *   尊重する）
 * - **Space / クリック ＝ 開く**（Space はネイティブ select が開く打鍵。
 *   クリックともども Radix の既定に任せる）
 * - **Enter ＝ 開かない。セルの操作言語へ渡す**——ネイティブ select は
 *   Windows では Enter で開かず、Enter は現状 `onCellKeyDown` へ流れている。
 *   Radix の「Enter で開く」既定は preventDefault で降ろす
 * - Alt+↑↓・Tab ほかは `onKeyDown`（セルの操作言語）へそのまま渡す
 *
 * 現在値の印（`menuitemradio` のチェック）を出すのは、これが**値を選び直す**
 * 部品だからである。判断ピッカー（KindMenu）が印を出さないのは、あちらが
 * イベントの追記であって値の選択ではないため——流儀の差は意図
 */
export function CellSelect(props: CellSelectProps) {
  const [open, setOpen] = useState(false)

  const onTriggerKeyDown = (e: React.KeyboardEvent<HTMLButtonElement>): void => {
    if ((e.key === 'ArrowUp' || e.key === 'ArrowDown') && !e.altKey) {
      e.preventDefault()
      const at = props.options.indexOf(props.value)
      const next = props.options[at + (e.key === 'ArrowDown' ? 1 : -1)]
      if (next !== undefined) props.onPick(next)
      return
    }
    if (e.key === 'Enter') {
      // ネイティブ select は Enter で開かない。セルの操作言語（行の追加等）に
      // 使われている打鍵なので親へ渡し、Radix の「Enter で開く」既定は降ろす
      props.onKeyDown?.(e)
      if (!e.defaultPrevented) e.preventDefault()
      return
    }
    props.onKeyDown?.(e)
  }

  return (
    <DropdownMenu open={open} onOpenChange={setOpen}>
      <DropdownMenuTrigger
        type="button"
        aria-label={props['aria-label']}
        data-cell={props['data-cell']}
        className={`${props.className} text-left`}
        onKeyDown={onTriggerKeyDown}
      >
        {props.labelOf(props.value)}
      </DropdownMenuTrigger>
      <DropdownMenuContent align="start">
        <DropdownMenuRadioGroup
          value={props.value}
          onValueChange={(value) => {
            setOpen(false)
            props.onPick(value)
          }}
        >
          {props.options.map((option) => (
            <DropdownMenuRadioItem key={option} value={option}>
              {props.labelOf(option)}
            </DropdownMenuRadioItem>
          ))}
        </DropdownMenuRadioGroup>
      </DropdownMenuContent>
    </DropdownMenu>
  )
}
```

**「素の ↓ でメニューが開かない」のテストが落ちる場合**（Radix が `defaultPrevented` を無視する版だったら）: トリガーの `onKeyDownCapture` で同じ処理をして `e.stopPropagation()` まで行う形へ切り替え、その理由をコメントに書く。**これが決定4 の唯一の実装リスクで、テストが検出器である。**

- [ ] **Step 4: 通ることを確認** — `npx vitest run src/components/CellSelect.dom.test.tsx` → PASS

- [ ] **Step 5: コミット**

```bash
git add src/components/CellSelect.tsx src/components/CellSelect.dom.test.tsx
git commit -m "feat(m25): CellSelect——ネイティブ select 置換の部品。素の↑↓の値切り替えを部品が所有"
```

---

### Task 7: 2つのエディタを `CellSelect` へ配線

**Files:**
- Modify: `src/modules/glossary/GlossaryEditor.tsx:366-406`（種別セル）
- Modify: `src/modules/error-catalog/ErrorCatalogEditor.tsx:264-305` 付近（対応レベルセル）
- Test: `src/modules/glossary/GlossaryEditor.dom.test.tsx`／`src/modules/error-catalog/ErrorCatalogEditor.dom.test.tsx`（Task 1 Step 3-3 で列挙した箇所）

**Interfaces:**
- Consumes: `CellSelect`（Task 6）、各エディタの `cellInput` / `KIND_OPTIONS` / `kindLabel` / `LEVEL_OPTIONS` / `resolutionLabel` / `onCellKeyDown`（既存）
- Produces: 2つのセルの操作は据え置きのまま、開いたリストが styled になる

- [ ] **Step 1: 用語集を差し替える**

`GlossaryEditor.tsx` の `<select ...>...</select>`（`:367-396`）を差し替え:

```tsx
                    <CellSelect
                      className={`${cellInput} appearance-none pr-6`}
                      aria-label={`${FIELD_LABELS.kind}（${row}行目）`}
                      data-cell={cellId(rowKey, 'kind')}
                      value={term.kind}
                      options={KIND_OPTIONS}
                      labelOf={kindLabel}
                      onPick={(kind) => updateTerm(index, { kind: kind as Term['kind'] }, null)}
                      onKeyDown={(e) =>
                        onCellKeyDown(
                          e,
                          { index, visiblePos, field: 'kind' },
                          {
                            editing: false,
                            fieldEmpty: false,
                            deletableField: false,
                            caretAtStart: true,
                            caretAtEnd: true,
                            // 素の↑↓は CellSelect が値切り替えに消費する（ここへ届かない）
                            arrowsOwnedByField: true,
                          },
                        )
                      }
                    />
```

import に `import { CellSelect } from '@/components/CellSelect'` を足す。**直後の矢印 SVG（`:400-406`）はそのまま残す**（`td` の `relative` 基準の absolute 配置で、トリガーが button になっても同じ位置に出る）。`KIND_OPTIONS` の型が `readonly string[]` に合わなければ `options={KIND_OPTIONS as readonly string[]}`。

- [ ] **Step 2: 用語集の既存テストを直して緑にする**

Run: `npx vitest run src/modules/glossary/`
Expected: `fireEvent.change` で値を変えていたテストが FAIL する。それらを **CellSelect の操作**（閉じたまま `fireEvent.keyDown(trigger, { key: 'ArrowDown' })`、または開いて `menuitemradio` を click）に書き換える。**アサーション（値が変わったこと・保存が走ったこと）は変えない**——変えたのは操作の経路だけである。全部緑になるまで。

- [ ] **Step 3: エラーカタログを同じ形で差し替える**

`ErrorCatalogEditor.tsx` の `<select ...>...</select>`（`:267-292`）を差し替え（`cellNode` 内なので変数は `at` / `entry` / `rowKey` / `label`）:

```tsx
          <CellSelect
            className={`${cellInput} appearance-none pr-6`}
            aria-label={label}
            data-cell={cellId(rowKey, field)}
            value={entry.resolutionLevel}
            options={LEVEL_OPTIONS}
            labelOf={resolutionLabel}
            onPick={(level) => updateLevel(at.index, level as ErrorEntry['resolutionLevel'])}
            onKeyDown={(e) =>
              onCellKeyDown(e, at, {
                editing: false,
                fieldEmpty: false,
                deletableField: false,
                caretAtStart: true,
                caretAtEnd: true,
                // 素の↑↓は CellSelect が値切り替えに消費する（ここへ届かない）
                arrowsOwnedByField: true,
              })
            }
          />
```

矢印 SVG（`:295-` ）は同じく残す。

- [ ] **Step 4: エラーカタログの既存テストを直して緑にする** — `npx vitest run src/modules/error-catalog/` → Step 2 と同じ要領で全部緑に

- [ ] **Step 5: 全体を回してコミット**

```bash
npm test && npx tsc -b
git add src/modules/glossary/GlossaryEditor.tsx src/modules/glossary/GlossaryEditor.dom.test.tsx src/modules/error-catalog/ErrorCatalogEditor.tsx src/modules/error-catalog/ErrorCatalogEditor.dom.test.tsx
git commit -m "feat(m25): 用語集の種別・エラーカタログの対応レベルを CellSelect へ——開いたリストが styled になる"
```

（テストの修正がこの2ファイル以外に及んだら `git add` に足す。）

---

### Task 8: 角丸の統一と許可リスト検査（決定5）

**Files:**
- Modify: `src/styles/conventions.test.ts`（`sourceFiles` の引数化＋新 describe）
- Modify: `src/components/badge-styles.ts:30`
- Modify: `src/modules/issue-tree/IssueTreeEditor.tsx`（`DEFER_TRIGGER_FACE` と周辺 JSDoc）
- Modify: `src/modules/issue-tree/HypothesisRow.tsx:245`
- Modify: `src/components/ui/button.tsx`／`src/components/ui/dropdown-menu.tsx`／`src/components/ui/alert-dialog.tsx`

**Interfaces:**
- Consumes: `sourceFiles` / `stripComments` / `relative`（`conventions.test.ts` 内の既存関数）
- Produces: 許可リスト検査「角丸の段（M25 決定5）」。以後 `rounded-sm` / `rounded-md` / `rounded-full`（＋方向付き `-sm`/`-md`）以外は赤くなる

- [ ] **Step 1: 検査を先に書いて RED にする**

`conventions.test.ts` の `sourceFiles` を引数付きに変える（既定は現状と同じ）:

```ts
function sourceFiles(excluded: readonly string[] = EXCLUDED): string[] {
```

（内部の `EXCLUDED.some(...)` を `excluded.some(...)` に。）そして新しい describe を追加:

```ts
describe('角丸の段（M25 決定5）', () => {
  // 部品・セル・チップ＝rounded-sm（6px）／浮遊面（メニュー・ダイアログ）＝
  // rounded-md（8px）／意図した円＝rounded-full。裸の `rounded`（radius 倍率制の
  // 外にある固定 4px）・任意値・他の段は弾く。**sm と md の使い分けの当否は
  // 機械では判定できない**——この検査が守るのは「3語（＋方向付き）以外が
  // 現れない」ことだけで、使い分けはレビューと実機が守る
  const ALLOWED = /^rounded-(?:sm|md|full)$|^rounded-(?:t|b|l|r|tl|tr|bl|br)-(?:sm|md)$/

  it('rounded 系は rounded-sm / rounded-md / rounded-full（と方向付きの -sm/-md）だけ', () => {
    const offenders: string[] = []
    // **ui/ を除外しない。** EXCLUDED の理由（段・行間の規約は自作コードに課す）は
    // タイポグラフィの話で、角丸は M25 で ui/ を意図して改造した（M23 が button.tsx の
    // text-base / h-9 を改造したのと同じ扱い）。shadcn を将来更新したときに
    // rounded-lg が黙って戻るのを、この検査が捕まえる
    for (const file of sourceFiles([])) {
      const stripped = stripComments(readFileSync(file, 'utf8'))
      stripped.split('\n').forEach((line, index) => {
        for (const m of line.matchAll(/\brounded(?:-[\w[\]().,%*-]+)?/g)) {
          if (!ALLOWED.test(m[0])) {
            offenders.push(`src/${relative(file)}:${index + 1}  ${m[0]}`)
          }
        }
      })
    }
    expect(
      offenders,
      `角丸は rounded-sm（部品）/ rounded-md（浮遊面）/ rounded-full（円）だけ（M25 決定5）:\n${offenders.join('\n')}`,
    ).toEqual([])
  })
})
```

Run: `npx vitest run src/styles/conventions.test.ts`
Expected: **FAIL し、offenders に少なくとも次が並ぶ**——`badge-styles.ts` の `rounded`、`IssueTreeEditor.tsx` の `rounded`（`DEFER_TRIGGER_FACE`）、`HypothesisRow.tsx` の `rounded`、`ui/button.tsx` の `rounded-lg`×複数と `rounded-[min(...)]`×4、`ui/dropdown-menu.tsx` の `rounded-lg`×2 と `rounded-md`×4、`ui/alert-dialog.tsx` の `rounded-xl`・`rounded-b-xl`・`rounded-md`。**この一覧が Task 1 Step 3-2 の grep と食い違ったら報告**（検査の正規表現の穴か、grep の見落としのどちらか）。

- [ ] **Step 2: アプリ側の裸 `rounded` を `rounded-sm` へ（4px→6px の見た目変更）**

1. `badge-styles.ts:30` の `base`: `rounded` → `rounded-sm`
2. `IssueTreeEditor.tsx` の `DEFER_TRIGGER_FACE`（`:198`）: `rounded` → `rounded-sm`。**バッジとトグルは幾何の対**（JSDoc に明記あり）なので、**1 と 2 は同じコミットで変える**。あわせて `TRIGGER_BASE` の JSDoc（`:168-171`）の「見送りバッジ（`rounded`）」と `DEFER_TRIGGER_FACE` の JSDoc（`:189`）の `rounded` の言及を `rounded-sm` に直す
3. `HypothesisRow.tsx:245`（展開パネルの面）: `rounded` → `rounded-sm`

- [ ] **Step 3: ui/ を2段へ**

1. `ui/button.tsx:8`: `rounded-lg` → `rounded-sm`（ボタンは部品）
2. `ui/button.tsx:26,27,31,33`: `rounded-[min(var(--radius-md),10px)]` / `rounded-[min(var(--radius-md),12px)]` と `in-data-[slot=button-group]:rounded-lg` のトークンを**削除**（base の `rounded-sm` に任せる。小さいボタンだけ半径を落とす shadcn の仕掛けは、一律 6px にした時点で用が無い）
3. `ui/dropdown-menu.tsx:44,245`（`Content` / `SubContent` の面）: `rounded-lg` → `rounded-md`（浮遊面）
4. `ui/dropdown-menu.tsx:74,96,140,227`（`Item` / `CheckboxItem` / `RadioItem` / `SubTrigger`）: `rounded-md` → `rounded-sm`（浮遊面の中の部品）
5. `ui/alert-dialog.tsx:59`（`Content`）: `rounded-xl` → `rounded-md`（浮遊面）
6. `ui/alert-dialog.tsx:92`（`Footer`）: `rounded-b-xl` → `rounded-b-md`（`Content` の角と揃える）
7. `ui/alert-dialog.tsx:108`（`Media` のアイコン枠）: `rounded-md` → `rounded-sm`（面の中の部品）

- [ ] **Step 4: 検査が GREEN になり、全体も緑のままであることを確認**

```bash
npx vitest run src/styles/conventions.test.ts
npm test
```

Expected: どちらも PASS。DOM テストがクラス名 `rounded` の字面で落ちたら（Task 1 Step 3-2 で列挙済みのはず）、期待値を新しいクラス名に追従させる。

- [ ] **Step 5: コミット**

```bash
git add -A
git commit -m "feat(m25): 角丸を2段＋円に統一——rounded-sm/md/full の許可リスト検査を ui/ 含む母集合で追加"
```

---

### Task 9: 文書への反映（マイルストーン完了の3箇所＋UI ノート）

**Files:**
- Modify: `docs/facet-UI設計ノート.md`（D17・D18・U4・§5・§8）
- Modify: `docs/missing-semantics.md`（規約4 の節）
- Modify: `docs/overview-rev.md`（9章）
- Modify: `docs/open-issues.md`
- Modify: `docs/README.md`（地図の表）
- Create: `docs/history/m25-core-ui-note-f.md`

**Interfaces:**
- Consumes: Task 2〜8 の確定した実装（文言・パスは**実物から写す**）
- Produces: 「正」の文書群の現行化。**rev への反映は完了コミットで済ませ、TODO として申し送りに残さない**（CLAUDE.md）

- [ ] **Step 1: UI ノートを更新する**

1. **U4（`### U4. 破線エッジの意味`）**: 見出しを `### U4. 破線エッジの意味 ← M25 で決着` にし、本文の末尾に追記:

```markdown
**M25 での決着（実装からの言語化）:** 課題ツリーのエッジは**すべて親子関係**である。
別種の参照を表すエッジは存在しない。破線は関係の**種類**ではなく抑制の**状態**を
運ぶ——「祖先の見送りで抑制された配下へ入る線」だけが破線になり（色・濃さは
`stroke-rule` のまま。線の切れ方だけで区別する）、**見送りを掲げた当の箱へ入る線は
実線のまま**である（見送りは判断の表明であって「もう見なくてよい枝」ではない）。
正は `src/modules/issue-tree/IssueTreeEdges.tsx`（`SUPPRESSED_DASH` とその JSDoc。
「自分自身を含む集合を渡さないこと」という M3 の退行の記録ごと）。
心配していた「親子と参照が同じ視覚チャネルで混同される」は、参照が存在しない以上、
起きない。
```

2. **D17**: 末尾に追記小節:

```markdown
**M25 での実施:** 集計の前半（要対応から外す）は**実装済みだった**——`poseQuestions`
（`src/modules/issue-tree/derive.ts`）は抑制配下では問いを立てず、`tallyQuestions` は
立った問いだけを数える。M25 が足したのは**別枠の側**である。別枠は
`見送り N`（`deferredIssueCount` / `deferralLine`）——**件数だけで、配下の凍結中の
問いの内訳・総数は出さない**（人間の裁定。上の確定仕様の `（未決 2件）` の部分は
実装していない）。チップはグレー（`badgeClass('deferred')`）で押すと見送りを掲げた
課題を巡回し、`title` に「見送り配下の問いは要対応に数えません」（`DEFERRAL_NOTE`）
を出す。登録 Skill の報告（`issue-tree-write.mjs --check`）も同じ行を出す。
```

3. **D18**: 「未確認事項」の段落の直後に追記:

```markdown
**M25 での決着:** 上の確定ルールは**実装済みである**——正は
`src/modules/issue-tree/derive.ts` の `suppressedIssueIds`（祖先を遡る導出。
フィールドは持たず、循環しているファイルでも止まらない）。未確認だった破線の意味は
U4 の決着（親子エッジ。破線＝抑制の状態）で解消した。
```

4. **§5「課題ツリー」の未確認事項**: 破線エッジの箇条書きの末尾に `——**M25 で決着**（U4 の決着の節を見よ。親子エッジであり、別種の参照は存在しない）` を足す。
5. **§8 の実施状況の行**: `残るのは**優先6 の高さ（省略）**・優先7（U4 待ち）・優先8（U1 待ち）・優先10。` を次に差し替え:

```markdown
**優先7・10 は M25 で消化した**（D17 の別枠は件数のみ・D18 は文書固定・select 置換・
角丸統一。U4 も M25 で決着）。残るのは**優先6 の高さ（省略。M24 で撤回済み）**と
優先8（U1 待ち）。
```

6. **§8 の着手条件の表**: `| 優先7 | **U4**（破線エッジの意味）の確定 |` の行に `——M25 で確定済み` を追記。

- [ ] **Step 2: `docs/missing-semantics.md` の規約4 に2項を追記**

規約4 の本文（「抑制された配下（課題ツリーの見送り）は数えない。」の文の後）に:

```markdown
**見送りの別枠（M25。UI ノート D17・D18）:**

- **見送り配下の問いは要対応に数えない**（`poseQuestions` が抑制配下では問いを
  立てない）。ただし**存在は消さない**——見送りを掲げた課題の数を、帯の要対応の隣に
  グレーの別枠 `見送り N` として出す（`derive.ts` の `deferredIssueCount` /
  `deferralLine`。0件では出さない。チップの `title` と Skill の報告が同じ注意書き
  `DEFERRAL_NOTE` を出す）。凍結中の問いの内訳・総数は出さない（件数だけ。人間の裁定）
- **見送り配下は祖先から導出し、フィールドを持たない**（`suppressedIssueIds`。D18）。
  子に見送りフラグを複製しない——親の解除で子が取り残される嘘を作らないため
```

あわせて規約4 の1行目 `「⚠ 要対応 N ＋内訳チップ（押すと次へ）」` を `「要対応 N（CircleAlert のアイコン付き）＋内訳チップ（押すと次へ）」` に直し、直後に `（M25 で ⚠ の絵文字はアイコンになった。**端末に出す `tallyLine` は `⚠` のまま**——SVG は端末に出せない）` を足す。

- [ ] **Step 3: `docs/overview-rev.md` 9章に2箇所追記**

1. **判断軸の行**（`### 決定：役割トークンとパレットの分離` の箇条書き内、「見送りは専用トークンを持たず `surface-muted` の面＋`rule` の枠＋`ink-muted` の文字」の直後）に追記:

```markdown
見送りの**集計は要対応と別枠**（M25。UI ノート D17）——見送り配下の問いは要対応に数えず、見送りを掲げた課題の数だけを帯のグレーのチップ `見送り N` が持つ（`docs/missing-semantics.md` 規約4）。
```

2. **「確定要素」の箇条書き**に新しい1項（「数字は等幅」の項の隣あたり）:

```markdown
- **角丸は2段＋円**（M25。UI ノート §1.1）。部品・セル・チップ・バッジ・ボタン＝`rounded-sm`／浮遊面（メニュー・ダイアログ）＝`rounded-md`／意図した円（トグルのつまみ・ドット）だけ `rounded-full`。実効値は `src/index.css` の `--radius: 0.625rem` ×倍率（sm＝6px・md＝8px）で、**基準値は M25 では動かしていない**（吟味はダークと同じ持ち越し）。裸の `rounded`（倍率制の外の固定 4px）・任意値・他の段は `src/styles/conventions.test.ts` の許可リスト検査が弾く——**この検査だけは shadcn の生成物（`src/components/ui/`）も走査対象に含む**（段・行間の検査が ui/ を除外する理由はタイポグラフィの話で、角丸は ui/ を意図して改造したため。shadcn を更新したとき `rounded-lg` が黙って戻るのを捕まえる）。`sm` と `md` の使い分け（部品か浮遊面か）は機械では判定できないので、そこはレビューと実機が守る。帯の `⚠` は M25 で `CircleAlert`（lucide）になった——絵文字は色が「色を持つのは意味だけ」の管理の外にあり、SVG なら `text-missing` を明示できる（端末に出す `tallyLine` は `⚠` のまま）
```

- [ ] **Step 4: `docs/open-issues.md` を更新**

1. **消す**: 「デザイン」の `**UI ノートの F（見送り集計の2段構え・select 置換・角丸統一）が未着手**`（`[M21]`。`:121`）——**消す前に、U4 の答えが UI ノート側（Step 1）に書かれていることを確認する**（この項は U4 の答えの唯一の置き場を兼ねていた）
2. **足す**: 「次に手を付ける候補」の7件目として:

```markdown
7. **M25（UI ノート F の決着——見送りの別枠集計・⚠ のアイコン化・select 置換・角丸統一）の実機確認が未実施**（[`history/m25-core-ui-note-f.md`](history/m25-core-ui-note-f.md) にチェックリストを空のまま写してある）——サブエージェントは Tauri の GUI を操作できないため。**このマイルストーンは「見た目」そのものが成果物なので、確認しないと成否が分からない種類である**（issue-tree-m3・M21〜M24 と同じ扱い）。とくに **角丸はテストがクラス名しか見ておらず**（`rounded-lg` 10px → `rounded-sm` 6px 等の見え方は機械検査の外）、**select の「閉じたまま ↑↓」は jsdom で通っているが実機の WebView では未確認**である。済んだらこの項を消すこと `[M25]`
```

3. 冒頭の「最終更新」の段落を M25 完了時点の記述で書き足す（既存の流儀——何を消し・足し・書き換えたかを1段落で）。

- [ ] **Step 5: `docs/README.md` の地図を更新**

1. UI ノートの行（`:10` 付近）: 「残る」の列挙から F を外し、現状（残るは D の高さと E）へ直す
2. マイルストーン表に M25 の行を足し、通し番号の範囲を `M1`〜`M25` に直す

- [ ] **Step 6: `docs/history/m25-core-ui-note-f.md` を新規作成**

M24 の申し送り（`m24-core-node-width-lock.md`）と同じ構成で書く——冒頭（何のマイルストーンか・スペックと計画へのリンク・コミット範囲・テスト件数の増減）／「何を作ったか」／「実装で確定した事項」（**計画と実物の食い違いがあったらここに列挙**。とくに Task 6 Step 3 のフォールバックを踏んだかどうか）／「直さずに残したもの」／「実機確認について」。実機確認の節には設計スペック「検証」の11項目を**空のチェックリストのまま**写し、末尾に:

```markdown
**実機確認とこのタスクは束ねていない。** チェックリストは空のままで、消し込みの管理は
[`../open-issues.md`](../open-issues.md) の「次に手を付ける候補」7件目が持つ。
確認で出た変更要求は計画外修正として扱う（症状と人間の言葉を分けて記録すること）。
```

- [ ] **Step 7: コミット**

```bash
git add docs/
git commit -m "docs(m25): 完了時の3箇所＋UI ノート——U4 決着・D17/D18 固定・角丸規約を rev 9章へ"
```

---

### Task 10: 通しの検証

**Files:** 変更なし（検証のみ。直しが出たらその場で）

- [ ] **Step 1: 全部を回す**

```bash
npm test
npx tsc -b
npm run lint
```

Expected: すべて緑・exit 0。**対象を絞らない**（教訓——絞ると依存の見落としを隠す）。

- [ ] **Step 2: 作業ツリーが計画どおりの状態であることを確認**

```bash
git status --short   # 空であること
git log --oneline issue-tree/main..HEAD
```

Expected: スペック3コミット＋実装6コミット＋文書1コミット前後が並ぶ。`sample-project/` に差分が無いこと。

- [ ] **Step 3: 結果を報告する**

テスト件数の増減・計画との差分（報告した矛盾の一覧）・実機確認が未実施のまま残ること（`open-issues.md` 候補7件目）を報告にまとめる。**実機確認は人間の作業**であり、このタスクには含めない。

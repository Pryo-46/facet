# 実装スコープ定義書：用語集エディタ ＋ コア機構

作成日: 2026-08-02
位置づけ: **実装計画（Superpowers writing-plans 等）への入力。** 設計の「なぜ」は overview-rev.md と glossary-session-notes.md にあり、本書はそこから「今回何をどの順で作るか」だけを切り出したもの。設計判断の蒸し返しは本書の管轄外。

前提ドキュメント（すべて既に確定済み。読むこと）:

- `overview-rev.md` — 全体方針。3〜10章が設計制約の本体（本書では以下 **rev** と表記）
- `glossary-session-notes.md` — 用語集の全論点の決定と理由
- `schemas/glossary.schema.json` — 用語集スキーマ（正）。**コピーを作らないこと**（Skill 側もこの実体を読む）
- Skill 一式（`SKILL.md` / `glossary-write.mjs` / `new-id.mjs`）— 既に動作するAI側の実装。**アプリはこれと正規形が完全一致していなければならない**

---

## 1. スコープ

### 今回作るもの

**用語集エディタ1本と、それが依存するコア機構のみ。**

### 今回作らないもの

- **他3ツール（ロジックツリー / シーケンス / 状態遷移）は一切作らない。** rev に4ツール記載があるが、3つは設計未了であり計画にも含めない
- 表記ゆれ検知の「指摘（suggestion）」レイヤ（照合対象となる他ツールが存在しないため実装しても検証できない）
- AI層の用語チェック（Skill側の管轄）
- 用語のインライン登録コンポーネント（呼び出す側の他ツールが無いため。ただし**用語集ファイルの自動生成**だけはコア側に必要）

### ただし守るもの

**モジュール規約（rev 6章）に従った構造で作る。** 用語集1本しか無くても、コアは「type からモジュールを引く」レジストリ経由で動かす。ここを近道すると、2本目のツールでコアの作り直しになる。用語集モジュールは規約6点セット（type識別子 / JSON Schema / エディタコンポーネント / 整合性検証ルール / 出力ロジック / マイグレータ）を実装する。マイグレータは初版につき恒等（枠だけ）。

---

## 2. 技術セットアップの前提（**完了済み。やり直さないこと**）

計画フレームワークが想定する出発点（リポジトリが存在し、テストがグリーンで通る状態）は M0 として実施済み。**計画の第1タスクを環境構築にしないこと。** 現状は以下。

- Tauri 2 ＋ Vite ＋ React ＋ TypeScript。`npm run tauri dev` で起動する
- Tailwind CSS ＋ shadcn/ui（Button のみ導入済み）。役割トークンは `src/index.css` に枠だけあり、値は仮置き
- Vitest。`npm test` で空のテストが1本グリーン
- `schemas/glossary.schema.json` → `src/types/glossary.ts` の型生成が `npm run gen:types` で通る
- `.gitattributes` に `*.json text eol=lf`（**必須。** Windows の autocrlf 下では、これが無いと commit 時に CRLF 変換され全行 diff になる。正規形の LF が Git 上で無意味になる）
- Tauri の fs / dialog プラグインを capabilities まで導通済み

計画時に前提を取り違えやすい点:

- **型定義は書かない。** `src/types/glossary.ts` はスキーマからの生成物で、**コミットしていない**（`.gitignore` 済み）。`prepare` / `predev` / `prebuild` / `pretest` で自動再生成されるため、スキーマと型がズレることはない。手書きの型と二重管理しない
- **Tailwind は v4** で、`tailwind.config.js` も `postcss.config.js` も存在しない。トークンは CSS の `@theme inline` で定義する。旧来の JS 設定を前提にした手順は通らない
- **shadcn/ui は Radix ベース（`-b radix`）** で初期化済み。コンポーネント追加は `npx shadcn@latest add <名前>` で、必要になった時点で足す
- `src/components/ui/**` は生成物のため lint 対象外にしてある。手で整形しない

Rust は原則書かない（rev 7章）。ロジックは全て TypeScript 側。Tauri コマンドは1つも定義しておらず、**追加もしない**（ファイルアクセスは fs / dialog プラグインで足りる）。ただし capabilities の scope は現状ダイアログで選んだファイルまでなので、**フォルダ走査（M2 の検証スコープ、M5 の監視）には scope の追加が要る**。

---

## 3. 正規形（アプリのシリアライザが厳守する値）

Skill 側の `glossary-write.mjs` と**完全一致**させること。1文字でもずれると、Skill が書いたファイルをアプリが開いて保存するたびに全行 diff が出て、「変更履歴を仕様の変更履歴として読める」という目的（rev 5章）が壊れる。

| 項目       | 値                                                       |
| ---------- | -------------------------------------------------------- |
| キー順     | **JSON Schema の properties 記載順から実行時に導出する** |
| インデント | スペース2                                                |
| 改行コード | LF                                                       |
| 末尾改行   | あり                                                     |
| BOM        | なし（読み込み時は除去する）                             |
| 非ASCII    | エスケープしない（日本語をそのまま書く）                 |

**キー順をアプリ側にハードコードしないこと。** Skill 側はスキーマから実行時導出しているため、ハードコードするとスキーマ改訂時に静かにずれる。`$ref` の解決を含む導出ロジックが必要（実装の参考: `glossary-write.mjs` の `reorder` / `deref`）。

**テストで担保する**: Skill が書き出したファイルをアプリが読み→保存して、バイト単位で不変であること。これはこのプロジェクト最大のリスク箇所であり、回帰テストの筆頭。

---

## 4. 実装順（マイルストーン）

原則は **「歩けるスケルトンを最初に一周させる」**。最もリスクの高い機構（正規化・検証・ファイルI/O）を最初に実証し、その後で独立した層を足す。UIの作り込みを先行させない。

### M1: 歩けるスケルトン（最優先）

フォルダを開く → JSON 読込 → スキーマ検証 → リスト描画 → 編集 → 自動保存（正規形で書き込み）→ **Git diff が編集箇所だけに出る**。

完了条件:

- Skill が生成した用語集ファイルをアプリで開いて1フィールド編集し、`git diff` が該当行だけに出ること（バイト一致テストがグリーン）
- スキーマ検証に落ちるファイルが「開けない」（レベル1拒否）として扱われること

この一周が通れば、プロジェクト固有の技術リスクはほぼ消える。

### M2: 検証レイヤの完成

- **整合性検証（レベル2＝受け入れて赤表示）**: ID重複 / alias重複（用語内・用語間）/ alias と他用語の name 衝突 / **name 同士の重複** / 用語集の単一性違反
  - **name 重複は Skill 側スクリプトにも現状無い検査。今回追加する**（同名2件は「この語を正式名とする」という宣言の矛盾であり、追記のたびに発生確率が上がる。スキーマでは防げない）
- warning 可視化: `kind === "undecided"` は種別セル、`definition === ""` は定義セルに（セル単位）
- 検証スコープはプロジェクト全体。フォルダ内全 JSON を走査し、ファイル一覧に赤バッジ
- 未知 type / 未知 schemaVersion のファイルは「一覧表示のみ・編集不可」（クラッシュしない）

完了条件: 意図的に壊したファイル群（ID重複・name重複・alias衝突・用語集2個・未知type・スキーマ違反）を用意し、それぞれ期待どおりの区分で扱われること。

### M3: エディタの操作性

- キーボード操作を**共通フック/モジュールに一元化**（rev 10章。ツールごとの自前実装は禁止）
- **修飾キーはプラットフォーム抽象を通す**（Windows: Ctrl / Alt、macOS: Cmd / Option）。各コンポーネントが `Ctrl` を直接見ない。Tauri なので macOS で動く可能性は残っており、Ctrl 直書きだと Undo が効かない。M0 時点の動作確認は Windows のみ。以下のキー表記は Windows 側の呼び方
- 階層・リスト系ファミリー標準: Enter＝直後に行追加、空欄 Backspace＝行削除、Alt+↑↓＝並び替え、矢印＝行間移動、**Tab＝セル間移動**（用語集に「子」の意味が無いため）
- **IME 対応**（composition イベント判定。変換確定 Enter の誤爆防止。日本語入力アプリ最大の地雷）
- 境界規則: モーダル表示中・テキスト編集中は操作言語を停止（例外は編集境界キー Enter/Tab/Esc/空欄Backspace のみ）
- 検索（name / aliases / definition 横断）＋種別フィルタ。**導出表示中は Alt+↑↓ を無効化**
- Undo/Redo（Ctrl+Z / Ctrl+Shift+Z、ファイル単位・メモリ内）

### M4: ファイル一覧の額縁とファイル操作

- 新規作成（type選択）/ 削除（OSゴミ箱へ。完全削除しない）/ 赤バッジ表示
- 用語集0個の状態からの自動生成

### M5: 外部変更検知

- **フォルダ単位**で監視（外部リネームも検知するため）
- 自己書き込みの除外（検知ループの構造的排除）
- 未保存編集なし → 再読込＋トースト通知。**Undo履歴は破棄**し、**取り込み前の内容を一時退避してトーストに「取り込み前に戻す」を出す**
- 未保存編集あり → 二択ダイアログ（自分の編集で上書き / 外部変更を取り込む）。マージUIは作らない

### M6: NotePM 向け Markdown 出力

- 種別グルーピングの表形式。`##`＝title、`###`＝種別グループ（h1 は使わない。NotePM 目次は h1〜h3）
- 空の種別は見出しごと省略。グループ順は enum 定義順で固定。グループ内はデータ配列順
- 列: 名称／種別／定義／別名／備考（IDは出さない）
- `definition` 空は `（未定義）`、`undecided` は `### 未分類` として**出力にも明示する**
- クリップボードコピー ＋ .md 書き出しの両方

### M7: デザイントークン整備

- 役割トークン（`ink` / `warning` / `ok` ＋無彩色系）を Tailwind 設定に定義。**色値の直書き禁止**
- ライト/ダークは同じ役割トークンにモード別パレットを割り当て（反転による自動生成はしない）
- 現行3色（#22303C / #C73E3A / #2C7A6E）はライト側の**仮置き**。コード上も仮置きと明示
- 方眼紙背景・フォントもトークン経由
- **フォントは明示的なスタックを書く。** 現在の `--font-sans` は shadcn プリセットが入れた Geist で、**日本語グリフを持たない**（英数字だけ Geist、日本語は OS 既定にフォールバックする混在状態）。フォールバック任せにすると Windows は Yu Gothic UI、macOS は Hiragino Sans となり、**表形式の列幅と行の詰まり方がプラットフォームで変わる**。`system-ui, -apple-system, 'Segoe UI', 'Hiragino Sans', 'Yu Gothic UI', 'Noto Sans JP', sans-serif` のように書けば、フォールバックはグリフ単位に効くので英数字は各OSのUIフォント、日本語は各OSの標準日本語フォントに振り分かる
- **日本語 Web フォントは同梱しない。** Noto Sans JP は丸ごとで数MB、サブセット化すると「どの字を含めるか」の管理が発生する。ユーザーが入力する用語にどの漢字が来るか事前に分からないため、サブセットは破綻する。完全な同一描画は諦め、各OSでネイティブに見えることを取る

M7 は M1〜M3 と並行してよいが、色値の確定はドッグフーディング後（rev 9章）。

---

## 5. 計画立案時の注意

- **設計判断を蒸し返さない。** MCP不採用 / DB不採用 / tags除外 / condition保留 / 自動保存＋Git の二層構造は決定済み（理由は rev・session-notes 参照）
- **ディレクトリ構成・状態管理方式・コンポーネント分割は計画側で決めてよい**（本書では規定しない）。ただしモジュール規約の境界（コア / 用語集モジュール）は跨がないこと
- **テストを書く価値が特に高い箇所**: 正規形のバイト一致、スキーマ検証の合否判定、整合性検証の各ルール、マイグレータの恒等性。いずれも期待値が完全に決定的で、かつ人間の目視では確認しにくい

---

## 6. 本書の確定に伴う rev への反映事項

- **5章**: 「シリアライズは決定的にする」の具体値を本書3節の表で確定（rev は具体値未定のまま「Skillセッションで確定させ本章に反映」としていた）。あわせてキー順は**スキーマからの実行時導出**である旨を明記
- **5章**: 整合性検証の項目に **`name` 重複** を追加
- **7章**: プロジェクト雛形の要件として `.gitattributes` の `*.json text eol=lf` を追記

---

## 7. M1 完了に伴う申し送り（2026-08-02 追記）

M1 は実装・レビュー・E2E 完了（PR #2）。完了条件2つ（バイト一致で編集行だけの diff／スキーマ違反のレベル1拒否）は実機確認済み。実装時のレビューで出た残件を後続マイルストーンの計画入力として記録する。

### M2 で扱うもの

- **ウィンドウ close 時の flush**（レビュー指摘・最重要）: 現状、ウィンドウを閉じると React の unmount を経ずに webview が落ちるため、デバウンス窓（500ms）内の最後の編集が保存されない。Tauri の `onCloseRequested` で `flush()` を await してから閉じる配線が必要。あわせて、close/ファイル切替時の flush が**再失敗**した場合に dispose が復元済み pending を破棄する二重失敗エッジも同時に塞ぐ（`src/App.tsx` closeCurrentFile / `src/core/autosave.ts`）
- **自動保存失敗の可視化**: write 失敗は現状 console.error のみ（pending 復元により次の flush/編集で再試行はされる）。M2 の検証エラー表示と同じ枠で UI 通知を設計する
- **listOnly / rejected の理由文言の整備**: `type` が非文字列のとき「type がありません」と表示される・`schemaVersion` 欠落と新版が同文言、など分類は正しいが文言が不正確な箇所がある（`src/core/load.ts`）。M2 の「未知 type / 未知 schemaVersion の一覧表示」本格対応時に直す
- **openFolder の失敗時 UI**: フォルダ読込失敗時に選択状態だけ先にクリアされ、旧フォルダの一覧が残る不整合。openFolder 連打の相互ガードも未実装（selectFile 側のトークンガードは実装済み）
- **レジストリの idPrefixes 自己重複検査**: `['term','term']` のような同一モジュール内重複が検査をすり抜ける（`src/core/registry.ts`）。2本目のモジュールを手書きする時点で実害化するため、モジュール追加前に塞ぐ

### M3 で扱うもの（M1 の暫定仕様の作り直し）

- **別名セル**: 読点・半角カンマ区切りの1入力欄は M1 暫定。全角カンマ・改行貼り付けは分割されない。M3 のキーボード操作設計と一体で作り直す
- **kind セルの表示**: enum 生値（actor 等）をそのまま表示中。日本語ラベルの対応表を UI 層に持たせる（データ・スキーマは英語 enum のまま）
- **非制御入力からの移行**: M1 は IME 対応回避のため defaultValue 方式。M3 の composition 対応時に制御方式を再設計する

### 実装で確定した事項（計画の前提に昇格）

- **自動保存の重複判定は「書き込み実行時点」で行う**: スケジュール時点の比較（M1 計画の原案）は、in-flight write との競合で編集消失を起こすことが実装時に判明した。確定した設計は「update 時の早期 no-op は write が飛んでいない時だけ／chain 実行時に lastSaved（確定したディスク状態）とだけ比較／失敗時は pending に復元して flush で再試行」（`src/core/autosave.ts`）。M5 の外部変更検知（自己書き込み除外）はこの構造を前提に設計すること
- **ファイル選択時は必ずディスクから読み直す**: 走査時キャッシュを編集起点にすると直前の自動保存分を古い内容で上書きする。M5 の監視実装でもこの原則を維持する
- **tsconfig は3分割**（app / test / node）: テストは `node` 型が必要なため app から除外し、`tsconfig.test.json` で型チェックする。以後のテスト追加はこの構成に従う

---

## 8. M2 完了に伴う申し送り（2026-08-02 追記）

M2 は実装・レビュー完了（PR #3）。整合性検証（レベル2）・warning のセル単位可視化・プロジェクト全体の検証スコープ・M1 申し送りの解消まで入った。手動 E2E で出た指摘（前後空白での重複すり抜け / warning の視認性 / fixture の正規形ずれ）も修正済み。以下は後続マイルストーンの計画入力。

### M3 で扱うもの

7節の M3 項目（別名セル / kind の日本語ラベル / 非制御入力からの移行）は引き続き有効。M2 で新たに積んだものを以下に追加する。

- **別名セルは実使用でも再指摘された**（7節の再掲を兼ねる）: データ自体は `aliases: string[]` の配列なので、直すのは入力 UI だけ。読点・半角カンマ区切りの1入力欄は暫定で、全角カンマ・改行貼り付けは分割されない
- **行キーが配列 index になっている**: ID 重複ファイルを「受け入れて赤表示」する以上 `term.id` はキーに使えない（重複キーで描画が壊れる）。M3 の並び替え（Alt+↑↓）では index キーだと行の同一性が保てないため、この時点で再設計が要る（`src/modules/glossary/GlossaryEditor.tsx`）
- **セル赤表示のキーが entityId のみ**: ID 重複時、同じ ID を持つ全行に同じセルマークが波及する（現状は行全体が赤なので実害は小さい）。上の行キー再設計と併せて直す
- **検索の照合も `normalizeForMatch` を使うこと**: 重複判定と検索で規則を分けない（`src/core/normalize.ts`）

### M4 で扱うもの

- **赤バッジは M2 で実装済み**。4節の M4 に残るのは新規作成（type選択）/ 削除（OSゴミ箱）/ 用語集0個からの自動生成
- **単一性違反の解消手段が無い**: M2 で「検出して赤表示」までは入ったが、アプリ内で直す手段が無い。M4 の削除機能が入って初めて「拒否は修正に外部エディタを強いるので最小限に」の原則（rev 5章）が閉じる

### M5 で扱うもの

- **自己書き込みの除外は autosave の現構造を前提に設計する**: `flush()` は「書き残しの有無」を boolean で返し、ウィンドウ close のゲートに使っている。write 失敗時は pending に復元するが、**復元するのは失敗した内容が直近の要求（`latest`）と一致するときだけ**——後続の write が最新を書き終えた後に古い内容で上書きする巻き戻りを防ぐため（`src/core/autosave.ts`）
- **外部変更の取り込み経路でも `computeIssues` を通す**: 検証は現在「フォルダ走査時」「編集時」の2経路で全ファイル再計算している。外部変更が3本目の経路になる（`src/App.tsx`）
- **`saveError` のクリア条件**: 現状は `onSuccess`（実際に書き込みが起きて成功）でしかクリアされないため、書き込みが起きないままファイルを切り替えると前のファイルのバナーが残りうる。M5 のトースト通知の設計と併せて整理する

### いつでもよいが、忘れると実害化する残件

- **`checkProjectConsistency` の `out.set(f.path, [issue])` が上書き代入**: コア横断ルールが2本目になった瞬間、先のルールの issue を消す。**2本目を足す時点で必ず配列への追記に変える**（`src/core/project-consistency.ts`）
- **unmount 時の effect が `void flush()` の直後に `dispose()` を呼ぶ**: flush 失敗で復元された pending を捨てる経路。実際の close は `interceptClose` 側を通り、そちらは flush 成功時のみ閉じるので現状は実害なし（`src/App.tsx`）
- **保存できないと閉じられない**: flush 失敗時に close をブロックするため、ファイルが恒久的に書けない状態（権限・ロック）だと終了手段が無くなる。「破棄して閉じる」の脱出口が要る
- **編集1打鍵ごとに全ファイルの `checkConsistency` を再実行している**: 数百レコード規模では問題ないが、規模が増えたら選択中ファイルだけの差分計算に変える

### 実装で確定した事項（計画の前提に昇格）

- **モジュール規約の実装スロットが6点中5点埋まった**: `type` / `displayName` / `schema` / `idPrefixes` / `Editor` / `checkConsistency`（規約4）/ `singleton` / `migrate`（規約6・恒等）。残るは出力ロジック（規約5・M6）。**2本目のモジュールはこの形をそのまま実装する**（`src/core/registry.ts`）
- **整合性検証は2層に分かれる**: モジュール内検証＝各モジュールの `checkConsistency`（自ファイルで完結）、コア横断検証＝`src/core/project-consistency.ts`（単一ファイルでは判定できないもの）。参照切れは「参照する側」のモジュールの提供物なので、2本目のツールが来た時点でそちら側に実装する
- **`LoadResult` の3 variant すべてが `type` を持つ**: 単一性は「`type: glossary` のファイルが2つ以上」という物理条件なので、スキーマ違反（rejected）や未知版（listOnly）のファイルも数える必要がある（`src/core/load.ts`）
- **照合の正規化は NFKC ＋ 前後空白の除去 ＋ 英字大文字小文字同一視**: 前後空白の除去は表記ゆれの吸収ではなく入力ノイズの除去（無いと末尾にスペースを足すだけで重複判定をすり抜けられる）。NFKC が全角スペースを半角化した**後**に trim する順序が必要。カナ同一視・送り仮名吸収は引き続きしない
- **エラーと warning の視覚表現は「同系色の面の濃さ」で区別する**: 点線下線は視認性が低く行の罫線と紛れたため廃止した。**波線は表記ゆれの「指摘（suggestion）」用に予約**されているので warning には使わない（session-notes 論点5）。空の定義セルは「未定義」のプレースホルダで明示する（M6 の Markdown 出力の表記と揃えた）。濃さの値は仮置きで、確定は M7
- **capabilities は JS API を追加で使うたびに権限追加が要る**: close 横取りの `destroy()` には `core:window:allow-destroy` が必要だった（無いと ACL 拒否で**ウィンドウが閉じなくなる**）。「Rust を書かない」方針とは別問題なので、新しい Tauri JS API を使うときは `src-tauri/capabilities/default.json` を確認すること。なお2節の「フォルダ走査には scope の追加が要る」は M1 で `recursive: true` により解決済み

### rev への反映事項

- **5章**: 整合性検証の正規化規則に**前後空白の除去**を追加する（現状の記載は「NFKC 正規化＋英字大文字小文字同一視で行い」）
- **9章**: 視覚表現の強度を3段階として明記する — レベル2エラー＝濃い面 / warning＝淡い面 / suggestion（将来）＝波線

### 運用上の落とし穴（M2 の E2E で踏んだ）

- **worktree で動作確認するときは dev サーバーの出所を確認する**: `vite.config.ts` は `strictPort: true` でポート5173固定のため、別チェックアウト（main など）の dev サーバーが先に5173を掴んでいると、worktree で `npm run tauri dev` しても**古いコードのアプリが表示される**。「実装したはずの機能が動かない」という症状になるが原因は起動元。`Get-NetTCPConnection -LocalPort 5173` で掴んでいるプロセスのパスを確認できる

---

## 9. M3 完了に伴う申し送り（2026-08-05 追記）

M3 は実装・レビュー・E2E 完了。キーボード操作の共通化・IME 対応・境界規則・Undo/Redo・検索/フィルタが入り、7節・8節に挙げた M3 向けの申し送り（別名セル / kind の日本語ラベル / 非制御入力からの移行 / 行キーが配列 index / セル赤表示のキーが entityId のみ / 検索の照合規則）はすべて解消した。実機の日本語入力での確認も完了している。

### M4 で扱うもの

- **モーダルの配線点は用意済み**: `KeyContext.modalOpen` を true にすると操作言語が止まる（Esc の取り合いを構造的に排除する）。削除確認ダイアログを出すときは、額縁の `GLOBAL_KEY_CONTEXT`（`src/App.tsx`）とエディタの `onCellKeyDown`（`src/modules/glossary/GlossaryEditor.tsx`）の2箇所に渡す
- **用語が1件だけの状態でその行を消すとフォーカスが `document.body` に落ちる**: 削除後に移る先の行が無いため。M4 の空状態（新規作成ボタン・用語集の自動生成）と併せてフォーカスの行き先を決める。**行が残っている場合の削除は M3 で解決済み**（削除後の配列から鍵を引き、先頭行を消したときは新しい先頭行へ移る）
- **単一性違反の解消手段が無い**（8節からの積み残し）: M4 の削除機能が入って初めて閉じる

### M5 で扱うもの

- **外部変更の取り込みが無いことは E2E で実害として出た**: `git restore` でファイルを元に戻しても、アプリは開いたままの内容を表示し続ける。人間から見ると「戻したはずの内容が戻っていない」状態で、そのまま編集すると古い内容が書き戻る。**取り込み時に Undo 履歴を破棄する**（4節 M5 の既定）方針はこの経路でも正しい——履歴の中身は取り込み前のファイルを指しており、取り込み後にそこへ戻すとディスクの内容を無言で巻き戻すため
- **取り込みは `applyEdit` の4本目の経路になる**: 現在この関数（`src/App.tsx`）を通るのは編集・Undo・Redo の3経路で、いずれも「自動保存へ渡す」「整合性検証をやり直す」を必ず通る。外部変更もここに合流させること。履歴の作り直しは `selectFile` 内の `createHistory(result.data)` と同じ形になる
- **別名パネルを開いたまま外部変更が来ても表示は追随するはず**: `AliasCell` の下書き再同期は `aliases` の**内容**比較で行う（参照比較では Undo→Redo と区別できずデータ喪失を起こしたため。下記「実装で確定した事項」参照）。外部変更でも内容が変われば下書きを作り直す
- **`saveError` のクリア条件**（8節からの継続）: トースト通知の設計と併せて整理する

### M6 で扱うもの

- 出力ロジック（規約5）がモジュール規約6点セットの最後の空きスロット
- **種別の日本語ラベルは `src/modules/glossary/kind-labels.ts` にある**。Markdown 出力の見出しでも使い回すこと。`undecided` は既に「未分類」で、4節 M6 の「`### 未分類` として出力にも明示する」と表記を揃えてある。enum を拡張したらラベルの足し忘れをテストが検出する

### M7 で扱うもの

- 別名パネルの面・罫線・チップ、検索欄、種別フィルタチップの選択状態（`bg-ink text-canvas`）は既存の役割トークンの流用で仮置き
- **修飾キーの表示名は `altModifierLabel` / `primaryModifierLabel`（`src/core/keyboard/platform.ts`）を通す**。キーヒントを増やすときもベタ書きしない（macOS では Option / Cmd になる）

### いつでもよいが、忘れると実害化する残件

- **定義セル・種別セルは `mark(index, field)` を参照していない**（自セルの warning 条件だけを見る）。現状これらのフィールドを指す検証ルールが無いので実害は無いが、増えた時点で issue 一覧には出るのにセルが赤くならない
- **`@testing-library/user-event` を devDependencies に入れたが未使用**。キャレット・選択範囲の忠実度が要るテストを書くときに使うか、外すか
- **`resolveCommand` の細かい非対称**: macOS の `Ctrl+Backspace`（主修飾キーは Cmd なので素の Backspace として通る）と `Alt+Shift+↑↓`（`altKey` を先に見るため並び替えになる）。どちらも実害は小さいが、`Enter` / `Backspace` の分岐が余分な修飾キーを弾いているのと不揃い
- **`CellInput` の `caretAtStart` / `caretAtEnd` は完全に collapsed なキャレットのみ true**。選択範囲があると行間移動に1打鍵余分に要る（Excel 等と同じ挙動でもある）
- **編集1打鍵ごとに全ファイルの `checkConsistency` を再実行している**（8節からの継続）

### 実装で確定した事項（計画の前提に昇格）

- **キー解釈は `src/core/keyboard/keymap.ts` の `resolveCommand` ただ1箇所**。各コンポーネントは `KeyContext`（編集中か・空欄か・削除を認める欄か・キャレットが端か・欄自身が↑↓を使うか・並び替えが有効か・モーダル中か）を渡してコマンドを受け取り、自分の構造に写像するだけ。`e.key` を直接見るコードはコア以外に存在しない。**入れ子のリスト（別名パネル）が同じ関数で動くことを実証した**ので、2本目のツールもこの形に乗せる
- **行の同一性キーは `${id}#${出現順}`**（`src/core/row-keys.ts`）。データから毎回導出し、内部状態を持たない。**構造操作の後のフォーカス先は必ず「操作後の配列」から鍵を引くこと**——操作前の鍵は ID が重複していると別の行を指す（実装時に踏んだ）
- **新規行の名称は `新しい用語` を入れ、全選択状態でフォーカスする**。空文字はスキーマ違反（`minLength: 1`）で、自動保存が走った瞬間に次に開けないファイルを自分で作る。全選択は E2E の指摘（既定値を消す手間）への対応で、打ち始めればそのまま置き換わる
- **Redo は Ctrl+Shift+Z と Ctrl+Y（Windows/Linux）**。macOS は Cmd+Shift+Z のみ（Cmd+Y を Redo とする慣習が無いため）
- **テキスト編集中の Ctrl+Z もアプリの履歴が取る**。制御入力ではブラウザ標準の Undo が React の再レンダリングと食い違うため、挙動を一本化する。rev 10章の境界規則（編集中は操作言語を停止）への明示的な例外
- **編集・Undo・Redo は同じ後処理（`applyEdit`）を通す**。自動保存への反映と整合性検証のやり直しを取りこぼさないための単一経路
- **導出表示中（検索・フィルタ適用中）は並び替えだけでなく行追加も止める**。挿入するとその行が絞り込みに掛からず、見えないまま増えるため。理由は並び替えを止めるのと同じ「データ順と表示順の食い違い」
- **ローカル下書きと親の値の同期は参照でなく内容で比較する**（`AliasCell`）。`src/core/history.ts` の `redo` は Undo 前のオブジェクトを**同一参照**で返すため、参照比較だと「自分が上げた変更」と区別できず、パネルを開いたまま Undo→Redo すると表示だけ古いまま残り、次の打鍵で Redo した内容が消える（最終レビューで発見。回帰テストあり）
- **DOM テストは対象ファイル先頭の `// @vitest-environment jsdom` で切り替える**。グローバルの `test.environment` は `node` のまま（純ロジックのテストを重くしない）。Vitest の `globals` が無効なので `afterEach(cleanup)` を明示する。**要素は role とアクセシブル名で引き、レイアウトやクラス名に依存させない**（UI の微調整でテストを書き直さないため）。DOM テストは「壊れても画面は一見正常」な回帰——IME 誤爆・制御入力の巻き戻り・フォーカス移動・Undo の表示反映——に絞る

### rev への反映事項

- **10章**: グローバル層の Redo に **Ctrl+Y（Windows/Linux）** を追記する
- **10章**: 境界規則の明示的な例外として「**テキスト編集中も Undo/Redo はアプリが取る**（制御入力のため）」を追記する
- **10章**: 導出表示中に止めるのは並び替えだけでなく「**表示順とデータ順の対応が崩れる操作**（追加を含む）」と一般化する

### 実装計画そのものに含まれていた誤り（次回の計画立案への教訓）

計画（`docs/superpowers/plans/2026-08-02-m3-editor-operability.md`）に完全なコードを書いても、次の4件は実装・レビューの段で初めて出た。**計画のコードは検証済みの正ではなく、レビューを通す前提の下書きとして扱うこと。**

1. 履歴上限テストの期待値が実装の意味と1つずれていた（110件積んで末尾100件なら先頭は 10）
2. 行入れ替え後のフォーカス先を「移動前の配列」の鍵で指していた（ID 重複行が隣接すると別の行を指す）
3. window リスナーの依存配列が lint 警告を出した（挙動は正しいが、リポジトリの警告ゼロ基準を壊した）
4. **下書き再同期の参照比較（上記の Critical）**。単体では正しく見え、Undo と Redo を跨いで初めて壊れる。継ぎ目（額縁の履歴とエディタのローカル状態）にテストが無かったことが見逃しの原因で、実際の履歴を通す回帰テストを足して塞いだ

---

## 10. M4 完了に伴う申し送り（2026-08-06 追記）

M4 は実装・レビュー完了。新規作成（type選択）／削除（OSゴミ箱へ移動）／用語集0個からの自動生成／確認ダイアログ／「保存できないまま閉じられない」状態の脱出口が入り、8節・9節の M4 項目（単一性違反の解消手段が無い／モーダルの配線点／用語1件の行を消すとフォーカスが `document.body` に落ちる）と、8節の残件「保存できないと閉じられない」は解消した。

**ただし本節は 7〜9節と違い、実機確認（`npm run tauri dev` の GUI ウォークスルー）を実施する前に書かれている。** 裏取りできているのは自動テスト（29ファイル／215テスト）とコードレビューまでで、「実際に動かして確かめた」保証は付いていない。特に `src/App.tsx` の配線（新規作成・削除・用語集の自動生成・閉じるときの脱出口）にはリポジトリに自動テストが1件も無く、完了条件1〜9の検証は人間の実機確認が唯一の経路である。**M5 の計画立案時には、まず実機確認が済んだかを確認すること**——未実施なら、本節の「解消した」はコード上の解消であって観測された解消ではない。

### 実装で確定した事項（計画の前提に昇格）

- **自前の Tauri コマンドは `move_to_trash` の1本だけ**: 2節の「Tauri コマンドは追加しない」は「fs / dialog で足りる」を前提にした記述で、OS ゴミ箱だけはその前提が成り立たない（fs プラグインの `remove` は完全削除）。Rust 側は `trash::delete` を呼ぶだけで判断を持たない（rev 7章の原則は維持）。**自前コマンドは ACL 対象外なので capabilities への追記は要らない**——M2 で確定した「新しい Tauri JS API を使うたびに権限追加が要る」はプラグイン／コアコマンドの話。クレートは `trash` v5（`src-tauri/Cargo.toml`）
- **削除の経路は flush してはいけない**: 開いているファイルをゴミ箱へ移すとき `closeCurrentFile()`（flush する経路）を通すと、消したファイルを自動保存が書き戻して復活させる。`src/core/file-ops.ts` の `trashFile` が dispose → trash の順を守り、`flush()` を呼ばないことをテストで固定してある。順序も逆にできない（先にゴミ箱へ移すと直後のデバウンス発火で同じことが起きる）。**ただしこの不変条件はまだ完全ではない**——下記「残件」の in-flight write を参照。M5 の外部変更検知でも「外部で消えたファイル」に対して同じ注意が要る
- **モジュール規約に `createEmpty(title)` が加わった**: rev 6章の6点セットには無い7つ目のスロット。額縁の新規作成が `type` からモジュールを引く以上、雛形を置ける場所はモジュール側しかない。**2本目のモジュールもこれを実装する**（`src/core/registry.ts`）。あわせて `ModuleRegistry.list()` を足した（新規作成ボタンを登録済みモジュール分だけ並べるため。ボタンをハードコードしない）。規約6点の残る空きスロットは出力ロジック（規約5・M6）のみ
- **新規ファイルも正規形で書く**: 非正規形で作ると、作った直後の最初の1文字の編集で全行 diff が出る。`buildNewFile`（`src/core/new-file.ts`）はモジュールの `schema` を渡して既存のシリアライザを通す。テストが「スキーマ適合」と「再シリアライズでバイト一致」の両方を固定している（`src/core/new-file.test.ts`）
- **ファイル名は `<displayName>.json`、衝突時は `-2` から連番**（`src/core/file-naming.ts` の `resolveNewFileName`）。ファイル名は識別子ではない（rev 5章）ので意味は持たせず、衝突回避だけを目的とする。Windows は大文字小文字を区別しないので比較も区別しない。`title` は拡張子を除いたファイル名を初期値にする（単一性違反時にどちらの話か見分けられるようにするため）
- **作成したファイルの登録経路は `addCreatedFile` の1本**（`src/App.tsx`）: 新規作成と用語集の自動生成が「`classifyFile` で分類 → 一覧に足す → `computeIssues` → 開く」を同じ関数で通る。書いたテキストをそのまま分類し直すのは、editable にならないなら雛形かシリアライザが壊れている証拠だから（一覧に出す前に気付ける）。**M5 の外部変更の取り込みもここへ合流させられる**
- **`ProjectFile` / `computeIssues` は `src/core/project-file.ts` に移した**（App.tsx の肥大化対策。`computeIssues` は `appRegistry` をクロージャで掴まず引数で受け取る）。`computeIssues` の呼び出し経路は「フォルダ走査時」「ファイル選択時の読み直し」「編集時」の3本に「ファイル作成時」「ファイル削除時」が加わって5本になった（8節が「2経路」と書いているのは選択時を数え落としている）。**M5 の外部変更の取り込みが6本目**
- **モーダル中の操作言語停止の配線点は3箇所**（9節は2箇所と書いていたが不足だった）: `src/App.tsx` の `globalKeyContext(modalOpen)`、`GlossaryEditor` の `onCellKeyDown`、そして `AliasCell`（別名パネルが独自に `resolveCommand` を呼ぶため、ここを忘れるとダイアログ表示中に別名パネルの Enter/Backspace/Esc が反応する）。`EditorProps` に `modalOpen: boolean` が入ったので、**2本目のモジュールのエディタもこれを受け取って自分の `resolveCommand` 呼び出しすべてに配る**
- **Radix の `AlertDialogAction` と `AlertDialogCancel` は内部的にどちらも `Dialog.Close`**: `composeEventHandlers(props.onClick, () => onOpenChange(false))` の形なので、確認ボタンを押しても `onClick` の後に `onOpenChange(false)` が走り、`onCancel` まで発火する。`ConfirmDialog` は確認側の `onClick` で `event.preventDefault()` してから `onConfirm()` を呼んで止めている（`composeEventHandlers` の `checkForDefaultPrevented` を使う）。キャンセル側は `onClick` を持たず `onOpenChange` に一本化。**2本目のモーダルを作るときも同じ罠を踏む**
- **確認ダイアログはアプリに1つだけ置き、`confirm` state（`{ title, description, confirmLabel, onConfirm } | null`）で内容を差し替える**。ファイル削除の確認と「破棄して閉じる」の脱出口が同じ機構を共有している。`modalOpen = confirm !== null` がそのまま操作言語の停止条件になる
- **ファイル削除には確認を挟み、用語（行）の削除には挟まない**: 行の削除は Undo で戻せるが、ゴミ箱への移動はアプリの履歴では戻せない（戻す手段は OS のゴミ箱）。確認の有無はこの基準で決める
- **削除ボタンは `rejected` / `listOnly` の行にも出す**: 単一性違反の解消には「壊れている方の用語集を消す」が要る。ここを塞ぐと外部エディタを強いることになり、rev 5章「拒否は最小限に」に反する
- **保存できないまま閉じられない状態の脱出口は `destroy()` で抜ける**（`src/fs/app-window.ts` の `forceClose`）: `close()` だと `onCloseRequested` が再発火して interceptor がループする。M2 で追加済みの `core:window:allow-destroy` がそのまま効く

### M5 で扱うもの

8節・9節の M5 項目（自己書き込み除外は autosave の現構造を前提に設計する／取り込みは `applyEdit` と `computeIssues` の経路に合流させる／`saveError` のクリア条件／別名パネルの下書き再同期）は引き続き有効。M4 で新たに積んだものを以下に足す。

- **削除・新規作成も監視の自己書き込み除外の対象になる**: フォルダ監視を入れると、アプリ自身のファイル作成・ゴミ箱移動が外部変更として跳ね返る。除外は「書き込み」だけでなく「作成」「消滅」も対象にすること
- **「外部で消えたファイル」の後始末は `deleteFile` と同じ形になる**（`src/App.tsx`）: 選択中なら `selectSeq.current++` で in-flight の `selectFile`/`openFolder` を捨てさせ、saver を **flush せずに** dispose し、`selectedPath` / `history` / `saveError` を落とし、`computeIssues` をやり直す。外部消滅で flush すると消えたファイルを書き戻す——M4 の削除と同じ事故
- **`ioError` / `saveError` / トーストの整理**: M4 でファイル作成・削除の失敗も `ioError` バナーに合流させた（`ファイルを作成できませんでした:` / `ファイルを削除できませんでした:` / `用語集を作成できませんでした:`）。現状バナーは2本（`ioError` と `saveError`）が縦に並ぶだけなので、M5 のトースト設計と併せて「どれをバナーに残し、どれをトーストにするか」を決める
- **`confirm` スロットが1つしかない**（下記「残件」と同じ話）: 現状の生産者は「削除確認」と「破棄して閉じる」の2つで、M5 は外部変更の二択ダイアログという3人目を連れてくる。削除確認や close の脱出口と衝突しうるので、この時点で queue 化かスロット分離を判断すること

### M6 で扱うもの

9節の M6 項目（出力ロジックが規約6点セットの最後の空きスロット／種別の日本語ラベルは `src/modules/glossary/kind-labels.ts` を使い回す）は引き続き有効。M4 で新たに積んだものは無い。

### M7 で扱うもの

9節の M7 項目（別名パネル・検索欄・種別フィルタチップの仮置き／修飾キーの表示名は `platform.ts` を通す）は引き続き有効。M4 で新たに積んだものは以下。

- **確認ダイアログの見た目は shadcn の既定トークンのまま**（`src/components/ui/alert-dialog.tsx` は `npx shadcn@latest add alert-dialog` の生成物で手を入れていない）。`ConfirmDialog` を役割トークンへ寄せるのは M7。生成物そのものを書き換えるのか、`ConfirmDialog` 側で上書きするのかもこの時点で決める
- **新規作成ボタン・削除ボタン・空状態の文字色は既存の役割トークンの流用で仮置き**（`text-ink` / `text-ink-muted` / `text-warning` / `border-rule` / `hover:bg-surface`）。色値の直書きはしていないので、M7 はトークンの割当を見直すだけで済む
- **削除ボタンは「削除」の文字**（`src/components/FileList.tsx`）。アイコン化するならアクセシブル名（`aria-label="<名前> を削除"`）を維持したまま行うこと——DOM テストがこの名前で引いている

### いつでもよいが、忘れると実害化する残件

M4 で新たに見つかったもの。上2件は「削除したファイルが復活しない」という M4 の当の不変条件に触るため、レビューの重要度は Minor だが**扱いは他より上に置く**。

- **`trashFile` の `dispose()` は in-flight の write を待たない**（`src/core/file-ops.ts` / `src/core/autosave.ts`）: `dispose()` はタイマーと `pending` を消すだけなので、デバウンスが発火して write が飛んだ**直後**に削除を確定すると、trash が先に完了し、後から着地した write がファイルを復活させうる。M4 が防ごうとしている当の事象。修正案は `trashFile` を `dispose(); await flush()` にすること（dispose 後は `pending` が null なので flush は何も書かず、進行中の chain の完了を待つだけになる）。窓は狭いが、起きたときの症状は「消したはずのファイルが戻ってくる」で、原因の特定が難しい
- **`deleteFile` の `wasSelected` が古いレンダのクロージャ由来**（`src/App.tsx`）: 確認ダイアログを表示している間に in-flight の `selectFile` が解決すると `wasSelected` が false になり、`selectSeq` の更新と teardown がどちらも飛ぶ。結果 `selectedPath` / `saverRef` がゴミ箱行きのファイルを指したまま残る。実害は現状限定的（editor は unmount され、孤児 saver は pending が null なので書かない）だが、上の in-flight write の件と組み合わさると復活経路になりうる。修正案は `selectedPath` を ref に写して `deleteFile` 内で `selectedPathRef.current` を見ること
- **`src/App.tsx` に配線レベルのテストが1件も無い**（リポジトリに App のテスト自体が無い）: 「削除で flush しない」は `trashFile` の単体テストだけが担保しており、App がその関数を正しい引数で呼ぶことは誰も検証していない。新規作成・用語集の自動生成・close の脱出口も同様。App のテストハーネスを1つ作るか、コアへの切り出しを進めるかの判断が要る（M4 では `project-file.ts` と `file-ops.ts` への切り出しでその方向に一歩進めた）
- **`confirm` の状態スロットが1つしかない**（`src/App.tsx`）: 削除確認を出したまま OS の × を押して flush が失敗すると、削除確認が無言で上書きされて要求が落ちる。発生条件が狭く実害も無い（何も削除されない）が、confirm の生産者が増えたら queue 化を検討する。M5 が3人目の生産者を連れてくる
- **`forceClose()` が `close()` でなく `destroy()` を呼ぶことを固定するテストが無い**（`src/fs/app-window.ts`）: `@tauri-apps/api/window` をモックする狭い単体テストで足りる。取り違えると interceptor ループ（閉じられなくなる）が再発する
- **`src/core/file-naming.ts` の `ILLEGAL` は Windows の予約デバイス名（CON/PRN/AUX/NUL/COM1-9/LPT1-9）と末尾のドット・空白を弾かない**: 現状 `module.displayName` しか渡らないため実害は無い。**ユーザー入力（ファイル名の指定・リネーム UI）が直接届くようになった時点で塞ぐこと**
- **`FileList` の行ボタンのアクセシブル名が「`<名前>` を開く」で固定**（`src/components/FileList.tsx`）: スクリーンリーダーには `title`・「開けない」「編集不可」・issue 件数バッジが読まれない。`aria-describedby` で補うのが筋
- **`ConfirmDialog` の可読性・テストの小さな負債**: `ConfirmDialog.dom.test.tsx` の見出しを `getByText` で引いている（`AlertDialogTitle` は h2 なので `getByRole('heading')` が使える）。また `AlertDialogCancel` 側に「なぜハンドラが要らないか」の一行が無く、`preventDefault` を持つ Action 側との非対称が読み取りにくい

8節・9節からの継続（M4 では手を付けていない）:

- **unmount 時の effect が `void flush()` の直後に `dispose()` を呼ぶ**（`src/App.tsx`）: flush 失敗で復元された pending を捨てる経路。実際の close は `interceptClose` 側を通るので現状は実害なし
- **編集1打鍵ごとに全ファイルの `checkConsistency` を再実行している**（M4 でファイル作成・削除の経路が増えたが、いずれも頻度が低いので事情は変わらない）
- **定義セル・種別セルは `mark(index, field)` を参照していない**（`src/modules/glossary/GlossaryEditor.tsx`）。これらを指す検証ルールが増えた時点で、issue 一覧には出るのにセルが赤くならない
- **`@testing-library/user-event` を devDependencies に入れたが未使用**
- **`resolveCommand` の細かい非対称**（macOS の `Ctrl+Backspace` / `Alt+Shift+↑↓`）
- **`CellInput` の `caretAtStart` / `caretAtEnd` は collapsed なキャレットのみ true**

※ 8節の「`checkProjectConsistency` の `out.set` が上書き代入」は M3（`addIssue` ヘルパの追加）で、8節の「保存できないと閉じられない」は M4 Task 8 で解消済み。

### rev への反映事項

**本節の分は反映済み**（このコミットで `docs/overview-rev.md` を編集した）:

- **7章**: 「Rustは原則書かない」の例外として `move_to_trash` を明記
- **6章**: モジュール規約6点の直後に `createEmpty(title)` を追記

あわせて、**9節の「rev への反映事項」3件（10章の Ctrl+Y／編集中の Undo/Redo／導出表示中に止める操作の一般化）が M3 完了時から未反映のまま残っていた**ので、同じコミットで反映した。教訓として、**rev への反映は当該マイルストーンの完了コミットで済ませ、申し送りに TODO として残さない**こと——次のマイルストーンの計画者は rev を「正」として読むため、反映漏れは設計と実装の食い違いとして伝播する。

### 実装計画そのものに含まれていた誤り（次回の計画立案への教訓）

計画（`docs/superpowers/plans/2026-08-05-m4-file-operations.md`）は着手前に3件修正し（commit `4ec937d`）、さらに実装・レビューの段で3件が出た。9節の教訓（**計画のコードは検証済みの正ではなく、レビューを通す前提の下書きとして扱う**）は M4 でも同じ形で再現している。

着手前に見つかったもの:

1. Task 2 のテストが `@tauri-apps` のモックを1つしか置いていなかった（実際には `api/core` / `api/path` / `plugin-dialog` / `plugin-fs` の4つが要る。テスト対象がインポートするモジュールを数え落とすと import 時に落ちる）
2. Task 5 が `onDelete={() => {}}` の no-op スタブを作り、Task 6 が即座に置き換える構成だった → 削除ボタンとその prop を Task 6 に一本化した。**次のタスクが即消すスタブを置く分割は、分割線そのものが間違っている**
3. 実機確認（`npm run tauri dev`）を実装者が実行する前提で書かれていた。**サブエージェントは GUI を操作できない**ので、実機確認は人間の作業として計画に明記する（M4 では最後にまとめて実施する形にした）

実装・レビューの段で出たもの:

4. **`createNewFile` と `ensureGlossary` に同じ9行を書けと指示していた**（Task 7 のレビューで Important 判定。人間が「今直す」と裁定し `addCreatedFile` に抽出）。**計画に完全なコードを載せると、そのコードの設計上の欠陥まで忠実に実装される**——重複は実装者が「計画通りに書いた」結果として生まれる
5. Task 4 の `ConfirmDialog` のコード（`AlertDialogAction onClick={props.onConfirm}`）では、確認ボタンでも `onCancel` が発火してテストが落ちた（Radix の内部 close 発火。上記「実装で確定した事項」参照）。**ライブラリの内部挙動は計画時点では未検証**であり、計画のコードを verbatim で書けという指示と衝突する。実装者はライブラリのソースを読んで機構を確認し、レビュアーも同じ経路を辿って追認した
6. Task 5 の期待テスト件数が「5件」と書かれていたが、計画が示したテストファイル自体には `it` が4件しか無かった（数え間違い。実装者は件数に合わせてテストを増やさず、計画の矛盾として報告した——これが正しい対応）

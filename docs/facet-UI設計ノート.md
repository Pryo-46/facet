# facet UI 設計ノート — 視覚言語の統一

---

## 0. この文書の位置づけ

現行UIに対する「安っぽく感じる」という主観的な違和感の原因を構造的に分解し、
修正方針を決定事項として固定するための文書。

**重要な前提**: 本件は美観の問題ではなく、**facetの中核機能の劣化**として扱う。

facetの核心価値は「未定義を物理的に見えるようにする」ことにある。
未定義の視覚表現がモジュール間で分裂している状態は、この価値そのものが
実装されきっていないことを意味する。以降の指摘は、すべてこの観点から導かれる。

---

## 1. 根本原因の診断

### 1.1 差をつける場所と、そろえる場所が逆転している

UIの品質感は、印刷物の版面設計と同じ仕組みで生まれる。すなわち
**「限られた数の差（コントラスト）を意図的に配置し、それ以外は徹底的に揃える」**。

安っぽく見える画面はたいてい逆であり、
「細部がバラバラなのに、情報階層は平坦」という状態になっている。
現行facetはまさにこれに該当する。

**そろっていない（本来そろえるべき）:**

| 対象 | 現状 |
|---|---|
| 角丸半径 | ボタン・入力欄・参加者ボックス・ツリーノードで全て異なる |
| ボーダー | パネル境界も入力欄も同じ1px同色。境界線に階層がない |
| セレクト要素 | 用語集「種別」列がOSネイティブのまま（shadcn/ui未適用） |

なお、ボタン形状の不統一（黒塗り／白＋枠／枠のみ／枠なしアイコンの4種混在）は
本項の一例ではなく独立した設計課題であるため、**D19** に分離した。

**平坦（本来差をつけるべき）:**

- タイポグラフィがほぼ1ウェイト。サイズ差のみで階層を作ろうとしている
- 太さ・色の濃度・字間が階層表現に使われていない

### 1.2 強調の重みと使用頻度が一致していない

`フォルダを開く` が黒塗り（=最強調）だが、これは起動時に1回押すだけの操作。
一方、常時参照される警告表示や未定義セルの方が弱い。

### 1.3 彩度の予算が分散している

これが最も深刻。

facetは「未定義は赤で見える化」を設計原則としている。
であれば**彩度の予算は赤が独占すべき**である。

しかし現状は以下が同時に主張している:

- クリーム地色
- テーブルヘッダーの緑帯（意味を担っていない純粋な装飾）
- 未解決チップのピンク
- 黒塗りボタン
- 方眼グリッド線
- ロジックツリーのノード文字の青

結果として、右パネルに「未定義」が何個並んでも目に飛び込んでこない。
**赤の情報量が装飾によって希釈されている。**

### 1.4 密度の稼ぎ方が定石と逆になっている ← rev.2 で追加

現行facetは「**フォントサイズが小さく、行間が広い**」という構成になっている。

日本語の業務システムにおける定石はこの逆である（§3 参照）。
密度は**行間を詰めることで稼ぎ、フォントサイズは削らない**。

情報密度の予算を、行間という「削っても情報が失われない部分」ではなく、
フォントサイズという「削ると可読性が直接落ちる部分」から取ってしまっている。
D3（レイアウトが内容に支配される）と同じ根から出た問題。

---

## 2. 決定事項

### D1. 未定義の視覚表現を全モジュールで単一化する

**現状の分裂（4通り）:**

| 場所 | 現在の表現 |
|---|---|
| シーケンス右パネル | 赤枠＋赤背景のセル |
| 用語集・定義列 | 「未定義」という**黒い文字列** |
| 用語集・別名列 | 「別名なし」という**別の文字列** |
| 用語集・備考列 | **完全な空欄**（警告なし） |
| ロジックツリー | **無色の空ノード**（警告ゼロ） |

用語集では「空＝赤」なのに、ツリーでは「空＝無害」に見える。
**単一権威ソースの発想がデータ層には適用されているのに、UI層には適用されていない。**

特にロジックツリーは空ノードが多数あるにもかかわらず視覚的警告が一切なく、
facetの中核機能が機能していない状態。

**さらに危険な点:**
「未定義」「別名なし」という文字列をセルに埋めるのは、
**データに存在しない値をUIが捏造している**構図である。
ユーザーが実際に「未定義」と入力した場合と区別がつかない。

→ **空セルは空のまま描画し、色と枠だけで警告する。**

### D2. 1つの色に1つの意味だけを割り当てる

現状、1つの見た目が複数の意味を持っている。

| 見た目 | 現在担っている意味 |
|---|---|
| 黒塗り | 主要アクション（フォルダを開く）／ 選択中トグル（サポート向け） |
| 緑ベタ | 意味なし（全テーブルヘッダーの装飾） |
| 青文字 | ロジックツリーの「テキストが入っているノード」のみ |
| ピンク | 警告（ただし薄ピンク行／濃ピンクセルの2段階が凡例なしで併存） |

**問題点:**

- 黒塗り: 「押すもの」と「状態を表すもの」が同じ見た目になっている
- 青文字: リンクでもないのに青は「クリックできる」と誤解を招く。かつ他画面に存在しない色
- 緑ベタ: 分類ラベルに彩度背景を敷くのは階層表現として最も安価な手段であり、
  90年代デスクトップアプリの記号として読まれる

**色が意味を持つUIでは、ユーザーが一度でも「色は当てにならない」と学習した瞬間、
警告機能は死ぬ。**

#### rev.3 での訂正

> rev.1〜2 の記述「無彩色ベースに振り切り、**彩度を持つのは赤のみ**とする」は誤り。
> 課題ツリーの判断軸（支持／棄却）に色が必要であることが判明したため、破棄する。

**D2の本質は「赤だけを使う」ではなく、以下である:**

> **色を持つのは「意味」だけ。「装飾」には一切使わない。**

この原則のもとで、彩度を持てるのは次の2軸に限られる（D14〜D16参照）。

| 軸 | 例 |
|---|---|
| 欠落軸 | 未定義 / 未決 / 仮説なし |
| 判断軸 | 支持 / 棄却 / 見送り |

→ **緑のテーブルヘッダー帯は廃止する。** この結論は変わらない。むしろ判断軸に緑を
　 割り当てる以上、装飾の緑は**必ず**消さなければならない（同じ緑が意味と装飾を兼ねてしまう）。
→ 緑帯の代替は、小さめ・グレー・字間広めのテキストラベル（D11参照）。
→ ボタンは彩度を持たない（D19参照）。
→ 分類は彩度を持たない（D16参照）。

### D3. レイアウトが内容に支配される構造をやめる

**現状:**

- 用語集: 備考列のテキスト量が行高を決めており、「役割トークン」行だけ突出して高い
- ロジックツリー: 長文ノードだけ幅が3倍になり、ツリーの骨格が読めない

高品質なUIは逆で、**レイアウトが先に決まっていて、内容がそこに収まる**。

→ テーブルは行高固定＋2行で省略
→ ツリーはノード幅固定＋省略記号、詳細は別パネルで表示

**根拠**: 会議中に構造を俯瞰するツールである以上、
全文表示よりも骨格の可読性が優先される。

#### rev.3 での再定義: ノード幅固定は「スキャン性」の要請である

課題ツリーの観察により、ノード幅固定のより重要な根拠が判明した。

現状、ノード幅がテキスト量で決まるため、**右寄せのステータスバッジがバラバラの位置に散る。**
結果として「どれが未決か」を知るには全ノードを個別に読む必要がある。

ノード幅を固定すればバッジが縦一列に揃い、**列を上から下へなぞるだけでスキャンできる。**

→ ノード幅固定は、可読性の問題であると同時に**スキャン性の問題**である。
　 会議中に俯瞰するツールとしては、後者の方が効く。
→ この根拠により、ノード幅固定の優先度を引き上げる。

#### M24 での実施結果（追記）

**ノード幅の固定は実施した。** 課題ツリー・ロジックツリーとも全ノードが 320px 固定になり、右上のバッジは列ごとに縦一列に揃う。**rev.3 が優先度を引き上げた根拠（スキャン性）は、幅だけで満たされている。**

**「＋省略記号」は実装したあと実機で撤回した。** 「閉じた箱に出る文章を3行で打ち切る」を一度は入れたが、**`overflow: hidden` の `textarea` はキャレットに追従してスクロールせず、打ち切った箱は4行目以降を編集できない箱になる**ことが実機で判明した（`docs/history/m24-core-node-width-lock.md` の「実機確認の所見」）。**ノードの高さは内容が決めるままである。**

**再挑戦するなら、先に「溢れた行をどう編集させるか」を決めること。** `text-overflow: ellipsis` も `line-clamp` も `textarea` には効かないので、省略記号を出す道は「非フォーカス時だけ `<span>` に差し替える」（`HypothesisRow` の畳まれた行と同じ作法）しか無い。**有力な逃げ道は、フォーカス中だけ `overflow-y: auto` にし、スクロールバーを `scrollbar-width: none` で消すこと**——`auto` は確実にキャレットへ追従し、バーを消せばレイアウト幅を食わないので測定の前提が崩れない。M24 は `overflow-y: auto` を「スクロールバーが幅を食う」の1点で退けており、**この逃げ道を検討していなかったのが設計の穴だった。**

#### M26 での決着（追記）

**打ち切り——現状を確定。** M24 の撤回を経て UI ノートで唯一残っていたこの1件を、2026-08-26 に人間が「**高さは内容が決めるままを確定**」と裁定した。「ノードの高さに上限は無い——内容が決める」を確定設計として D3 を閉じる。**これで UI ノートの実施項目は全部閉じた**（§8）。

**「採らない」と決めたのは再挑戦（省略記号）そのものではなく、上限を設けないと決めたことである。** 上に書いた逃げ道（フォーカス中だけ `overflow-y: auto` ＋ `scrollbar-width: none`）は**棄却されたわけではない**——それを使ってでも高さに上限を設けるという方向自体を採らないという裁定であって、逃げ道の技術的な当否には触れていない。逃げ道の記録はこの M24 追記節と [`history/m24-core-node-width-lock.md`](history/m24-core-node-width-lock.md) にそのまま残す。

**テーブルの行高固定＋2行省略は「変えない」と決着した。** 理由は2つ——(1) `CellInput` の `MAX_ROWS = 8` に付いた明示的な過去判断「行の高さが揃わなくなるが、読めないよりよい」（M10 決定17）を反転させることになる、(2) テーブルには展開パネルに相当する場所が無く、「省略した全文をどこで読むか」の答えを新設する必要がある。**これは先送りではなく判断である。**

**シーケンスは D3 の対象ではない。** ステップラベルは矢印の上に載る文字で「箱が縦に並んでバッジが散る」構造を持たず、答えセルとガターは既に「幅を導出しない」と決着済み（design-notes 論点7）。

### D4. 警告メッセージは「件数」と「位置」で表現する

**現状の問題:**

```
名称が重複しています:「新しい用語」と「新しい用語」と「新しい用語」と「新しい用語」
```

同じ文字列を4回並べても情報量はゼロ。

```
「新しいエラー」はユーザー対応としていますが、ユーザーの対応が空です
```

3行とも同名のため、どの行を指すのか特定できない。**No列があるのだから `#1` と書ける。**

→ 重複は `「新しい用語」が4件重複しています` と件数で表現
→ 該当行へのジャンプを提供する
→ 行番号が存在するモジュールでは必ず行番号で指し示す

### D5. 警告は「問題のあるセル」だけを染める

現状、名称の重複警告なのに**行全体**がピンクになっている。
行を染めると「この行は全部ダメ」に見え、実際の問題箇所が特定できない。

→ 染めるのは問題のあるセルのみ。

### D6. フォントをOS環境からの導出ではなく、バンドルによる固定にする

**現状の構造的問題:**

Tailwindのデフォルト `font-sans` は `ui-sans-serif, system-ui, ...` であるため、
上書きしていなければ WebView2 (Windows) 上では
**ラテン文字 = Segoe UI、日本語 = Yu Gothic UI** に解決される。

これはfacetの設計哲学と正面から衝突する。

> 実装ステータスはGitブランチ位置から導出、表示順は配列順から導出、
> 座標はレイアウトアルゴリズムから計算

というように単一権威ソースを徹底しているにもかかわらず、
**文字の見た目だけがOSのバージョンとインストール済みフォント次第という
非決定的な導出**になっている。

図面に例えれば、寸法だけミリで指定して、
線の太さは「印刷所にあるペンで適当に」と言っているのと同じ。

→ Tauriの `resources` にフォントを同梱し、ローカル `@font-face` で固定する。

**副次的効果**: 現在依存している Yu Gothic / Meiryo は**再配布が許諾されていない**。
OSに存在するものを参照しているだけなので現時点で違反ではないが、
「配れないフォントに依存している」状態ではある。OFLフォントへの移行は健全化にあたる。

#### M26 での実施

**同梱した。** ただし Tauri の `resources` ではなく、**バンドラ（Vite）が dist へ入れる形**にした——
`@font-face` の `url()` が dist の資産を指せば実行形へ入るので、`resources` に置いて
実行時のパスを解決する必要が無い。

- **fontsource の3パッケージ**を npm 依存として入れた——`@fontsource-variable/ibm-plex-sans`
  （可変 wght）／`@fontsource/ibm-plex-sans-jp`（static 400/500/600）／
  `@fontsource/ibm-plex-mono`（static 400/700）。U1 の決着どおりの構成。
- **素の `@import` はしない。生成 CSS を経由する。** static 版の fontsource の CSS は
  woff2 と woff を**両方**参照しており、素で import すると旧形式の woff まで dist に入る
  （JP だけで数MB）。`scripts/gen-fonts-css.mjs` が fontsource の CSS から woff の参照だけを
  落とし、`url()` をリポジトリ相対に書き換えて `src/styles/fonts.css` を作る。
  **`fonts.css` は生成物だがコミットする**——直に編集せず、fontsource の版を上げたら
  スクリプトを再実行する。形と参照先の実在は `src/styles/fonts.test.ts` が固定する。
- **4KB 未満スライスの data URI 化を止めた**（`vite.config.ts` の
  `assetsInlineLimit: (p) => p.endsWith('.woff2') ? false : undefined`）。JP の woff2 には
  4KB を割るスライスがあり、Vite の既定（4096）だと CSS 内の `data:` URI になる。
  **`tauri.conf.json` の CSP は `font-src 'self'` で `data:` を許していない**ので、
  そのスライスだけが静かにブロックされ、**珍しい漢字が豆腐になる**。
  塞ぎ方は2通りあるが、**CSP を緩める側ではなくインライン化を止める側**を採った。
- **実測サイズ**（`npx vite build` 後の `dist/assets/*.woff2`）: **385本・計 4.41MB**
  （JP 369本＝123分割×3ウェイトで 4.16MB／Sans 可変 6本 0.15MB／Mono 10本 0.10MB）。
  `.woff` は0本。NSIS インストーラは 8.19MB で、同梱前のベースライン 3.67MB に対し
  **+4.52MB**（woff2 の合計とほぼ整合する）。
- **U1 が書いた数値の訂正**（**決定の反転ではなく、記録の精密化である**）: U1 の
  「124分割」は `files/` の**ファイル数**を数えたもので、**CSS から参照されない一体型の
  `japanese-*-normal.woff2` を1本余計に含んでいた**——正しくは **123分割／ウェイト**。
  同じ理由で「JP 約6.8MB」「合計約8.2MB」も過大で、**実際に参照・同梱される woff2 は
  計 4.41MB** である。選定（案A＝IBM Plex 全面移行）そのものは変わらない。
- **OFL の表記**はリポジトリ直下の [`../THIRD-PARTY-NOTICES.md`](../THIRD-PARTY-NOTICES.md)
  の「フォント」の表が持つ（3書体とも OFL-1.1。埋め込みは許諾・単体販売は禁止・
  改変再配布は予約名を使えない）。**ただし配布物（インストーラ）にこの表記は同梱されて
  いない**——リポジトリにしか無い。[`open-issues.md`](open-issues.md) の
  「小さな負債」に項がある。

### D7. 和欧混植のズレを、フォント選定によって解消する

Segoe UI と Yu Gothic UI は**仮想ボディに対する字面（じづら）の比率が異なる**。
Yu Gothicは字面が小さめ、Segoe UIは大きめ。
そのため同じ `font-size: 13px` を指定しても、
並べると日本語だけ一段小さく、ラテン文字だけ浮いて見える。

**該当箇所（facet内は混植だらけ）:**

- `glossary.json` / `シーケンス-3.json`
- `Enter: ステップ追加` / `Alt+↑↓: 並び替え`
- `type=glossary の JSON` / `definition が空文字の状態`
- `9 / 9 件` / `未定義 15`

特にキーボードヒント行は、ラテン部分だけ大きく明るく見え、行としてまとまっていない。

**これは配色やスペーシングでは直せず、フォント選定でしか直らない種類の粗さである。**

なお、字面が小さいことは**「文字が小さく感じる」体感の直接原因**でもある。
同じ指定サイズでも Noto Sans JP / IBM Plex Sans JP なら明確に大きく見えるため、
フォント差し替えだけでサイズ指定を変えずに体感サイズが上がる。

#### M26 での実施

**混植の相手を「別設計の2書体」から「同一設計の3書体」に替えた。**
`--font-sans` は `'IBM Plex Sans Variable', 'IBM Plex Sans JP', 'Yu Gothic UI', 'Hiragino Sans', sans-serif`。
ラテンは Plex Sans、和文は Plex Sans JP、等幅は Plex Mono で、いずれも IBM Plex の
同一設計から来る——U1 が案Aを推した理由（**D7 の解決が構造的に保証され、`size-adjust` の
補正作業が不要**）がそのまま効く形になった。**フォールバック列（Yu Gothic UI / Hiragino Sans /
sans-serif）は残してある**——同梱が何らかの理由で届かなかったときに MS UI Gothic まで
落ちないための保険で、正常時は一度も使われない。

**実機の見え方は未確認である。** 混植が実際に揃って見えるか（D7 が名指しするキーボード
ヒント行・混在セル）は、実機確認のチェックリスト項目4 が持つ
（[`history/m26-core-font-bundle.md`](history/m26-core-font-bundle.md)）。結果はそこへ追記する。

### D8. ウェイトを9段階持つフォントに移行し、階層をウェイトで作る

Yu Gothic UI は CSS の `font-weight` から実質 Regular / Bold の2段階しか到達できず、
中間がない。結果として:

- 弱すぎる → 階層が出ない
- Bold にする → 急に重くなる

の二択となり、テーブルヘッダーとセルが平坦化する。

さらに、ウェイトを持たない字形にBoldを指定すると WebView2 が
**疑似ボールド（合成太字）** を描画する。輪郭を太らせるだけの処理のため、
小サイズでは潰れて汚くなる。（例: `facet 動作確認用サンプル 用語集`）

→ Noto Sans JP / IBM Plex Sans JP はいずれも9ウェイトを持つ。
→ ヘッダー = Medium(500)、本文 = Regular(400)、補助 = Regular＋グレー の3層を組む。

**これが実現すれば、D2で廃止する緑のベタ塗り帯は不要になる。**

#### M26 での決着——3層は「追加変更なし」で実体化した。階層はウェイトに移さない

**rev 9章の予約「E が入ったらカラム名は階層をウェイトに移せる」は、「移さない」と決めて
閉じる。** カラム名（用語集・エラーカタログのテーブルヘッダ、ファイル一覧の種類見出し）は
`font-medium`＋`tracking-wide`＋`text-ink-muted`＋`bg-surface-muted` のままである。

理由は、**E で Medium(500) が実際に描き分かるようになったので、上の3層
（ヘッダ = Medium / 本文 = Regular / 補助 = Regular＋グレー）は追加変更なしで実体化した**
から。M21 が `font-bold` を外して字間・グレー・面へ寄せたのは「和文のウェイト段が
環境依存で出ない」ことへの迂回だったが、迂回した先の4チャネルは**それ自体として正しい**
——字間・グレー・面を外して「ウェイトだけ」に痩せさせる案は、**M21 の実機確認で出た
「面なしでは見出しの行がデータ行に溶ける」という観察と正面から衝突する**ので採らない。

**4チャネルが過剰に見えるかは実機確認で人間が見る**（チェックリスト項目3。過剰と出たら
計画外修正として扱う）。**行間は動かさない**——M23 で「見出しの 130% は本文の 125% に畳む」
と確定済みで、フォントが替わっても根拠（16px では差が 0.8px しかない）は変わらない。

**唯一の `font-bold` は 600 に落とした**（`App.tsx` の「ファイルが開けない」見出し）。
§4.3 の「Regular/Medium/SemiBold で足りる」への答えがこれで、**1箇所のために Mono
以外でもう1段（700）を積むことはしない**。疑似ボールド（合成太字）はもう起きない
——Plex は 600 の実フェイスを持つ。

### D9. 数字を等幅（tabular-nums）にする

facetは**数字が意味を持つUI**である。

- `No` 列の 1, 2, 3
- ステップ番号 `#1`〜`#8`
- `9 / 9 件`
- `未定義 15 / 回答済 2 / 考慮不要 1`

プロポーショナル数字では `1` と `8` の幅が異なるため、桁が揃わず列が微妙に揺れる。
この揺れは意識に上らないまま、雑な印象だけを残す。

→ 数値表示箇所に `font-variant-numeric: tabular-nums` を適用する。
→ あわせて、**テーブル内の数値列は右揃え**とする（桁数を縦方向に比較できるようにするため。
　 SmartHR Design System の表スタイル規約と同じ思想）。

**優先度: 最高。1行で効く、投入コスト比で最も効果が大きい施策。**

### D10. 等幅フォントを「機械が所有する文字列」に割り当てる

これは装飾ではなく**意味の分離**である。

facetの画面には2種類の文字が混在している。

| 種別 | 例 |
|---|---|
| 人間が書いた文字 | 用語の名称、定義、備考 |
| 機械が生成・所有する文字 | `glossary.json`, `type=glossary`, `definition`, nanoid（`gl_` + 10文字）, JSON Schemaのキー名 |

現状これらが同じフォントで描かれているため、
備考欄に書かれた `definition` が、ただの英単語なのか
スキーマ上のフィールド名なのか、見た目から判別できない。

→ 等幅を割り当てることで、
**「これはあなたが決めた言葉ではなく、システムが持っている識別子です」**
という情報が、読まずに伝わる。

### D11. タイポグラフィスケールを固定する ← rev.2 で全面訂正

> **rev.1 の記述「サイズは4段階に固定（例: 11 / 13 / 15 / 19px）」は誤りのため破棄。**
> 11px は日本語の実用下限を割っている。§3 の調査結果に基づき、以下に差し替える。

現状、11〜14px前後が段階を決めずに混在している。
エラーカタログの「表示」「絞り込み」ラベルとチップの高さ違いは、この無管理の表面化。

**確定スケール（27型WQHD / 100%スケーリング環境を前提。根拠はD12）:**

| 用途 | サイズ | 行高 | ウェイト |
|---|---|---|---|
| 文書タイトル | 22px | 130% | Medium (500) |
| セクション見出し | 16px | 130% | Medium (500) ＋ 字間widen ＋ グレー |
| テーブルセル・入力欄（本文） | **16px** | **125%** | Regular (400) |
| 複数行テキスト（定義列・備考列） | 16px | 150% | Regular (400) |
| 補助・キーボードヒント | 14px | 130% | Regular (400) |

**規約:**

- **上記5段階以外のサイズを使わない。**
- **14px を下限とする。** これ未満は使用しない。
- **密度は行高で稼ぎ、サイズでは稼がない。**（本文125% ／ 読ませる箇所のみ150%）
- **小さくするときは、薄くしない。** 現状のキーボードヒントは「小さい＋薄いグレー」の
  二重減衰になっている。小ささと薄さは、どちらか一方だけにする。
- セクションラベルは字間を広げてグレーに。日本語は字間を空けると
  「見出し」ではなく「ラベル」に見える。D2で廃止する緑帯の代替として最も効く。

#### M26 での部分反転

**確定スケールのうち「テーブルセル・入力欄（本文）16px」と「複数行テキスト 16px」を、
入力値については 14px へ反転した**（2026-08-26。人間が実機を見て裁定）。

| 用途 | M23〜M26 | M26 以降 |
|---|---|---|
| 入力値のうち**単行**（用語名・別名・種別・エラー名・対応レベル・シーケンスの種別／参照） | 16px / 125% | **14px / 130%**（`text-sm` の既定行間。**行高の比率も動いた**） |
| 入力値のうち**複数行**（定義列・備考列・自由記述・キャンバスの折り返し） | 16px / 150% | **14px / 150%**（`leading-normal` 明示。**行高 150% だけが据え置き**） |
| セクション見出し（カラム名・ファイル一覧の種類見出し） | 16px / 125% | **16px / 125%（据え置き）** |
| ボタン・バッジ・帯・検索欄・ファイル一覧の行 | 16px | **16px（据え置き）** |
| **アプリが書いた文章**（トースト・`IssueBanner`・空状態の説明） | 16px / 150% | **16px / 150%（据え置き）** |
| 文書タイトル | 22px | **22px（据え置き）** |

**行高まで据え置いたのは複数行の 150% だけである。** 単行の入力値は `text-base`（1.25）から
`text-sm`（1.3）へ移ったので、比率が 125% → 130% に動いている（実高は 20px → 18.2px で、
絶対値としては詰まっている）。**`<button>` で描く種別セル・参照セルはさらに例外**で、
`@layer base` の `--tw-leading: 1.2` を受けるため実効行間は 1.2 である（この上書きは M8 以来の
既存事項で、M26 が作ったものではない）。

**理由は D7 の字面差が逆向きに効いたこと。** D7 は「同じ指定サイズでも Noto Sans JP /
IBM Plex Sans JP なら明確に大きく見えるため、フォント差し替えだけでサイズ指定を変えずに
体感サイズが上がる」と書いていた。M26 で実際に Plex へ移ったところ、その予告どおり
体感サイズが上がり、**上がりすぎた**。人間の言葉は「フォント大きく見えるな？ウェイトは
変えてないんでしょ？1段くらい下げるのありだな。カラム名とかボタンとかバッジのフォント
サイズはそのままでいんだけど、入力値だけ小さくしたいよな」。

**据え置いた側と、割っていない線:**

- **UI ラベル側は 16px のまま。** カラム名・ボタン・バッジ・帯・検索欄・ファイル一覧、
  および文書タイトル 22px。下げたのは**データの文字（入力値）だけ**である
- **アプリが書いた文章も 16px / 150% のまま。** トースト・`IssueBanner` の指摘リスト・
  空状態の説明文は、複数行で「読ませるもの」だが**入力値ではない**ので触っていない。
  この段（`text-base` ＋ `leading-normal`）は M23 のまま生きている
- **14px 下限は割っていない。** 14px は D11 の下限そのもので、規約「14px を下限とする」に
  ちょうど接している。これ以上は下げられない
- **「小さくするときは、薄くしない」は維持。** 濃度（`text-ink`）は一切変えていない。
  小ささと薄さの二重減衰は起きていない
- **「密度は行高で稼ぎ、サイズでは稼がない」は、複数行については維持。** 複数行の入力値は
  行高の比率 150% を据え置いたままサイズだけ下げた（実高は 24px → 21px。方眼のマス目 24px
  との一致が失われた件は rev 9章を見よ）。**単行の入力値については、比率も 125% → 130% へ
  動いている**——`text-base` から `text-sm` へ段を移した結果であって、密度を稼ぐために
  行高をいじったのではない（実高はむしろ 20px → 18.2px と詰まっている）

**D12 の 16px 導出は読み直せる。** D12 は `14 × (109 / 96) ≒ 15.9 → 16px` と PPI から
基準値を導いたが、この 14px は**字面の小さい書体（Yu Gothic UI）を前提にした体感**の上に
立っていた。書体を字面の大きい Plex に替えれば、同じ体感に必要な指定サイズは下がる。
つまり D12 の正規化は「物理サイズの正規化」であると同時に「Yu Gothic UI の字面込みの
体感の正規化」でもあり、書体が変われば導出値も動く。**D12 の計算式そのものは訂正しない**
——PPI 補正の論理は正しく、変わったのは入力側の前提である。

### D12. 表示環境に基づき基準値を正規化する ← rev.2 で新規追加

**前提となる実機環境: 27インチ WQHD（2560×1440）、Windows表示スケーリング 100%**

CSSの `px` は物理的な長さではなく、**96 PPI を基準とした仮想単位**である。
表示スケーリング100%のとき、この仮想値がそのまま実ピクセルに1:1で写される。

27型WQHDのパネル実効密度は **約109 PPI**。96 PPIより密であるため、

```
96 / 109 ≒ 0.88
```

つまり同じ `14px` でも、**物理的には基準比 約88%（= 12.4px相当）の大きさでしか届かない。**

これはD6と同じ構造の問題である。フォントがOSに依存していたのと同様に、
**寸法の物理的な現れがモニターのPPIに依存している。**

**対処: 基準値そのものを引き上げて正規化する。**

```
14 × (109 / 96) ≒ 15.9  →  16px
```

D11の本文16pxはこの計算から導かれている。
結果的に、デジタル庁デザインシステムおよびSmartHR Design Systemが
基準値としている16pxと一致する。

### D13. ズームではなく基準値で解決し、ボーダーは1pxで固定する ← rev.2 で新規追加

D12の正規化は、WebViewズーム（1.13倍）でも数値上は達成できる。**しかし採用しない。**

**理由: 倍率が非整数になり、ヘアラインが破綻する。**

ズームは画面全体を等倍で拡大するため、1pxのボーダーが1.13pxになる。
物理ピクセルは分割できないので、レンダラは1pxに丸めるか、
アンチエイリアスで薄く滲ませるかを選ぶ。結果として
**罫線がにじみ、隣り合う線で太さが揃わなくなる。**

facetは方眼グリッド・テーブル罫線・ツリーのエッジといった
**ヘアラインが画面を構成している比率が高い**ツールである。
ここが滲むことは、まさに「安っぽく見える」原因そのものになる。

**確定方針:**

| 項目 | 決定 |
|---|---|
| ズーム | 100%固定。D12の正規化はフォントサイズ側で吸収する |
| ボーダー・グリッド線 | **1pxで固定**。文字サイズが変わっても追従させない |

文字が大きくなっても罫線は1pxのままというのは、印刷物や
ネイティブアプリと同じ振る舞いであり、正しい挙動である。
相対的に線が細くなることで画面が締まる副次効果もある。

**将来ズームを提供する場合の制約（現時点では実装しない — YAGNI）:**

- ルート `font-size` ではなく WebView ズームを使うこと。
  `rem` ベースで文字だけ拡大すると、1pxボーダー・方眼・アイコンが追従せず比率が崩れる。
- ズーム値および基準フォントサイズは、**Git管理下のプロジェクトJSONに保存しない。**
  これは仕様ではなく機材固有の設定であり、別PCで開いたときに他人の環境設定が
  降ってくる。差分ノイズにもなる。Tauriのappデータ領域（ローカル設定）に置く。

**トークンの持ち方:**

サイズを直接 `16px` と書かず、`--font-base` を基準とした相対値で定義する。
将来96 PPIの機材や4Kで開いた際、基準値1つの差し替えで全体が追従する。

#### rev.4（M23）での訂正

> 上の表の「ズーム: **100%固定**」は誤り。**既定は100%**であり、**手動ズーム（Ctrl+± / Ctrl+0）は逃げ道として残す。**

**経緯:** 2026-08-16、人間が「アプリの拡大縮小を可能に」として
`src-tauri/tauri.conf.json` に `zoomHotkeysEnabled: true` を入れた（`2fa2785`）。
これは D13 の「100%固定」と真正面から矛盾しており、
M23（タイポグラフィスケール v2）で**どちらを正とするかを人間に裁定してもらった**。

**裁定（2026-08-24）: 残す。**

| 項目 | rev.2 の記述 | rev.4 での訂正 |
|---|---|---|
| ズーム | 100%固定 | **既定100%。手動ズームは残す** |
| 正規化の担い手 | フォントサイズ側 | **変更なし**（D12の正規化は基準値側で吸収する） |
| ボーダー・グリッド線 | 1px固定 | **変更なし** |

**ズーム中に1pxの罫線が滲むことは、代償として受ける。**
D13の理由（ヘアラインの破綻）は事実として正しく、取り下げていない。
訂正しているのは「だから機能を持たない」という帰結の方である——
**滲みを承知で拡大したい場面がある**という利用者の判断を、
設計側が先回りして塞ぐ必要はない。既定が100%である限り、
通常の利用でヘアラインが滲むことはない。

**将来制約（上の「将来ズームを提供する場合の制約」）は生きたままで、
`zoomHotkeysEnabled` は両方を満たしている**——WebView ズームそのものであり
（ルート `font-size` ではない）、倍率はどこにも保存されない
（プロジェクトJSONにも、appデータ領域にも）。
**ズーム倍率の保存とズームUIは、引き続きYAGNIとして持たない。**

なお rev 9章（`overview-rev.md`）にも、この訂正後の内容を規範として書いてある。

### D14. 判断軸と欠落軸を、別の視覚チャネルに分離する ← rev.3 で新規追加

facetの画面には、**直交する2つの軸**が同時に載っている。

| 軸 | 値 | 意味するもの |
|---|---|---|
| **欠落軸** | 未定義 / 未決 / 仮説なし | まだ埋まっていない |
| **判断軸** | 支持 / 棄却 / 見送り | 埋まった上で、どう決めたか |

**問題**: 両方に色相を割り当てると必ず衝突する。

とくに「棄却」に最も自然な色は赤だが、赤は欠落軸が持っている。
これを許すと**「決着済み」と「未着手」が同じ色になる**という、
意味としては正反対の最悪の混同が起きる。

**解決: 色相を奪い合わせず、そもそも別の視覚チャネルに載せる。**

| 軸 | チャネル | 表現 |
|---|---|---|
| 欠落軸 | **輪郭** | 赤の破線枠、**塗りなし** |
| 判断軸 | **面** | **塗りつぶし**、色相で区別 |

破線枠のチップと塗りチップは形状からして別種であるため、
色相が近くても混同しない。
地図が「行政区分は面の色、道路は線の色」と分けるのと同じ発想である。

**現状評価**: 課題ツリーは既に欠落軸を破線枠で表現できている。
**規約として明文化すれば足りる**状態にある（U3参照）。

### D15. 判断軸の配色 ← rev.3 で新規追加

**現状の問題**: `支持` が濃いオリーブ、`棄却` が黒。
**どちらも同じサイズ・同じ形・同じ塗り方の濃色チップ**であり、輝度差がほとんどない。

正反対の結論が同じ視覚的重みで置かれている。周辺視野では完全に同一に見え、
暗い環境やダークモードでは差がさらに縮まる。

構造的には、**色相という最も弱い識別チャネルだけに、最も重要な意味を載せている**状態。
人間の視覚は輝度差・形状差には敏感だが、小面積の色相差には鈍い。

これはD2の「1色1意味」より深刻で、**2つの意味が1つの見た目を共有している**ケースである。

**確定方針:**

| 状態 | 塗り | 意味の方向 |
|---|---|---|
| 支持 | 緑（明度高め・彩度あり） | 前進する |
| 棄却 | ダークグレー〜チャコール | 検討は閉じた |
| 見送り | ライトグレー（枠のみでも可） | 検討自体を先送り |

**規約:**

- **彩度を持つのは「支持」のみ。** 棄却・見送りは無彩色の明度差で分ける。
  3色すべてに彩度を与えると、どれも同じ強さで主張して結局読めなくなる。
  目的は「**前進しているものが一目で拾えること**」。
- **支持を明るく、棄却を暗く**することで、白黒印刷でも判別可能にする。
  現状の問題は色相の選択ではなく、支持のオリーブと棄却の黒の**輝度が近すぎること**だった。

**却下した案: 取り消し線（棄却）**

2つの理由により採用しない。

1. **意味の問題**: 取り消し線は「削除された」「無効になった」を含意する。
   しかし**棄却は失敗ではなく、検討した結果として正当な結論**である。
   仮説検証の文脈では、棄却できたこと自体が成果。削除と同じ記号で描くのは思想に反する。
2. **物理的な問題**: 小さいチップに取り消し線を引くと文字が潰れる。

### D16. 分類には色を付けない ← rev.3 で新規追加

用語集の種別（アクター／状態／イベント／画面／データ）や、
エラーカタログの解決レベルも色分けしたくなるが、**行わない。**

**理由:**

| 理由 | 内容 |
|---|---|
| 数が増える | 分類は増え続ける。増えるたびに色を追加すると、いずれ虹色になる |
| 順序の嘘をつく | 分類には優劣がない。色相は無意識に順序や重要度を示唆するため、存在しない序列を伝えてしまう |
| 必ず衝突する | 数が増えれば、いずれ赤系（欠落軸）・緑系（判断軸）に衝突する |

**規約:**

> **色を持つのは「判断」と「欠落」だけ。「分類」は持たない。**

この線引きがないと、半年後には元の状態に戻る。

### D17. 見送り配下の未決は、要対応から外し別枠で集計する ← rev.3 で新規追加

**現状の問題**: `需要がない` ノードは全体がグレーアウトされているが、
その中に `未決 2` の赤いチップが入っている。

グレーアウトは「注意を向けなくてよい」、赤は「注意を向けろ」という信号であり、
**同じノードが相反する2つの指示を同時に出している。**

さらにヘッダーの `要対応 4` には、このグレーアウトされた2件が算入されている。
「対応が必要」と集計しながら「見なくていい」と描画している。

**決定: グレーアウトは維持し、要対応カウントからは外す。**

ただし、単に外すと**そのノードは要対応から完全に消える**。
facetは「未定義を見えるようにする」ツールであるため、これは核心機能に穴を開ける。

また見送りは、棄却と違って**判断が保留されている状態**である。
前提が変われば戻ってくるため、そのとき存在を忘れていては記録した意味がない。

構造的には在庫と同じで、**倉庫にしまうことと、廃棄することは別**である。
しまったものは台帳に残さなければならない。

**確定仕様: カウントを2段構えにする。**

```
⚠ 要対応 2  未決 2       ← 見送り配下は含まない（現在の作業対象・赤）
   見送り 1（未決 2件）   ← 別枠（凍結中の在庫・グレー）
```

前者だけが赤、後者はグレー。
**視覚的な緊急度は下がるが、存在は消えない。**
グレーアウトの意図（今は見なくてよい）と、facetの原則（欠落は消さない）が両立する。

実装は、既存の集計ロジックに「見送り配下かどうか」の判定を足し、
2つの数値を返すだけでよい。**データ構造の変更は不要。**

**M25 での実施:** 集計の前半（要対応から外す）は**実装済みだった**——`poseQuestions`
（`src/modules/issue-tree/derive.ts`）は抑制配下では問いを立てず、`tallyQuestions` は
立った問いだけを数える。M25 が足したのは**別枠の側**である。別枠は
`見送り N`（`deferredIssueCount` / `deferralLine`）——**件数だけで、配下の凍結中の
問いの内訳・総数は出さない**（人間の裁定。上の確定仕様の `（未決 2件）` の部分は
実装していない）。チップはグレー（`badgeClass('deferred')`）で押すと見送りを掲げた
課題を巡回し、`title` に「見送り配下の問いは要対応に数えません」（`DEFERRAL_NOTE`）
を出す。登録 Skill の報告（`issue-tree-write.mjs --check`）も同じ行を出す。

### D18. 見送り配下は祖先から導出する。フィールドを持たない ← rev.3 で新規追加

D17の集計を成立させるには、「見送り配下」の判定ルールが必要。

**確定ルール:**

> **祖先に1つでも見送りがあれば、そのノードは見送り配下とみなす。**
> これは**導出**であり、子ノードに見送りフラグを持たせない。

**根拠**: facetの単一権威ソース原則。
「実装ステータスはGitブランチ位置から導出」「表示順は配列順から導出」と同じ思想であり、
子に見送りフラグを複製すると、親子で不整合が起きうる状態を作ってしまう。

**未確認事項**: 現状の画面では `需要がない`（見送り）と `認知されてない` が
**破線**で繋がっており、これが親子関係なのか別種の参照なのかが判別できない。
D18の判定は親子関係に依存するため、破線の意味を確定させる必要がある（§5参照）。

**M25 での決着:** 上の確定ルールは**実装済みである**——正は
`src/modules/issue-tree/derive.ts` の `suppressedIssueIds`（祖先を遡る導出。
フィールドは持たず、循環しているファイルでも止まらない）。未確認だった破線の意味は
U4 の決着（親子エッジ。破線＝抑制の状態）で解消した。

### D19. ボタン階層を3段階に固定する ← rev.3 で新規追加

**現状の問題**: 黒塗り／白＋枠／枠のみ／枠なしアイコンの4種が混在。
さらに `フォルダを開く` が黒塗り（=最強調）だが、これは**起動時に1回押すだけの操作**であり、
強調の重みと使用頻度が一致していない。

ボタンの見た目は装飾ではなく、
**「この画面で何が主要な操作か」という情報を運ぶ**。
形状の羅列ではなく、強調度で設計する。

**確定スケール:**

| 段階 | 見た目 | 用途 | facetでの例 |
|---|---|---|---|
| Primary | 塗り（無彩色のダーク） | 画面に1つだけ | （下記参照。現状は該当なし） |
| Secondary | 枠線のみ | 通常操作 | 課題を追加、仮説を追加、Markdownをコピー |
| Tertiary | 枠なし・アイコンのみ | 補助・低頻度 | undo/redo、右上のパネル切替 |

**規約:**

- **ボタンは彩度を持たない。** 色の予算は判断軸（緑）と欠落軸（赤）が使い切っている。
  ボタンは無彩色の明度差だけで階層を作る（D2・D16と同じ原則）。
- **破壊的操作（削除）も常時は赤にしない。** 赤は欠落軸が持っているため。
  ホバー時にのみ赤系に変化させるなど、常時表示しない形に留める。

**重要: facetでは Primary を使わない。**

エディタ画面の主操作は「セルに文字を打つ」ことであり、ボタンではない。
その場合、**3段階すべてを埋める義務はない。**

ツールバーが全てSecondaryで揃っていれば、それは
「ここに主要操作はない、本体はキャンバスだ」という**正しいメッセージ**になる。
無理に1つ黒く塗ると、そこが主役だと嘘をつくことになる。

### D20. ダークパレットの吟味 ← M27 で §9 スコープ外から昇格

M21 まで、ダーク（`palette.css` の `.dark`）は「要件（`palette.test.ts`）を
通る値を置いただけ」だった。M27 で全数を実測して吟味した。
**新しい設計原則は増えていない**——ライトで確定した原則（D14〜D16、
「淡い面＋線＋文字」、MARGIN 1.03、D型は明度で分ける）を暗い地に
翻訳しただけである。ただし翻訳で明度の向きが反転するため、
単純写像にならなかった箇所が2つある。

**動かした値は2つだけ:**

| トークン | 変更 | 根拠 |
|---|---|---|
| `invalid-face` | L 0.30 → 0.28 | `invalid` が上で 4.52:1 と MARGIN 割れだった（→ 4.83:1）。`invalid` 側を明るくする解は `missing` との D型分離を削るので採らない。ライトの面の構図（invalid-face だけ地から一段遠い）の翻訳でもある |
| `judge-yes` | C 0.10 → 0.08 | P型で `missing` との ΔE が 0.101 と `DISTINCT_MIN` 0.10 ちょうどだった（→ 0.107。ライトの最薄 0.105 と同水準） |

**知見: ダークでは意味色4軸が全部「明るい色」側に集まる。**
ライトは黄土 L 0.49・赤 0.38・青 0.48 と暗い側に散らせたが、暗い地では
全軸を明るくするしかなく、`judge-yes` の L は `missing`（0.82）と
`pending`（0.75）に挟まれて **0.80 付近しか居場所が無い**——彩度を
ライトと同水準（C 0.08 以上）に保ち、これ以上浮かせない（L 0.80 以下）
という条件の下では、全制約を満たす格子点は L 0.80 C 0.08 の1点だけ
だった。C を 0.07 以下へ薄めれば L 0.76〜0.79 も可行になるが、
canvas 比は 10.6:1 → 9:1 までしか下がらず、「浮きすぎ」の解には
ならないまま緑の彩度を失う。`judge-yes` は暗い地で
canvas 比 10.6:1 と明るく浮くが、**これを暗くして沈める調整は識別との
綱引きになる**——実機で「浮きすぎ」と出たら、fg の反転（明るい面＋
暗字 → 暗い面＋明るい字）まで含めた構図の再設計が要る。

**据え置き 16 個の根拠**（吟味した結果の据え置きであり、未吟味ではない）:
`ink` on `surface` 12.5:1 は GitHub Dark（≈14:1）と VSCode Dark+（≈11.5:1）の
間（M7 設計スペックの比較先例と同じ物差し）。`grid` は canvas 上 1.195:1 で
ライト（1.202:1）と同水準。淡い面はダークでは地との分離（1.29〜1.41:1）が
最初から付いており、ライト（`missing-face` は canvas と 1.006:1、区別は
色相だけ）より条件が良い。

実機確認（暗い面での黄土の破線・`judge-yes` の浮き・赤と黄の分かれ・
淡い面の浮き）は人間の作業として残る。結果は
`docs/history/m27-core-dark-palette.md` に記録する。

**実機確認の一巡目（2026-08-26）——無彩色の面の序列を作り直した。**
上の机上吟味が「据え置き」とした無彩色3面に、実機でしか見えない欠陥が
あった：**ライトの面の序列は muted（0.91）< canvas（0.95）< surface
（0.985）——「沈んだ面 < 地 < 作業面」の輝度順——だが、ダークは
canvas（0.17）< surface（0.205）< muted（0.27）で、凍結を示すはずの
見送りの箱が3面で最も浮いて見えた。** 机上吟味は意味色と淡い面に
「ライトの構図の翻訳」を適用したのに、無彩色3面には適用していなかった。

対応：**ダークもライトと同じ輝度順にする**——`surface` 0.205 → 0.24
（canvas 比 1.07 → 1.16:1。ノードが地から浮く）、`surface-muted`
0.27 → 0.13（地より暗い側へ。沈む向きがライトと揃う）。`surface` の
天井は 0.28（`ink-faint` の 3:1×MARGIN が割れる）。

**派生して `rule-muted`（19個目のトークン）を分離した。** `surface` を
上げた瞬間、表の罫線（当時 `border-grid`）が面と同明度（1.03:1）で
消えた。ライトでは `grid`（0.89）が canvas（0.95）にも surface（0.985）
にも暗い側で差が出るので1値で方眼と罫線を兼ねられたが、**ダークでは
canvas と surface が線を挟むため、方眼（canvas 上）と罫線（surface 上）
は1値で両立しない。** 罫線・弱い境界（テーブルの行と列・ファイル一覧の
行・別名バッジの枠）を `rule-muted`（ダーク 0.40、surface 上 1.79:1）へ
移し、`grid` は方眼専任として 0.20（canvas 上 1.06:1）へ薄めた。
ライトの `rule-muted` は `grid` と同値 0.89——ライトの見た目は変えない。
命名は `ink` → `ink-muted` / `surface` → `surface-muted` と同じ派生規則
（`rule` ＝ 3:1 で見せる境界の、弱い版）。要件は課さない（`grid` と同じ
装飾扱い。理由は `palette-requirements.ts` のコメント）。

---

## 3. タイポグラフィの一次情報調査（rev.2 で追加）

「文字が小さいのではないか」という問題提起を受けて、日本語UIの相場を調査した。

### 3.1 デジタル庁デザインシステム（最も参考になる）

業務システム・管理画面向けに **Dense** という専用テキストスタイルを定義している。
データテーブル内での情報提示など、表示領域に制約がある場合に使うもの。

| スタイル | サイズ | 行高 | 用途 |
|---|---|---|---|
| Dense (Dns) | 17 / 16 / **14px** | 120% or 130% | 業務システム・データテーブル |
| Standard (Std) | 16〜45px | 150〜175% | 一般的な読み物・本文 |
| Oneline (Oln) | 17 / 16 / 14px | 100% | ボタン等のUI要素（1行前提） |
| Mono | 17 / 16 / 14px | 150% | コード系コンテンツ |

**最重要の発見（2点）:**

1. **Denseの最小が14pxで止まっている。** サイズ全体の方針としても、
   14pxは領域的制約がある場合にのみ使うもので、
   **14px未満は原則として許容されないと明記されている。**
2. **密度はサイズではなく行間で稼いでいる。** Denseは行高を120〜130%まで詰めることで
   情報量を確保しており、フォントサイズは削っていない。

→ 現行facetは「サイズが小さく行間が広い」という**真逆の構成**になっている（§1.4）。

### 3.2 SmartHR Design System

- 基準値は **16px**
- `S`（13.71px）と `XS`（12px）は、**積極的な使用を避け、
  レイアウト上どうしようもない場合にのみ使う**と位置づけられている
- 表の数値列は、桁数を縦に比較できるよう右揃えにする規約がある（→ D9に反映済み）

### 3.3 Fluent 2（Microsoft / デスクトップ）

| トークン | サイズ | 行高 |
|---|---|---|
| Body1 | 14px | 20px |
| Caption1 | 12px | 16px |
| Caption2 | 10px | 14px |

デスクトップ前提のため一段小さい構成だが、**欧文基準のスケールであり
日本語にそのまま適用はできない。** 参考値として記録するに留める。

### 3.4 結論

日本語の業務系デスクトップツールとしては、
**本文14〜16px・行高120〜130%が定石**であり、
27型WQHD環境での正規化（D12）を加味して **本文16px** を採用する。

---

## 4. フォント候補とライセンス

### 4.1 ライセンス整理

| フォント | ライセンス | 商用 | アプリ同梱 |
|---|---|---|---|
| IBM Plex Sans / Sans JP / Mono | SIL OFL 1.1 | 可 | 可 |
| Noto Sans JP | SIL OFL 1.1 | 可 | 可 |
| Inter | SIL OFL 1.1 | 可 | 可 |
| JetBrains Mono | SIL OFL 1.1 | 可 | 可 |

**SIL Open Font License 1.1** は、
「フォントファイルそのものを単体で売ることは禁止するが、それ以外はほぼ自由」
という設計のオープンライセンス。ソフトウェアへの同梱配布、改変、商用利用すべて可能。

実務上の制約は2点のみ:

1. **改変時は元の名前を名乗り続けてはいけない**（Reserved Font Name条項）。
   サブセット化のみであれば通常は改変にあたらないと解釈されるが、
   名前を変えておけば確実。
2. **ライセンス文の添付**。`OFL.txt` をフォントと同じ場所に置くか、
   アプリのライセンス表記に含める。

→ facetでは Tauri の `resources` にフォントと `OFL.txt` を並べて配置すれば完了。

なお、デジタル庁デザインシステムも Noto Sans JP / Noto Sans Mono（いずれもOFL）を
採用しており、公的機関での採用実績がある。

### 4.2 避けるべき選択肢

| 選択肢 | 理由 |
|---|---|
| Yu Gothic / Meiryo の同梱 | **再配布不可**。ライセンス違反になる |
| Google Fonts の CDN 読み込み | オフラインで動作しなくなる。デスクトップアプリが起動のたびに外部通信するのは避けるべき |
| 日本語フォントの字数サブセット化 | **禁止**。仕様書ツールである以上、ユーザーがどの漢字を入力するか予測できない。珍しい人名や専門用語が豆腐（□）になる |

### 4.3 ファイルサイズ対策

日本語フォントは収録字数が多く、全ウェイトを入れると数十MB規模になる。

→ **必要なウェイトのみに絞る**（Regular / Medium / SemiBold の3つで足りる）
→ **`unicode-range` でラテンと和文をファイル分割**する。
　 ブラウザが「この文字を描くのにどのファイルが要るか」を判断して
　 必要な分だけ読むため、初期描画が軽くなる。

字数削減ではなくウェイト削減で対処する。

---

## 5. 個別モジュールの指摘

### 課題ツリー ← rev.3 で追加

**本モジュールは他より明確に進んでおり、U3の参照実装とすべきである。**

#### 到達できている点（規約化の対象）

1. **欠落軸の表現が統一されている。**
   `未決` と `仮説なし` という別種の欠落が、**どちらも赤い破線チップ**で表現されている。
   D1が求めていた状態そのもの。
2. **空欄を放置せず、欠落として「存在させて」いる。**
   用語集の「別名なし」という黒テキストと比べ、思想が正しく実装されている。
3. **全体の未定義量が集計されている。**
   ヘッダーの `⚠ 要対応 4 / 仮説なし 2 / 未決 2` が、ツリー内と同じチップ表現で集計され、
   一目で総量が分かる。**他モジュールにない到達点。**

→ U3は、ゼロから規約を考えるのではなく、**この画面を言語化する**作業とする。

#### 問題点

| 問題 | 対応 |
|---|---|
| 支持／棄却が輝度差ほぼゼロで識別できない | **D15**（最優先） |
| ノード幅可変によりバッジが散り、スキャンできない | **D3**（再定義） |
| グレーアウトと赤警告が同一ノードで矛盾 | **D17** |
| 選択状態の緑が、用語集ヘッダーの装飾緑と衝突 | **D2**。選択は彩度ではなく枠線の太さ／リングで示す |
| `認知されてない` 内のカード内カードが木の文法を破っている | **D3**。ツリー上には `FB 1件` 等の指標のみ置き、実体は別パネルへ |

#### 未確認事項

- **破線エッジの意味が不明。** `需要がない` と `認知されてない` を結ぶ破線が、
  親子エッジと同じ視覚チャネル（線）を使いながら別の関係を表しているなら混同される。
  **D18（見送り配下の導出）は親子関係に依存するため、これを先に確定させる必要がある。**
  ——**M25 で決着**（U4 の決着の節を見よ。親子エッジであり、別種の参照は存在しない）
- ルートの `★` はASCII記号であり、他のどの表現とも系統が違う。
- キーボードヒントが5項目に増え、小さく薄いまま（D11の二重減衰）。

### 用語集

- 別名列: バッジ表示と「別名なし」プレーンテキストが同一列で混在
- 種別セレクトがOSネイティブ（shadcn/ui未適用）
- テーブル外に方眼グリッドが見えており、影も余白ルールもないため「板が浮いている」

### ロジックツリー

- エッジ線とノードのボーダーがほぼ同じ濃度。
  エッジは構造そのものなので、主張させるか、逆にノードのボーダーを消して面だけで表現するか、
  どちらかに振るべき
- ノードの角丸が大きめ（pill寄り）で、テーブルの角丸ゼロと明らかに別デザイン。
  **同一アプリに見えない**
- 右上のキーボードヒントが半透明で、下の方眼が透けている

### エラーカタログ

- 「表示」「絞り込み」ラベルがチップより小さいグレー文字で、ベースラインも揃っていない。
  ツールバーの一行が3種類の高さで構成されている
- テーブルが3行で終わり、下に広大な方眼が残る。
  コンテンツ領域の底が定義されていないため未完成に見える

### シーケンス

- 右パネルの回答欄が画面右端で切れている。未完成に見える最大要因の一つ
- 方眼グリッドのコントラストが高く、コンテンツと競合している
- 入力欄がすべて「枠付きの箱」で描かれており、画面の大半がフォームに見える。
  高密度入力なら、枠を消して罫線とし、ホバー／フォーカス時だけ枠を出す方が
  密度と静けさを両立できる

---

## 6. 補足: 現行の配色が持つもう一つの問題

現行UIは「クリーム地（#F4F1EA 近辺）＋ 高コントラストの見出し ＋ 温かみのある差し色」
という構成になっている。

この配色は、現在AIが生成するデザインが強く収束している3パターンのうちの1つに一致する。
とくに温かいクレイ／テラコッタ系の差し色（#D97757 近辺）は
Anthropic自身のアクセントカラーであるため、この組み合わせは
**「AIに作らせた画面」の記号として読まれるリスクがある。**

D2（無彩色ベース＋赤のみ）への移行は、この観点からも合理的である。
機能的要請（赤の情報量を回復する）と、識別性の要請が同じ方向を向いている。

---

## 7. 未決事項

### U1. フォントセットの選定 ← **2026-08-26 に決着（案A）**

| 案 | 構成 | 利点 | 欠点 |
|---|---|---|---|
| A | IBM Plex Sans + Sans JP + Mono | ラテン・和文・等幅が**同一の設計思想**で作られており、混植のズレ（D7）が構造的に起きにくい。ウェイトも揃っている | 字形にやや個性がある |
| B | Noto Sans JP + Noto Sans Mono | デジタル庁デザインシステムと同一構成。公的採用実績あり | ラテン側の選定が別途必要 |
| C | Inter + Noto Sans JP + JetBrains Mono | 字形の癖が少ない | Noto Sans JP は字面が大きく Inter と並べると和文が勝つため、`size-adjust` での補正が必要 |

**推奨: 案A**（D7の解決が構造的に保証され、補正作業が不要なため）

**2026-08-26 の決着（M26 前段の決定セッション。人間の合意取得済み）: 案A＝IBM Plex 全面移行。**

- **前提の更新:** 上の3案は Geist 導入前の記述である。M7 でラテンは Geist（OFL）を
  バンドル済みだったため、「**第4案: Geist を維持し和文だけ同梱する**」
  （Noto Sans JP 可変＋Geist Mono、約5.1MB）が実在し、これも並べて諮った。
- **採用構成:** ラテン＝**IBM Plex Sans**（`@fontsource-variable/ibm-plex-sans`、
  可変 wght 100–700、woff2 約1.3MB。**M7 で確定した Geist は外す**——rev 9章の
  書体の項の反転は M26 の完了コミットで行う）／和文＝**IBM Plex Sans JP**
  （`@fontsource/ibm-plex-sans-jp`。**variable 版は npm に存在しない**。static の
  **400/500/600 の3段**を同梱、woff2 約6.8MB＝1ウェイト約2.25MB）／
  mono＝**IBM Plex Mono**（`@fontsource/ibm-plex-mono`、static **400＋700**、
  約0.1MB。700 は端末の ANSI 太字用）。配布サイズ増は**合計約8.2MB**（woff2 実測）。
- **§4.3「Regular/Medium/SemiBold で足りる」への答え: 足りる。** 唯一の
  `font-bold`（`App.tsx` のファイルが開けない見出し）は 600 へ落とし、1箇所のために
  700 の約2.3MB を積まない（Plex に 800/900 は無いが使用箇所も無い）。
- **同梱の作法:** fontsource の static 版 CSS は woff2 と woff を両方参照するので
  **素の import は禁物**——woff2 だけ参照する CSS を自前で持つ。unicode-range の
  124分割は CJK 拡張まで含む全字収録で、§4.2 の字数サブセット禁止に抵触しない。
- **退けた案:** 第4案はサイズと管理（可変1軸に9段全部入り）で優位だったが、
  実フォントの比較ページ（D7 が名指しする3箇所＋ウェイト段＋ラテン単体）を見た
  人間が、正しいサイズ差の提示後の再確認でも Plex の字面を選好した。D7・D8 の成立は
  **両案とも Chromium 実測で確認済み**（字幅で 400/500/600 の描き分けが分離）なので、
  決め手は見た目の判断＝U1 が「人間の合意が要る」とした当のものであり、単一の
  技術的理由による棄却ではない。案B/C は Geist 導入後「ラテン別選定」という前提
  自体が古く、三点同一設計という案Aの利点への追加の利点が無い（案C は
  `size-adjust` も要る）。
- **未検証のまま M26 へ送る前提:** ① WebView2 実機での可変ウェイト描き分け
  （10分実験。JP は static なのでリスクはラテン可変側のみ）② unicode-range の
  遅延ロードと `canvas-font.ts` の測定整合——後から珍しい漢字を入力したとき
  スライスが遅れて届き測定がずれないか（`fonts.onloadingdone` での再測定要否）
  ③ Tauri ビルド後の実配布サイズと woff 排除の確認 ④ 端末（xterm は canvas 描画）が
  `@font-face` の Plex Mono を拾うか。なお**和文の等幅は OFL 候補が実在しない**ため、
  端末に出る和文は今後も OS フォールバックのまま（全案共通の制約）。

**M26 での数値の訂正（決定の反転ではない。記録の精密化である）:**

- **「unicode-range の 124分割」は誤りで、正しくは 123分割／ウェイト。** 124 は
  `node_modules/@fontsource/ibm-plex-sans-jp/files/` の**ファイル数**を数えたもので、
  **CSS から参照されない一体型の `japanese-*-normal.woff2` を1本余計に含んでいた**。
- **同じ理由で「JP 約6.8MB」「配布サイズ増は合計約8.2MB」も過大である。** 実際に
  参照・同梱される woff2 は **385本・計 4.41MB**（`npx vite build` 後の `dist/assets` 実測。
  JP 369本 4.16MB／Sans 可変 6本 0.15MB／Mono 10本 0.10MB）。インストーラの増分は
  **+4.52MB**（3.67MB → 8.19MB）。
- **採用構成・退けた案・§4.2 の字数サブセット禁止に抵触しないという判断は、いずれも
  変わらない**——分割の本数が 124 でも 123 でも「全字収録のまま unicode-range で割る」
  という形は同じであり、サイズは見積もりより小さかった側にずれている。
- **未検証のまま送った前提のうち ③ は M26 で成立を確認した**（`.woff` 0本・
  data URI 化 0件・実配布サイズは上のとおり）。①②④ は実機確認へ持ち越した
  （[`history/m26-core-font-bundle.md`](history/m26-core-font-bundle.md)）。

### U2. グレー階調の刻み数と、各色の具体値 ← M21 で決着（M26 で書き起こし）

D2・D15・D19の実施にあたり必要。未着手。

決めるべきもの:

- グレー階調の刻み数（背景 / ボーダー / テキスト補助 / 棄却 / 見送り で最低5段階が必要）
- 支持の緑の具体値（明度高め・彩度あり。装飾緑との差別化が前提）
- 欠落の赤の具体値と、破線の太さ・破線パターン

**M21 での決着（実装からの言語化。U4 を M25 で閉じたのと同じ形）:** 上の3項は
**M21 の役割トークン v2 が実質すべて答えている**——M26 の着手前に突き合わせて確認した。
残っていたのは「文書に書き起こす」ことだけである。

- **グレー階調は8段。** `src/styles/palette.css` の無彩色（C = 0）のトークンで、
  面が3つ（`canvas` / `surface` / `surface-muted`）・文字が3つ（`ink` / `ink-muted` /
  `ink-faint`）・線が2つ（`rule` / `grid`）。**「最低5段階が必要」は満たしている。**
  ただし**刻みの割り当ては U2 が想定した軸とは違う**——U2 は「背景 / ボーダー / 補助 /
  棄却 / 見送り」と**用途**で数えていたが、M21 は**チャネル**（面・文字・線）で数えた。
  棄却は判断軸の濃い面（`judge-no`）が持ち、見送りは専用トークンを持たず
  `surface-muted` ＋ `rule` ＋ `ink-muted` の組み合わせで表す（rev 9章）。
  **なおライトでは `ink-faint` と `rule` が同値**（`oklch(0.58 0 0)`。ダークは
  0.55 と 0.56 で分かれている）**なので、実効の階調は7段である（トークンは8個）。**
- **支持の緑の具体値は `judge-yes`（明るい青緑）。** 「装飾緑との差別化」という前提は
  **前提ごと消えた**——D2・D16 で装飾の緑（用語集ヘッダの帯・選択状態）を全廃したので、
  緑は判断軸が専有している。値そのものは `palette.css` にあり、
  `src/styles/palette-requirements.ts` ＋ `src/styles/palette.test.ts` が
  コントラストと色差（標準・P型・D型で OKLab 色差 ≥ 0.10）を機械検証する。
  **具体値をこの文書に写さない**——差し替え可能な下敷きの側の値なので、正は `palette.css`。
- **欠落の赤は、赤ではなく黄（`missing`）になった。** 赤（`invalid`）は無効軸が取り、
  欠落軸は黄土色の `missing` ＋ 淡い面 `missing-face` を持つ（D型色覚で黄と赤は色相が
  重なるため、明度で分けている）。**破線の太さ・パターンは実装が持つ**——バッジは
  `src/components/badge-styles.ts`、課題ツリーの抑制エッジは
  `src/modules/issue-tree/IssueTreeEdges.tsx` の `SUPPRESSED_DASH`。
  **線種が運ぶ意味は破線＝まだ見ていない／実線＝見たが決められない**（rev 9章）。

### U3. 「未定義」判定の共通規約 ← M22 で決着（M26 で書き起こし）

D1実施の前提として、
**「未定義とは何か、どう見せるか」を全モジュール共通の規約として1枚の文書に固定する**
必要がある。D1・D2は設計トークンの書き換えでは済まず、
各モジュールの描画ロジックに手が入るため。

**rev.3 での変更: 課題ツリーを参照実装とする。**

課題ツリーは既に欠落軸を破線チップで統一表現できており（§5参照）、
規約はゼロから設計するのではなく**既存実装の言語化**として作成する。
これにより作業量が大幅に減り、かつ「動いている実装がある」という裏付けが得られる。

規約に含めるべき項目:

- 欠落軸は輪郭（赤の破線枠・塗りなし）で表現する（D14）
- 空セルに文字列を埋めない（D1）
- 欠落は必ずヘッダーで集計する（課題ツリーの実装に倣う）
- 見送り配下の扱い（D17・D18）

**M22 での決着（実装からの言語化。U2・U4 と同じ形）:** 求められていた「1枚の文書」は
**[`missing-semantics.md`](missing-semantics.md) として実在する**——M22 が作った。
rev.3 の方針（**ゼロから設計せず、課題ツリーを参照実装として言語化する**）どおりに
作られており、上の4項はすべて規約として入っている（**輪郭で表す＝規約3**「見せ方は
rev 9章の欠落軸に従う」〔ただし M21 の実機確認で淡い面を足す方向に改訂され、
テーブルのセルは M25 に輪郭も外した〕／**空セルに文字列を埋めない＝規約2**「空は空のまま
描く」／**ヘッダーでの集計＝規約4**「必ずヘッダで集計する」／**見送り配下の別枠は
規約4 の中の小節**「見送りの別枠（M25。UI ノート D17・D18）」——独立した規約ではなく、
集計の規約の下に置かれている。別枠の側は M25 が実装した）。
**判定源は `src/core/reading-guide.md` と一対一**で、
「どのデータが欠落か」の定義がアプリと配布物で二重化しない構造になっている。
**この節は以後 `missing-semantics.md` の入口としてだけ読むこと**——規約の本文は
そちらが持ち、変更もそちらへ入れる。

### U4. 破線エッジの意味 ← M25 で決着

課題ツリーの `需要がない` — `認知されてない` 間の破線が、
親子関係なのか別種の参照なのかが不明。

**D18（見送り配下を祖先から導出する）は親子関係の定義に依存するため、
D17・D18の実装前に確定が必要。**

**M25 での決着（実装からの言語化）:** 課題ツリーのエッジは**すべて親子関係**である。
別種の参照を表すエッジは存在しない。破線は関係の**種類**ではなく抑制の**状態**を
運ぶ——「祖先の見送りで抑制された配下へ入る線」だけが破線になり（色・濃さは
`stroke-rule` のまま。線の切れ方だけで区別する）、**見送りを掲げた当の箱へ入る線は
実線のまま**である（見送りは判断の表明であって「もう見なくてよい枝」ではない）。
正は `src/modules/issue-tree/IssueTreeEdges.tsx`（`SUPPRESSED_DASH` とその JSDoc。
「自分自身を含む集合を渡さないこと」という M3 の退行の記録ごと）。
心配していた「親子と参照が同じ視覚チャネルで混同される」は、参照が存在しない以上、
起きない。

---

## 8. 実施優先順位

| 順 | 内容 | 対応D | 根拠 |
|---|---|---|---|
| 1 | **支持／棄却の識別性を回復** | D15 | **美観ではなく「ツリーが読めるか」の問題**。判断結果が拾えないなら主要情報が失われている |
| 2 | 数字の等幅化＋数値列の右揃え | D9 | 1行で効く。コスト比最大 |
| 3 | 未定義表現の統一 | D1, D14 | **facetの機能そのものの修復**。見た目改善より価値が高い |
| 4 | 色の役割整理（緑帯廃止・分類の脱色・ボタン脱色） | D2, D16, D19 | 既存トークンの書き換え中心で着手できる |
| 5 | タイポグラフィスケールの適用 | D11, D12, D13 | 基準値16px化・行高の是正。フォント差し替え前でも単独で効果あり |
| 6 | ノード幅・行高の固定＋省略 | D3 | バッジが縦一列に揃い、スキャン可能になる |
| 7 | 見送り配下の集計分離 | D17, D18 | U4（破線の意味）確定後 |
| 8 | フォント差し替え | D6〜D8 | U1確定後。5の完了後、階層表現をウェイトに移譲する |
| 9 | 警告メッセージの改善 | D4, D5 | |
| 10 | ネイティブselect置換・角丸統一 | §1.1 | |

**実施状況（2026-08-26 時点）:** 優先 1〜6・9 は消化済み（**1・2・4 ＝ M21**——D15 の判断軸の配色、D9 の等幅数字（`@layer base` の `tabular-nums`）と No 列の右揃え、D2・D16・D19 の色の役割整理。**3・9 ＝ M22**——D1・D14 の未定義表現の単一化と、D4 の「件数＋行番号」形式。**5 ＝ M23**、**6 ＝ M24**）。**なお D5「警告は問題のあるセルだけを染める」は M21 が片付けている**——シーケンスの行全体を染める帯を廃し、セルの状態を面から輪郭へ移した。**優先6 は幅・高さとも閉じている**——ノード幅の固定は M24 で実施し、**ノードの高さは 2026-08-26 の裁定で「内容が決めるまま」を確定設計とした**（D3 の M26 追記節）。テーブルの行高固定＋2行省略は M24 で「変えない」と別途決着している（同 D3）。**優先7・10 は M25 で消化した**（D17 の別枠は件数のみ・D18 は文書固定・select 置換・
角丸統一。U4 も M25 で決着）。**優先8（D6〜D8 ＝ フォント差し替え）は M26 で消化した**
——IBM Plex 3書体を woff2 のみの生成 CSS で同梱し、D8 の3層は「階層をウェイトに移さない」
という形で決着した（D6・D7・D8 の M26 の追記節）。**あわせて U2・U3 も、実装が既に
答えを持っていたことを確認して書き起こした**（U2 ＝ M21・U3 ＝ M22。U4 を M25 で
閉じたのと同じ形）。**優先6 の高さは 2026-08-26 の裁定で打ち切り（現状確定）。
実施優先順位の全項が閉じた。**

**着手条件:**

| 作業 | 前提 |
|---|---|
| 優先3・4 | **U3**（未定義の共通規約）の確定——M22 で確定済み（`missing-semantics.md`） |
| 優先1・4 | **U2**（グレー階調と各色の具体値）の確定——M21 で確定済み（`palette.css`） |
| 優先7 | **U4**（破線エッジの意味）の確定——M25 で確定済み |
| 優先8 | **U1**（フォントセット選定）の確定——2026-08-26 に決着済み（案A）。実装は M26 |

**U1〜U4 はすべて閉じた。** 着手条件として開いている未決事項はもう無い。

**注記:**

- 優先1（D15）を先頭に置いたのは、これが装飾ではなく**情報の可読性**の問題であるため。
  支持と棄却が判別できないなら、課題ツリーの主要な情報が読めていないことになる。
- 優先5（スケール適用）はフォント差し替えより前に置いている。
  D11のサイズ是正はフォントに依存せず単独で効果があり、
  かつU1が未決のままでも着手できるため。

---

## 9. スコープ外（今回は扱わない）

- ~~ダークモードの配色設計~~ ← M27 で D20 に昇格し決着（実機確認のみ残る）
- レスポンシブ対応（デスクトップ専用アプリのため）
- アニメーション・トランジション設計
- アイコンセットの選定・統一
- ズーム機能の実装（D13参照。現時点ではYAGNIとして見送り）

---

## 参考にした一次情報

- デジタル庁デザインシステムβ版 / タイポグラフィ
  https://design.digital.go.jp/dads/foundations/typography/
- SmartHR Design System / デザイントークン / タイポグラフィ
  https://smarthr.design/products/design-tokens/typography/
- Fluent 2 Design System / Typography
  https://fluent2.microsoft.design/typography

# 開いている残件

> **生きた文書。編集して増減させる。** 解消したものは**消す**（消したこと自体は、そのマイルストーンの [`history/`](history/) に残る）。
>
> ここに載るのは「**いつでもよいが、忘れると実害化するもの**」。いま着手すべきものはマイルストーンの計画に入れる。マイルストーン完了時にこのファイルを更新するのは義務（[`../CLAUDE.md`](../CLAUDE.md) 参照）。
>
> 最終更新: issue-tree-m5（仮説の展開 UI——展開の単位を課題ノードへ引き上げ、仮説側のキー処理を全廃してマウスの動線に置き換え、展開パネルを「ソリューション仮説／価値仮説／検証結果／以前の判断／FB」に組み直し、聞きたいこと `asks` と FB `feedbacks` を問いごとのブロックに入れ子で描き、判断バッジ自体をトリガーにした）完了時点（2026-08-30）。**消したのは5件（m5 が解消したもの）＋2件（m4 由来の誤記。下の別区分）・足したのは10件・書き換えたのは5件**（うち4件は残件の箇条、1件は「次に手を付ける候補」9番目）——「アクセシビリティ」の**「畳まれた仮説の行に入ると1回の `Tab` / `Shift+Tab` でフォーカスが2回動く」**（`[issue-tree-m3]`）は `onFocus={props.onExpand}` が消えたため**丸ごと消した**。「挙動の穴」の**「FB待ちのチップの数と巡回列の長さが一致しない」**（`[issue-tree-m4]`。決定Bの既知の非対称）は、問いごとに固有のセルを新設し `tallyQuestions` の `feedback` と行き先の数が恒等になることをテストで固定したため**丸ごと消した**。「将来の機能を作った瞬間に踏むもの」の**「`detail` / `value` / `asks` の編集コマンドが無い」**（`[issue-tree-m4]`）は `setHypothesisDetail` / `setHypothesisValue` / `addAsk` / `setAskText` / `removeAsk` を足したため**丸ごと消した**。「テストが無い箇所」の**「FB セルで主修飾キー＋`Enter` がキーを消費しないテストが無い」**（`[issue-tree-m4]`）は、仮説側のキー処理そのものを全廃したため**守るべき性質自体が無くなり丸ごと消した**（テストが無いのではなく、テストすべき性質が無くなった）。「将来の機能を作った瞬間に踏むもの」の**「`resolved` を新規に付ける動線が画面に無い」**（`[issue-tree-m4]`）は、**旗の無い箱に「見送り」「解決」の2つのボタンを並べて `resolved` を新規に立てられるようにしたため丸ごと消した**（issue-tree-m5 の追加作業。**当初はこの項を「m4 も m5 も実装せず担当が未定」という実態へ書き換えていた**が、依頼者の指示で m5 の中で作ったので項ごと消えた——`docs/history/issue-tree-m5-hypothesis-ui.md` の「m4 との境界」の2件目を参照）。**書き換えたのは5件**（うち4件は残件の箇条、1件は「次に手を付ける候補」9番目。いずれも項目自体は消していない）——(1)「課題ツリーの UI の `onCloseAutoFocus` の抑止にテストが無い」を、jsdom では `onSelect` が先に `onOpenChange(false)` を呼ぶため Radix の focus 復帰が走らず**原理的に守れない。実機でしか守れない**という理由の明記へ書き換えた。(2)「課題ツリーの構造編集に、直接のテストを持たない関数が残っている」から、issue-tree-m4 までに消えていた5関数（`setPendingNote` / `removePendingNote` / `setHypothesisText` / `setRationale` / `promoteNote`）の記述を落とし、いま実在する4関数だけの記述に書き換えた（実物と突き合わせて発見した幽霊）。(3)「課題ノードの見送りにキーボード経路が無い」から「主修飾キー＋`Enter` は仮説の追加に割り当てたのでキーが空いていない」という前提を落とし、**issue-tree-m5 Task 3 でその写像が消え、主修飾キー＋`Enter` は未割り当てになった（キーは空いている。ただし割り当てるかは決めない）**という実態へ書き換えた。(4)「仮説を別の課題へ付け替える手段が無い」を、**述べているのは `commands.ts` の関数の制約であって画面からできることの範囲ではない**（`moveHypothesis` 自体がどの動線からも呼ばれていないので、同じ課題の中ですら並び替えられない）と読めるように書き換えた。(5)「次に手を付ける候補」9番目（issue-tree-m4 の実機確認が未実施）を**「issue-tree-m4・issue-tree-m5（同じ画面なので一緒に見る）」**へ改め、展開パネルの節構成・判断バッジのトリガー化・仮説の追加削除のマウス動線など m5 の変更点も確認項目に足した。**加えて、`[issue-tree-m4]` 由来の誤記だった2件を消した——これは上の5件とは別の区分である**（「m5 が解消したから消した」のではなく「**書かれていた内容が実物と合っていなかった**」）。(a)「`src/modules/issue-tree/IssueTreeEditor.tsx` の `FLAG_KINDS` がコンポーネント本体の中にあり、毎レンダ作り直される」は**書かれた時点から偽**だった——`git log -S"const FLAG_KINDS"` が返すのは m4 の機能コミット `6d05014` 1本だけで、その時点から `FLAG_KINDS` はモジュール直下にあり、JSDoc も「データにも props にも依存しないのでモジュール直下に置く（毎レンダ作り直さない）」と書いている（文書コミット `ce09bf1` が実物と突き合わせずに書いた）。(b)「『見送りと解決の2つのチップが同時に並ぶ』を見るテストが無い」は `ce09bf1` の時点では真だったが、**m4 の最終修正 `8d6607e` がそのテストを実装している**（`IssueTreeEditor.dom.test.tsx` の「帯には見送りと解決が別々のチップとして並ぶ（両方同時に立ち、0件のほうは描かれない）」が、課題1に `deferred`・課題2に `resolved` を同時に立てて両方のチップを見る。`git log -S"bothFlagged"` が `8d6607e` を返す）——**項の方だけが消し忘れられた。** **この2件は「解消したから消す」列に混ぜないこと**——次の数え直しが滑る。**足したのは `[issue-tree-m5]` タグの10件**——計画が挙げた3件（展開中のノードが列全体を押し広げるため横スクロールが増えること／仮説の並び替えの手段が無いこと／「どの問いにも紐づかない FB」ブロックの固定文が編集できないこと）と、レビューが「繰り延べ Minor」として記録した7件（アクセシブル名の動詞の不揃い／`.add` の2ファイル複製／別種チップの起点が問いの欄でだけ先頭へ落ちること／`ensureVisible` / `panIntoView` のテストが無いこと／`solutionLabelH` の分岐が現在の書体では選ばれないこと／判断バッジのキーボード経路に DOM テストが無いこと／仮説0本の課題に末尾の追加ボタンが出ないこと）。詳細は [`history/issue-tree-m5-hypothesis-ui.md`](history/issue-tree-m5-hypothesis-ui.md) を参照。その前は issue-tree-m4（スキーマ v3——仮説の文言を `title` / `detail` / `value` に割り、聞きたいこと `asks` と属性つき FB `feedbacks` を新設し、課題に「解決」の旗 `resolved` と表示用の `date` を足した）完了時点（2026-08-30）。**消したのは3件・足したのは11件・書き換えたのは0件**——「テストが無い箇所」の**「課題ツリーの UI の reveal 機構と `onCloseAutoFocus` の抑止にテストが無い」**（`[issue-tree-m1]`）は、「根拠へ」ボタンごと reveal 機構が廃止されたため**reveal に関する記述だけを削り**、`onCloseAutoFocus` の抑止に関する部分（判断のドロップダウンが対象）は残した。その入れ子だった「同じ reveal 機構に `Shift+Tab` では届かないという実際の穴がある」（`[issue-tree-m1]`）は、reveal 機構そのものが無くなったので**丸ごと消した**。「小さな負債」の**「`setHypothesisText` / `setRationale` に戻り値型注釈が無い」**（`[issue-tree-m1]`）は、`setRationale` が消え `setHypothesisTitle`（旧 `setHypothesisText`）には最初から `: IssueTreeSchemaVersion3` の注釈があるため**丸ごと消した**。**足したのは `[issue-tree-m4]` タグの11件**——「挙動の穴」へ3件（存在しない `ask` を指す FB を整合性検証が見ていないこと／FB待ちのチップの数と巡回列の長さが一致しないこと〔決定Bの既知の非対称〕／`date` が「いつ言われたか」を保証しないこと）、「将来の機能を作った瞬間に踏むもの」へ2件（`resolved` を新規に付ける動線が画面に無いこと／`detail` / `value` / `asks` の編集コマンドが無いこと。いずれも m5 の担当として意図的に残した）、「テストが無い箇所」へ4件（FB セルで主修飾キー＋`Enter` がキーを消費しないテストが無いこと／見送りと解決の2チップが同時に並ぶテストが無いこと／`load.test.ts` の「旧版→editable」の `it` が空配列フィクスチャのままなこと／`today.test.ts` の「引数省略」の `it` が理論上フレークしうること）、「小さな負債」へ2件（`derive.ts` の `suppressedIssueIds` の JSDoc に v2 の前提〔旗は見送りしか無い〕が残っていること／`IssueTreeEditor.tsx` の `FLAG_KINDS` がコンポーネント本体の中で毎レンダ作り直されること）。あわせて「次に手を付ける候補」へ **issue-tree-m4 の実機確認が未実施であること**を9件目として足した（サブエージェントは Tauri の GUI を操作できない。[`history/issue-tree-m4-schema-v3.md`](history/issue-tree-m4-schema-v3.md) にチェックリストを**空のまま**写してある）。詳細は同 history を参照。その前は m28（Claude Code ペインへのファイル受け渡しと右クリック）完了時点（2026-08-29）。**消したのは0件・足したのは5件・書き換えたのは0件**——「挙動の穴」へ2件（`readClipboardText` が「クリップボードが空」と「読み取りに失敗した」を区別せず空文字に潰すため、`clipboard-manager:allow-read-text` が外れると右クリック貼り付けが無反応になること／起動時の差し込みが bracketed paste mode（`term.modes.bracketedPasteMode`）の到来待ちで5秒の上限を持ち、`claude` の起動が遅い環境では末尾のスペースが食われる症状に戻りうること）、「テストが無い箇所」へ2件（エクスプローラからのドロップにテストが1本も無いこと／右クリックで OS の既定メニューが実際に消えることを守っているのは実機確認だけであること）、「小さな負債」へ1件（端末の配色が `palette.css` の `.dark` が素のクラスセレクタであることに依存していること）を、いずれも `[M28]` タグで足した（詳細は [`history/m28-core-claude-pane-handoff.md`](history/m28-core-claude-pane-handoff.md)）。**その後、Task 10（実機確認と文書更新）で1件を書き換えた**——「起動時の差し込みが `ESC[?2004h` の到来に依存している」を、実際の最終決着（bracketed paste mode——xterm が解析した `term.modes.bracketedPasteMode`——が有効になった**かつ**出力の500ms静穏を待つ二条件、上限は8秒）に合わせて書き直し、あわせて `INSERTION_QUIET_MS`（500ms）が実機の1環境でしか確かめられていないことを添えた。**これで m28 完了時点は消したのは0件・足したのは5件・書き換えたのは1件になる。** **さらにその後、最終レビューの Minor 潰しで、その書き換え自体が引きずっていた誤り（`ESC[?2004h` というバイト列表記のまま——実装が見ているのは xterm が解析した `bracketedPasteMode` であってバイト列そのものではない）を本文から直した（項の増減は無い）。** その前は M29（表形式でコピー——TSV と HTML を同時に載せ、出力を画面の絞り込みに揃える）完了時点（2026-08-29）。**消したのは0件・足したのは5件・書き換えたのは0件**——「小さな負債」へ**`（未定義）` の文言が4箇所（用語集・エラーカタログ・ロジックツリーの Markdown 出力それぞれのローカル定数と、`core/table-export.ts` の `UNDEFINED_TEXT`）で複製されていること**、「挙動の穴」へ**エラーカタログの読み手（プロファイル）が表示プロファイル・`ExportMenu`・表形式コピーのダイアログの3箇所で別々に選ばれること**と**循環で根から到達できないノードが表出力からも警告なく落ちること**（Miro 出力・Markdown 出力と同じ穴を表にも揃えた）、「テストが無い箇所」へ**TSV の `"` 囲みが Excel で1セルに収まるかは実機でしか確かめられないこと**の4件を、いずれも `[m29]` タグで足した。あわせて「次に手を付ける候補」へ **M29 の実機確認が未実施であること**を8件目として足した（サブエージェントは Tauri の GUI を操作できない。[`history/m29-core-table-copy.md`](history/m29-core-table-copy.md) にチェックリストを**空のまま**写してある）。詳細は同 history を参照。**さらに M29 の最終ブランチレビュー（Critical 0・Important 0・マージ可と判定）の fix wave で、「挙動の穴」へ `[m29]` タグの1件を足した**——**表に貼ると `=`／`+`／`-`／`@` で始まるセルが数式として実行されうること**（CSV/TSV インジェクション。意図して直さず記録した。詳細は同項）。これで M29 が足したのは合計6件になる。 その前は logic-tree-m3（Miro のマインドマップとのクリップボード交換）完了時点（2026-08-29）。**消したのは0件・足したのは4件・書き換えたのは0件**——「小さな負債」へ**Miro のクリップボード形式が非公開で、Miro 側の変更で壊れうること**と**`sequence/mermaid.ts` の `escapeMermaidLabel` の re-export を参照している本番コードが無いこと**の2件、「挙動の穴」へ**Miro 書き出しのノード幅が概算でありフォントが変われば折り返すこと**と**循環で根から到達できないノードが Miro 出力・Markdown 出力の両方から警告なく落ちること**（参照切れは `buildTree` がルートへ繰り上げるため落ちない）の2件を、いずれも `[logic-tree-m3]` タグで足した（詳細は [`history/logic-tree-m3-miro-clipboard.md`](history/logic-tree-m3-miro-clipboard.md)）。その前は logic-tree-m2（ロジックツリー登録 Skill と `flat-tree` の分割）完了時点（2026-08-29）。**消したのは1件・足したのは5件・書き換えたのは3件。** **消したのは**「テストが無い箇所」の**`canonical.ts` のバイト一致コピーに「網羅」のアサーションが無い**（`[issue-tree-m2]`）——`src/core/skill-canonical-copy.test.ts` に `CANONICAL_COPIES`（**手書きの配列リテラル**）と `BUNDLED_SKILLS` の `toEqual` を置いて解消した（`map` で導出すると恒真式になるので導出していない。rev 4章）。**足したのは**、「次に手を付ける候補」へ **logic-tree-m2 の実機確認が未実施**であることを7件目として（サブエージェントは Tauri の GUI を操作できない。[`history/logic-tree-m2-register-skill.md`](history/logic-tree-m2-register-skill.md) にチェックリストを**空のまま**写してある）と、「テストが無い箇所」へ4件——**`logic-tree-register` の evals を実行ハーネスに掛けていない**（定義と判定器はあるが起動精度の実測がまだ無い）／**`.gitattributes` の警告が「整合性の警告」の見出しの下に出る**（見出しが嘘になっており、かつ未テスト）／**smoke テストが子プロセスの stderr を継承している**（緑の実行が壊れて見える）／**exit 2 の経路とスキーマの解決順が未テスト**（いずれも `[logic-tree-m2]`）。**書き換えたのは3件**（消していない）——「登録**4** Skill は整合性検証の警告文言・計上規則を…複製している」を**5本**にし、**ロジックツリーだけは判定そのものを複製していない**（3ルールが `flat-tree-core.ts` のバイト一致コピーの戻り値。複製しているのは文言と集計行だけ）ことを添えた／「`palette-fit.mjs` が Node の型ストリップに依存している」の列挙に `logic-tree-register`（`flat-tree-core.ts` / `canonical.ts`）と一致検査の所在を足した／「`src/core/reading-guide.md` の Skill 名一覧を縛るテストが無い」の「**5本目**を足したとき」を**6本目**へ改めた（**5本目＝logic-tree-m2 はこの穴を素通りできたが、更新自体は行われた**——守ったのは人の記憶であってテストではない、という状態は変わっていないので消さない）。その前は sequence-m5（問いの文言・アクター語彙・レールの幅）完了時点（2026-08-27）。**消したのは0件・足したのは1件・書き換えたのは0件**——「挙動の穴」へ**reply 行の一般文言の高さが行高に入らないこと**を（`[sequence-m5]`）。**この回で作った穴ではない**（`SequenceEditor.tsx` のコメントが以前から明記していた）が、問いの文言を伸ばしたことで一般文言も伸び、実際に折り返して次の行へ食い込むところまで来たため、文言を短縮して回避したうえで台帳へ立てた（詳細は [`history/sequence-m5-question-labels-and-actor-vocabulary.md`](history/sequence-m5-question-labels-and-actor-vocabulary.md)）。**なお「参加者」→「アクター」の置換はこの台帳の本文にも及んでいる**——語彙を1つにするのが目的なので、`docs/history`・`docs/superpowers`・`lessons-for-planning`（いずれも不変の記録）以外はすべて新語で読む。その前は M27（ダークパレットの吟味）完了時点（2026-08-26）。**消したのは0件・足したのは0件・書き換えたのは1件**——「デザイン」の**「ダークの値は要件を満たすだけで吟味していない」**（`[M21]`）を、机上の吟味が済んだことを踏まえ**「ダークの吟味値（M27）は実機確認待ち」**（`[M27]`）へ書き直した（詳細は [`history/m27-core-dark-palette.md`](history/m27-core-dark-palette.md)）。**その後、同日にマージ前の実機確認の一巡目が入り**、無彩色の面の序列を作り直し（`surface` 0.24／`surface-muted` 0.13——凍結を示す見送りの箱が3面で最も浮いて見えた反転の解消——／`grid` 0.20）、**`rule-muted` を19個目のトークンとして分離した**（表の罫線・弱い境界。ダークでは方眼と罫線が1値で両立しないため。同 history の「実機確認の一巡目」の節）。この一巡で項の増減は無く、書き換えたのは上の実機確認待ちの項1件（済んだ分を畳み、残りを意味色系の項目に絞った）。その前は M26（フォント同梱——IBM Plex 3書体を woff2 のみの生成 CSS で同梱し、UI ノート E ＝ D6〜D8 を閉じる）完了時点（2026-08-26）。**消したのは1件**——「デザイン」の**「UI ノートの E（フォント同梱）が未着手」**（`[M21]`。D6＝同梱・D7＝混植・D8＝ウェイト階層をすべて M26 が実施し、**D8 は「カラム名の階層をウェイトに移さない」という形で決着した**——移す案は M21 実機確認の「面なしでは見出しの行がデータ行に溶ける」と衝突する。消す前に、この項が唯一の置き場を兼ねていた「E が入ったらカラム名は階層をウェイトに移せる」という予約が UI ノート D8 の M26 の決着の節と rev 9章のウェイトの項の両方に書き起こされていることを確認した——U4 を M25 で消したときと同じ手順）。**足したのは2件**——「次に手を付ける候補」へ **M26 の実機確認が未実施であること**を7件目として（見た目が成果物のマイルストーンで、M21〜M25 と同じ扱い。[`history/m26-core-font-bundle.md`](history/m26-core-font-bundle.md) にチェックリストを**空のまま**写してある。**ただし M26 は「未検証のまま送る」と計画が明記した前提①②のブラウザ実測も未了**なので、チェックリストの先頭に項目0 として足してある——claude-in-chrome の拡張が接続できず自動実測ができなかった）と、「小さな負債」へ **配布物にサードパーティのライセンス表記が同梱されていないこと**を（`[M26]`。**M26 が作った欠陥ではなく、M7 でラテン書体をバンドルして以来同じ形で存在していたものが、書体の差し替えで目に入った**）。**書き換えたのは1件**（消していない）——「デザイン」の**D3 の高さ（省略）の項**に、**これが「UI ノートで残る唯一の1件」になったことと、決着を M26 の中で人間に諮ること**を書き足した。**その後、同日に人間が D3 の高さの裁定を下し**（打ち切り——「ノードの高さに上限は無い——内容が決める」を確定設計にする）、**上で書き換えたばかりの D3 の高さの項を消した**（これで**消したのは合計2件**になる。**「テーブルの行高固定は M24 で変えないと決めた」の別項は残る**——それは既決の記録として生きている。裁定の詳細は [`history/m26-core-font-bundle.md`](history/m26-core-font-bundle.md)）。**この裁定で UI ノートの実施項目は全部閉じた。** **なお U2・U3 も M26 で UI ノートに決着として書き起こしたが、この台帳の項ではない**（U2 ＝ M21 の役割トークン v2、U3 ＝ M22 の `missing-semantics.md` が既に答えを持っており、書き起こしていなかっただけ。詳細は [`history/m26-core-font-bundle.md`](history/m26-core-font-bundle.md)）。 **さらに同日、人間がマージ前に画面を見て計画外修正が1件入り**（**入力値だけを一段下げて 14px にした**——IBM Plex は仮想ボディに対する字面が Yu Gothic UI より大きく、`font-size` を変えていないのに体感サイズが上がったため。UI ノート D7 が「フォント差し替えだけで体感サイズが上がる」と予告していたことの逆向きの帰結である。カラム名・ボタン・バッジ・帯・検索欄・ファイル一覧・タイトル 22px は 16px のまま据え置き、14px は D11 の下限そのものなので下限は割っていない。反映先は UI ノート D11 の「M26 での部分反転」小節と rev 9章の段・方眼・セクション見出しの項。**方眼のマス目 24px と複数行段の行高の一致は失われた**が、人間が実機を見て `--grid-size` は動かさないと判定した）、**続いて人間が実機確認を一巡してチェックリスト12項目すべて問題なしと判定したため、上で足したばかりの「次に手を付ける候補」7件目を消した**（これで**足したのは差し引き1件**——「小さな負債」のライセンス表記だけが残る。**消したのは合計3件**になる。ただし**前提①②は probe ページの数値ログではなく実機の見た目での確認**であり、数値の記録は残っていない——その正直な但し書きは [`history/m26-core-font-bundle.md`](history/m26-core-font-bundle.md) の「実機確認の結果（追記）」にある。前提④＝端末の Plex Mono は問題なし）。 その前は M25（UI ノート F の決着——見送りの別枠集計・⚠ のアイコン化・select 置換・角丸統一）完了時点（2026-08-25）。**消したのは1件**——「デザイン」の**「UI ノートの F（見送り集計の2段構え・select 置換・角丸統一）が未着手」**（`[M21]`。消す前に、U4（破線エッジの意味）の答えが UI ノート側（U4「M25 での決着」の節）に書かれていることを確認した——この項は U4 の答えの唯一の置き場を兼ねていたため）。**足したのは1件**——「次に手を付ける候補」へ **M25 の実機確認が未実施であること**を7件目として（見た目そのものが成果物のマイルストーン。issue-tree-m3・M21〜M24 と同じ扱いで、[`history/m25-core-ui-note-f.md`](history/m25-core-ui-note-f.md) に設計スペック「検証」11項目のチェックリストを空のまま写してある）。**書き換えたものは無い。** **その後、同日にマージ前の一見で計画外修正が2件入り**（シーケンスの集計の帯をガター上部からツールバー行へ／見送り配下の箱も `surface-muted` で塗る——どちらも [`history/m25-core-ui-note-f.md`](history/m25-core-ui-note-f.md) の「実機確認の先行所見」の節）、チェックリストは項目12・13 を足して13項目になった。**さらに同日、人間が実機確認を一巡し、13項目すべて問題なしと判定されたため、上で足した候補7件目を消した**（結果は同 history の「実機確認の結果」の節。この削除以外に項の増減はない）。 その前は M24（ツリーのノード幅固定——課題ツリー・ロジックツリーとも全ノードを 320px に）完了時点（2026-08-25）。**同日の実機確認で「高さを3行で打ち切る」を撤回したため、下の記述はその撤回を織り込んだもの**である（`textarea` はキャレットに追従せず、打ち切ると4行目以降が編集で到達不能になる。[`history/m24-core-node-width-lock.md`](history/m24-core-node-width-lock.md) の「実機確認の所見」）。**書き換えは D の項を1件から3件に割ったこと**（幅は閉じた／高さの打ち切りは撤回して開いている／テーブルの行高は「変えない」と決着）**の1箇所。** **さらに同日、人間が実機確認を一巡し、撤回の引き金になった項目7 を除く全項目が問題なしと判定されたため、「次に手を付ける候補」へ足すつもりだった M24 の実機確認の項は起こさずに済んだ**（結果は [`history/m24-core-node-width-lock.md`](history/m24-core-node-width-lock.md) の「実機確認の結果」の節）。**消したのは1件**——「デザイン」の**「ドロップダウンのメニュー項目が 14px のまま据え置きである」**（`[M23]`。M23 決定8 が「沈んで見えたら `text-base` へ上げる。判断は実機で下す」として台帳に預けていたものだが、**M23 の実機確認の項目10 が問題なしと判定され、据え置きが判断として確定した**ため。根拠は [`history/m23-core-typography.md`](history/m23-core-typography.md) の「実機確認の結果」の節）。**書き換えたのは1件**（消していない）——「デザイン」の**「UI ノートの D（レイアウト固定）が未着手」**（`[M21]`）を、**ノード幅の固定は M24 で実施し、テーブルの行高固定＋2行省略は M24 で「変えない」と決着した**という現在の姿へ書き直し、タグを `[M24]` に移した。**D3 の半分を実施せずに決着させたという事実そのものが台帳に要る**ので消していない——消すと、次の計画者が「まだやっていないだけ」と読む。**足したのは1件**——「次に手を付ける候補」へ **M24 の実機確認が未実施であること**を7件目として（見た目そのものが成果物のマイルストーン。issue-tree-m3・M21・M22・M23 と同じ扱いで、[`history/m24-core-node-width-lock.md`](history/m24-core-node-width-lock.md) に設計スペック「検証」11項目のチェックリストを空のまま写してある。**とくに項目7——`overflow: hidden` の `textarea` がキャレットに追従して内部スクロールするか——は未検証の前提の上に「省略記号を出さず打ち切るだけ」の設計判断が乗っているので、先に見ること**）。**なお M24 の計画には旧定数名の残存箇所の数え違いが2件あった**（Task 4「参照は3ファイルだけ」・Task 5「grep で残るのは `wrap.test.ts` の1件だけ」）**が、どちらも台帳の項ではないので、[`history/m24-core-node-width-lock.md`](history/m24-core-node-width-lock.md) の側に記録した。** **UI ノート §8 の実施状況の行を書くときに帰属を突き合わせ、計画が想定していた `1・3・4 ＝ M21・M22 ／ 2・9 ＝ M22` が誤りであることも判明した**（正しくは **1・2・4 ＝ M21 ／ 3・9 ＝ M22**。詳細は同申し送り）。その前は M23（タイポグラフィスケール v2——3サイズ4段。密度は行高で稼ぎ、サイズでは稼がない）完了時点（2026-08-25）。**その後、同日に人間が実機確認を一巡し、チェックリスト11項目すべて問題なしだったため、「次に手を付ける候補」の7件目（M23 の実機確認が未実施）を消した**（結果は [`history/m23-core-typography.md`](history/m23-core-typography.md) の追記節。同じ一巡で出た計画外の2件——見送りトグルの幾何・グレースロットの削除ボタンの縦位置——は同 history の「実機確認の先行所見」の節のとおり修正済みで、この削除以外に項の増減はない）。**消したのは2件**——「デザイン」の**「UI ノートの B（タイポグラフィ）が未着手」**（`[M21]`。B ＝ D11・D12・D13 を本マイルストーンが実装し、段は `text-sm` / `text-base`（＋ `leading-normal`）/ `text-xl` の3サイズ4段、行間の既定は `@theme` の 1.3 / 1.25 / 1.3 になったため）と、「小さな負債」の**「行の高さをクラスで固定する書き方が2箇所ある」**（`[issue-tree-m1]`。**issue-tree-m1 が置いた予告「2件目が出たら書き方を決めること」が issue-tree-m3 で発火した状態のまま持ち越されていたのを、M23 の決定2・決定9 が機械検査として決着させた**——`src/styles/conventions.test.ts` の許可リスト検査が、明示してよい `leading-*` を `leading-none`（バッジ）と `leading-normal`（複数行の欄）の2つだけに絞り、`leading-5` のような数値指定と任意値 `leading-[...]` を弾く。**字面としての2箇所は残る**が——`src/components/badge-styles.ts:29` と `src/modules/issue-tree/HypothesisRow.tsx:83`、どちらも `text-xs leading-none` から `text-sm leading-none` になった——**「書き方を決める」という問い自体が閉じたので、台帳が管理する対象ではなくなった**）。**足したのは2件**——「デザイン」へ `[M23]` タグの1件（**ドロップダウンのメニュー項目が 14px のまま据え置き**。決定8。補助段の下限内なので規約違反ではなく、`EXCLUDED` により段の検査も届かないが、16px のセルの隣で沈むかは実機で判断する）と、「次に手を付ける候補」へ **M23 の実機確認が未実施であること**を7件目として（見た目そのものが成果物のマイルストーン。issue-tree-m3・M21・M22 と同じ扱いで、[`history/m23-core-typography.md`](history/m23-core-typography.md) に設計スペック「検証」11項目のチェックリストを空のまま写してある）。**書き換えたのは0件。** **なお設計スペックと計画は「`ActorRefCell` の『文字入りとの 2px 残差』の項を更新する」と指示していたが、その項はこの台帳に一度も存在しなかった**（`git log -S 残差 -- docs/open-issues.md` が0件）——2px の段差は [`history/m22-core-missing-semantics.md`](history/m22-core-missing-semantics.md) に書かれた**記録**で、台帳には上がっていない。M23 で `min-h-6`（24px）を `min-h-6.5`（26px ＝ `<button>` に index.css の `@layer base` が当てる `--tw-leading: 1.2` による行箱 16×1.2=19.2 ＋ `py-0.5` 4 ＋ 枠 2＝25.2 の切り上げ）にしたことで空名トリガーと文字入りの実高は計算上同値になったので、**新しく項を起こしてもいない**（見え方の確認は上の候補7のチェックリストに含めた）。その前は M22（欠落の規約——空は空のまま、数えて、行番号で指す）完了時点（2026-08-24）。**その後、同日に人間が実機確認を一巡し、チェックリスト9項目すべて問題なしだったため、「次に手を付ける候補」の7件目（M22 の実機確認が未実施）を消した**（結果は [`history/m22-core-missing-semantics.md`](history/m22-core-missing-semantics.md) の追記節。この削除以外に項の増減はない）。**消したのは2件**——「デザイン」の**「UI ノートの C（未定義表現の本体）が未着手」**（`[M21]`。M22 が捏造文字列の除去・件数集計・`docs/missing-semantics.md`（U3）をすべて実装したため）と、**「『回答済み』と『考慮不要』の区別が面の色ではなく文言に頼っている」**（`[sequence-m1]`。決定6で「決着は文字が運ぶ」——考慮不要は固定語 `考慮不要`、回答済は答えの本文——と定め、面ではなく文字で分ける設計判断が確定したため閉じた）。**書き換えたのは1件**（消していない）——「テストが無い箇所」の**「`invalid`（無効）の見せ方に DOM テストが無い」**（`[M21]`）に、M22 でロジックツリーの `NodeBox` とシーケンスのアクターヘッダ・参照ボタンへ「`invalid` が欠落に勝つ」DOM テストが新設された事実を追記し、**残る対象を `HypothesisRow.tsx` / `IssueBox.tsx` の2ファイルに絞った**。**足したのは `[M22]` タグの8件**——「挙動の穴」へ4件（バナー `IssueBanner` から該当行へのジャンプが無い／欠落ジャンプは4モジュールが巡回 ref でフォーカス位置を起点にせず、課題ツリーだけがフォーカス位置起点／ドロップダウンの「名前が空のアクター」が面だけの空チップ／エラーカタログの集計はプロファイル非依存だがジャンプは表示中のセルだけ）、「テストが無い箇所」へ1件（欠落の帯そのものに DOM テストが無い3モジュールがある——用語集・エラーカタログ・ロジックツリーの `*.dom.test.tsx` には `要対応` の文字列アサートもチップの押下テストも無い。シーケンス・課題ツリーにはある）、「アクセシビリティ」へ1件（シーケンスの答えスロットの `aria-label` に状態語が入らない——「考慮不要」の接頭は `aria-hidden`）、「小さな負債」へ2件（`schemas/sequence.schema.json` と Skill 側のバイト一致コピーの `failures` description が「キーの欠落＝未定義」のままで画面・Skill の語彙（未回答）とずれている／シーケンスの「未記入」判定 `=== ''` が `missing.ts`・アクターヘッダ・ラベルセル・`ActorRefCell` の4箇所に散在し一元化されていない）。**あわせて「次に手を付ける候補」へ M22 の実機確認が未実施であることを7件目として足した**——[`history/m22-core-missing-semantics.md`](history/m22-core-missing-semantics.md) に設計スペック「検証」9項目のチェックリストを空のまま写してある（issue-tree-m3・M21 と同じ扱い）。その前は M21（役割トークン v2——色を持つのは意味だけ）完了時点（2026-08-23）。**その後、同日の実機確認を受けて追記した**——人間が一巡し、「1px の輪郭だけでは方眼と罫線に埋もれて拾えない」として**欠落・無効・着信に淡い面を足した**（トークン 15 → 18。規約2の改訂と、issue-tree-m3 の「未決を面で塗らない」の反転を含む。rev 9章と [`history/m21-core-design-tokens-v2.md`](history/m21-core-design-tokens-v2.md) の追記節）。**この追記で消した項・足した項は無く、書き換えたのは2件**——候補6（実機確認未実施）に一巡の結果を書き足し、「端末の ANSI 16色は xterm の既定のまま」の役割トークンの個数を 15 → 18 にした。以下は M21 完了時点の記述である: **消したのは6件**——「デザイン」から5件と「テストが無い箇所」から1件。デザインの5件は**M21 が体系ごと作り直したことで前提が消えた**もので、(1)**「`warning` と `ok` が P型・D型色覚で識別できない」**（`[M7]`）——意味色4つの総当たり×3色覚を `DISTINCT_PAIRS` が測るようになり、**印字するだけだった数字が門番になった**、(2)**「役割トークンに透過を掛けた箇所は `palette.css` のコントラスト保証の外に出る」**（`[M13]`。入れ子の「面の透過には既に登録簿があり、文字の透過にだけ無い」ごと）——**透過を全面禁止にした**（`OVERLAYS` は無く、`conventions.test.ts` が役割トークンへの `/NN` を弾く）ので、「登録簿を文字側にも作る」という宿題ごと消えた、(3)**「`ok` がどのコンポーネントからも参照されていない」**（`[M7]`）——`ok` というトークン自体が無くなった、(4)**「行全体の指摘と `from`/`to` の指摘が同時に出ると `warning` の面が二重になりうる」**（`[sequence-m1]`）——シーケンスの行の帯を廃止し、`#N` の通し番号に無効を出す形にしたので、重なる面が存在しない、(5)**「`bg-surface/80` がコードベースに前例のない不透明度指定」**（`[M14]`）——`KeyHints` の帯を不透明の `bg-surface` にした。**ただし (3) の入れ子は消さずに独立した項へ格上げした**——「**『回答済み』と『考慮不要』の区別が面の色ではなく文言に頼っている**」は `ok` が無くなっても残る問題で（`judge-yes` の面を借りるのか、判断軸ではないので別のチャネルで分けるのかを決める必要がある）、タグは `[sequence-m1]` のまま「デザイン」に置いた。6件目は**「テストが無い箇所」の「`palette.test.ts` は『面と文字』の組は検証するが『面と枠』の組は検証していない」**（`[issue-tree-m3]`）で、**これは brief の指定外だが解消を確認して消した**——症状として名指ししていた `surface-accent` というトークンが無くなり、後継の `surface-muted` は `BACKGROUNDS`（`canvas` / `surface` / `surface-muted`）に入っているので、`rule` の 3:1 はこの面の上でも機械検査される。症状が出ていた `GlossaryEditor.tsx` / `ErrorCatalogEditor.tsx` の見出し行も**面を持たなくなった**（`grep -rn "surface-accent" src/` に残るのは、**旧名をクラスとして使っていないことを検査する `conventions.test.ts` の門番**と `palette-requirements.ts` のコメントの2ファイルだけで、クラス名としての使用は0件）。**足したのは `[M21]` タグの11件**——「デザイン」へ7件（**UI ノートの残り B〜F を1項ずつ5件**。B＝タイポグラフィ 16px 基準／C＝未定義表現の本体（捏造文字列・ロジックツリーの空ノード警告・件数集計・U3 の規約文書）／D＝レイアウト固定（行高固定＋省略・ノード幅固定）／E＝フォント同梱（U1 が未決）／F＝見送り集計の2段構え・select 置換・角丸統一。各項は [`facet-UI設計ノート.md`](facet-UI設計ノート.md) の D 番号を指す。加えて**ダークの値は要件を満たすだけで吟味していない**ことと、**`invalid` がライトで暗い赤（L 0.38）なのは D型で `missing` と分けるためである**こと——実機で「黒っぽい」と出たら `DISTINCT_MIN` を 0.08 に下げて L 0.42 前後へ戻す）、「テストが無い箇所」へ2件（**`invalid` の見せ方に DOM テストが無い**——`HypothesisRow` の輪郭と `IssueBox` の赤枠。**これは M21 が作った欠落ではなく issue-tree-m3 から続く既存の穴である**／**`<Button>` の variant を見る検査は属性値に `>` を含むと早期に閉じる**——現状該当は0件）、「小さな負債」へ2件（**用語集の別名バッジ（`AliasCell.tsx:231`）を `Badge` に寄せていない**——状態ではなくデータの値のタグなので意味の variant が無い／**表記ゆれの「指摘」の波線下線は規約に書いたが実装が無い**——suggestion の検出自体がまだ無い）。あわせて「次に手を付ける候補」へ**M21 の実機確認が未実施であること**を6件目として足した——**見た目そのものが成果物なので、確認しないと成否が分からない。** **これは人間が一巡したら消す項である**（issue-tree-m1・issue-tree-m2・issue-tree-m3・M17・M20 と同じ扱い）。**書き換えたのは3件**（消していない）——(1)「端末の ANSI 16色は xterm の既定のまま」の役割トークンの個数を 11 → 15 に、(2)「行の高さをクラスで固定する書き方が2箇所ある」の1つ目のパスを `src/modules/issue-tree/badge-styles.ts` の `BADGE_BASE` から `src/components/badge-styles.ts:29` の `base` へ（**M21 でバッジが共通部品になったため。2箇所であることは変わらない**）、(3)「`ink-faint` を本文に使っていないことを機械検査していない」の6箇所のパス・行番号を現行に（1つ目が共通部品へ移った）。その前は issue-tree-m3（課題ツリーの俯瞰 UI と語彙）完了時点（2026-08-23）: **消したのは4件**——「デザイン」の**「課題ツリーの UI の見た目が『かなり微妙』」**と**「仮説カードの幅が固定で、長い仮説は縦に伸びる」**、「挙動の穴」の**「見送りの理由（課題側イベントの `note`）をアプリから打てない」**（いずれも `[issue-tree-m1]`）。前2件は**仮説カードという物ごと無くなった**ため——俯瞰を「箱は課題だけ・仮説は箱の中の1行・判断は行末のバッジ1つ」に作り替えたので、`grep -rn "CARD_WIDTH" src/` も `grep -rn "HypothesisCard" src/` も0件である。**ただし「かなり微妙」を消したのは、その言葉を生んだ画面が丸ごと無くなったからであって、新しい画面が良いと確かめたからではない**（実機確認は未実施。「次に手を付ける候補」の5番目）——**一巡して同じ言葉がまた出たら、この項は書き直して戻すこと。** 3件目は `commands.ts` に `setDeferralNote` が入り、箱の中に理由の欄が出たので解消した。**4件目は brief の指定外**——「`IssueBoxProps.label` の doc コメントが `label` ではなく `text` を説明している」は、Task 4 が部品を書き直したときに `label` の doc が「アクセシブル名の接頭（`課題{N}`）」に直っており、解消していた。**書き換えたのは4件**（消していない）——(1)「`leading-5` がこのリポジトリ唯一の `leading-*`」を**「行の高さをクラスで固定する書き方が2箇所ある」**に直した。`leading-5` は `HypothesisCard` ごと消えたが、issue-tree-m3 が `leading-none` を2箇所に生やしたので、**`leading-*` は消えていない**——この項の趣旨は「行の高さをクラスで固定する書き方」の管理で、**「2件目が出たら書き方を決めること」という予告が実際に発火した**状態である。`leading-5` という個別の値の話として読んで消すと、条件が満たされた瞬間に台帳から消える。(2)「書き出しスクリプトが配列順を整えない」の中の「`migrate.ts` で恒等変換」が事実として古くなったので直した（1 → 2 の変換が入った。並びを触らないことは変わらない）。(3)「`layoutIssueTree` の doc に不変条件が書かれていない」——**症状が悪くなった**（食い違うと throw していたのが、いまは黙って違うバッジが立つ）。(4)「ID が重複しているファイルで、仮説カードは『最後に歩いた方』の課題の下にだけ置かれる」——仮説がカードから箱の中の行になり、**上書きではなく両方の箱に重複描画**に変わった。**足したのは `[issue-tree-m3]` タグの7件**——「テストが無い箇所」へ2件（`ink-faint` を本文に使っていないことを機械検査していない／`schemaVersion` の移行が**保存側**と**実モジュールを通す読み込み経路**の2つで未検査）、「挙動の穴」へ3件（判断の誤操作を塞いでいない／`listOpenTargets` が「図に描かれない課題」を行き先から外していない＝仮説側で塞いだ穴が課題側に開いたまま／`load.ts` が整数でない・1未満の `schemaVersion` を旧版として移行経路に入れる）、「アクセシビリティ」へ2件（畳まれた仮説の行で1回の `Tab` にフォーカスが2回動く／チップの `aria-label` が可視ラベルを含まない）。あわせて「次に手を付ける候補」へ**実機確認（計画 Task 10）が未実施であること**を5件目として足した——**このマイルストーンは見た目そのものが成果物なので、確認しないと成否が分からない種類である。** **これは人間が一巡したら消す項である**（issue-tree-m1・issue-tree-m2・M17・M20 と同じ扱い）。**さらに issue-tree-m3 の最終ブランチレビュー（Critical 0・マージ可と判定）の fix wave が走ったが、この台帳の項は1件も消していないし足してもいない**——直したのは申し送り [`history/issue-tree-m3-overview-ui.md`](history/issue-tree-m3-overview-ui.md) の「直さずに残したもの」に載っていた**台帳外の細部**で、(1) `IssueTreeEdges.tsx` の `suppressed` の doc と実装コメントが**直す前の意味**を説明していたこと（渡るのは自分自身を含まない `inheritedSuppressed` なのに「自分自身を含む」と書き、**このブランチが途中で実際に退行させた誤り**を積極的に正当化していた）、(2) 廃止語の後始末（`カード` 6箇所——識別子 `runCardCommand` / `onCardKeyDown` を含む——／「6種」→7種／合計の意味の `未決`）、(3) チップの面（保留＝実線・他3種＝破線）を見る DOM テストの新設、(4) `⚠` が帯と `tallyLine` の2箇所に直書きされている件を**テストで縛った**こと（字面は2箇所のまま）、(5) `measure.ts` の箱クラスの `px-*` / `py-*` が絶対配置の子には効かないことの明記、(6) 空行1行。**`[issue-tree-m3]` の7件はすべて開いたままである**——とくに **`listOpenTargets` が「図に描かれない課題」を行き先から外していない**件は、細部の掃除とは別種の判断（課題側の到達可能性を行き先の導出へ持ち込む）を要するので、**意図して繰り越した。** **なお issue-tree-m3 には、この更新の後に人間が実機を見て出した後追いの変更（`24df3b3` / `717db0c`）がある**——判断の語彙を5語ちょうどに畳み（`supportedWithoutTest` / `rejectedWithoutTest` / `deferredToMainDev` を廃止）、移行は用意せず `schemaVersion` を 2 のまま再定義し、課題の箱がバッジのぶん広がるようにした。**この後追いで消した項は無く、足した項も無い。** 直したのは2件——「課題ノードの見送りにキーボード経路が無い」が廃止した種別を名指ししていたので指し先を `deferred` の1種に直し、「次に手を付ける候補」5番目の実機確認の項に**一度画面を見たが、チェックリストは通していない**ことと、項目9（旧版ファイル）の前提が変わったことを書き足した。 **さらにその後追いの続きとして、見送りを1択のドロップダウンから入り切りのトグルに変えた**——課題の `events` は追記専用ではなくなり、切ると最新1件が消える（設計ノート D2 の反転節）。**この変更でも消した項・足した項は無い。** 直したのは「判断の誤操作を塞いでいない」の1件で、課題側は押し直せば戻せるようになったため対象外にし、消えた関数名（`appendDeferral`）を落とした。 その前は issue-tree-m2（課題ツリー登録 Skill とお手本）完了時点（2026-08-23）: **消したのは1件**——「将来の機能を作った瞬間に踏むもの」の**「登録 Skill がまだ無い」**（`[issue-tree-m1]`）。本マイルストーンで `issue-tree-register` を同梱し、`src/core/reading-guide.md` の Skill 名一覧にも載せたので解消した（issue-tree-m1 が「存在しない Skill を名指しすると AI が探して混乱する」として意図的に伏せていた名前が、ここで初めて書ける状態になった）。**書き換えたのは2件**（どちらも `[Skill]` タグ。**新規には足していない**）——(1)「登録**3** Skill は整合性検証の警告文言・計上規則を、アプリと独立に複製している」を**4本**にし、4本目も smoke テスト（`src/modules/issue-tree/skill-write.smoke.test.ts`）で縛られていること・そこでは `rule` 名の集合（5種類）に加えて **`message` が6件であること**も固定していることを書いた、(2)「`palette-fit.mjs` が Node の型ストリップに依存している」の列挙に `issue-tree-register`（`derive.ts` / `canonical.ts` を同ディレクトリから import）と、その機械検査（`src/modules/issue-tree/skill-copy.test.ts`。判定ヘルパは `src/core/import-analysis.ts` へ切り出して sequence 側と共有した）を足した。**足したのは `[issue-tree-m2]` タグの2件**——「テストが無い箇所」へ**`issue-tree-register` に evals が無い**（description の起動精度を測る手段が無い）、「挙動の穴」へ**書き出しスクリプトが配列順を整えない**（**かつ「アプリが開いたときに整う」は誤りで、整うのはアプリで編集して自動保存が走ったときだけである**——`normalizeOrder` は編集コマンドからしか呼ばれない）。あわせて「次に手を付ける候補」へ**実機確認（計画 Task 4）が未実施であること**を4件目として足した——サブエージェントは Tauri の GUI を操作できないため。**これは人間が一巡したら消す項である**（issue-tree-m1・M17・M20 と同じ扱い）。**さらに issue-tree-m2 の最終ブランチレビューの fix wave で `[issue-tree-m2]` タグの3件を「テストが無い箇所」へ足した**（これで issue-tree-m2 が足したのは合計5件）——(1) `canonical.ts` のバイト一致コピーに**網羅のアサーションが無い**（スキーマ側にはある。検査が3ファイルに散っているため、5本目が検査を書き忘れても緑）、(2) `src/core/reading-guide.md` の **Skill 名一覧を縛るテストが無い**（計画が確認手段としていた `reading-guide.test.ts` は Skill 名に触れていない）、(3) issue-tree-m2 が rev 4章に書いた「導出ロジックのコピーを持つ Skill は集計結果の逐語一致も固定する」という規約に、**先行例の `sequence-register` が従っていない**。**同じ fix wave で rev 4章の事実誤りも直した**（`canonical.ts` のコピーの一致検査を `src/core/skill-canonical-copy.test.ts` 1本と書いていたが、実際は3ファイルに分かれている。この台帳の `[Skill]` の項が正しく書き分けていたので、そこから写した）。その前は issue-tree-m1（課題ツリーエディタ）完了時点（2026-08-22）。**消したものは無い**——新しいツールを1本足すマイルストーンで、既存の残件を解消していない。M20 が `[M20]` タグで残した幾何 primitive（`Rect` = `Point & Size`）の項は、**課題ツリーでは踏まなかったので据え置いた**（`layout.ts` が `Rect` リテラルを直接組み立て、`IssuePlacement.rect` をそのまま `edgePath` に渡せるため、M20 が心配したスプレッド組み立ての3本目は生えていない。[`history/issue-tree-m1-editor.md`](history/issue-tree-m1-editor.md) 参照）。**足したのは `[issue-tree-m1]` タグの17件**——「挙動の穴」へ5件（課題ノードの見送りにキーボード経路が無い／見送りの理由を打つ経路が無い／仮説を別の課題へ付け替えられない／ID 重複時にカードが「最後に歩いた方」に付く＝`flat-tree.ts` の先勝ちと**逆の慣習**／「課題を追加」ボタンが DFS 末尾を狙う）、「将来の機能を作った瞬間に踏むもの」へ3件（フォーカスモードと選択ハイライトが未実装／Markdown 出力が無い／登録 Skill がまだ無い）、「テストが無い箇所」へ3件（構造編集の移植関数／`derive.ts`・`consistency.ts` の契約／レイアウトと部品の未検査部分）、「小さな負債」へ4件（`IssueBoxProps.label` の doc の誤り／`leading-5` が唯一の `leading-*`／戻り値型注釈の欠落／`layoutIssueTree` の不変条件が doc に無い）、「性能」へ1件（`lastIssueKey` が state）、「デザイン」へ1件（仮説カードの幅が固定）。**テストの穴と小さな負債は、実装ログ（追跡外）にしか無かった繰り越し所見を拾い直したもの**である（[`history/issue-tree-m1-editor.md`](history/issue-tree-m1-editor.md) の「繰り越した所見の扱い」に、載せなかったものとその理由も書いてある）。あわせて**実機確認（計画の Task 11）が未実施であること**を「次に手を付ける候補」へ一度1項目として足した——サブエージェントは Tauri の GUI を操作できないため——**が、そのまま人間が Windows 実機で一巡し、動作に問題が出なかったため、PR 前に削除した**（2026-08-22。M17・M20 と同じ扱い）。**その一巡の所見として2件足した**——(1)「デザイン」へ、**UI の見た目が「かなり微妙」**という人間の評価（**症状＝どこがどう崩れて見えるかは未取得**であることを項自身に明記してある。人間の言葉の側しか無い、という状態そのものが記録である）、(2)「小さな負債」へ、**スクリーンショット（`docs/images/issue-tree-editor.png`）が未撮影で、`README.md` の画像の行がコメントの中にある**こと（消した候補の中にあった、いまも生きている事実を**残す場所を移した**もので、新しく見つかったことではない）。**なお報告は一巡ぶんの全体の所見であって項目ごとの結果ではない**ので、申し送りの16項目のチェックリストは空のまま残してある。これで `[issue-tree-m1]` タグで足したのは合計19件になる。**書き換えたもの（消していない）**: `[M14]` の `KeyHints` の `key` 衝突の項に、**3本目のヒント集合（`ISSUE_TREE_HINTS`）が実際に生えたが踏んでいない**ことを追記した。**さらに issue-tree-m1 の最終ブランチレビューの fix wave で更新**: 「テストが無い箇所」の構造編集の項から **`addRootIssue` を消した**——`commands.test.ts` に直接のテストが実在する（「循環を含むファイルに根を足しても、フォーカスは足した課題を指す」。それを足した事実の記録の側だけが更新され、台帳の行が逐語で持ち越されていた）。**足したのは2件**——「テストが無い箇所」の「根拠へ」ボタンの reveal 機構の項に、`Shift+Tab` で戻る経路ではボタンが Tab 順から飛ばされる（到達性が方向によって非対称である）ことを子項目として、「挙動の穴」の「課題を追加」ボタンの項に、循環を含むファイルでは図に一度も出ない課題が作られることを子項目として。**`IssueTreeEdges.tsx` は「テストが無い箇所」に載せていない**——同じ fix wave で `IssueTreeEdges.dom.test.tsx` を新設し、線の引き元（ブロックではなく課題ノードの矩形）と `suppressed[child.index]` の引き当てを固定したため。その前は M20（キャンバス基盤のコア化）完了時点（2026-08-22）。「小さな負債」の**「キャンバスの土台が logic-tree と sequence で丸ごと複製されている」の項を削除した**——`src/core/canvas/` への引き上げで複製そのものが解消したため。**「挙動の穴」の logic-tree 側の既知の穴3件（ID 重複時にノードが1つにしか付かない／ドラッグ中のアンマウントでリスナーが残る／`FOLLOW_MARGIN` の8pxずれ）は、消さずファイルパスだけ `src/core/canvas/` へ直した**（移設でパスが変わっただけで、穴そのものは解消していない）。あわせて「モーダル中もホイール／ドラッグが生きている」への言及も消えている——独立した項ではなく削除した複製項の末尾に列挙されていただけで、sequence M3 で既に解消済み（rev 10章参照）。`[MN]` タグ付きの新規項目（「テストが無い箇所」「小さな負債」「挙動の穴」等の各節への追加）は無い。**M20 の実機確認は人間が Windows 実機で一巡して問題が出なかったため、「次に手を付ける候補」へは載せていない**——マージ直前に一度4件目として追加したが、確認が済んだので削除した（[`history/m20-core-canvas.md`](history/m20-core-canvas.md) 参照）。**8項目のうち「開発機と違う OS で通す」だけは未実施**だが、このマイルストーンは OS 依存の変更を1つも含まないため、そのまま閉じる判断をした。**最終ブランチレビューの fix wave で追加更新**: 「小さな負債」の `focusSibling` の項を、`siblingsOf` の移設先（`@/core/canvas/flat-tree.ts`。移設で export 済みになった）を指すよう直し、幾何の primitive（`Rect`/`Point`/`Size`）が2ファイルに分かれ `Rect = Point & Size` になっていない項を `[M20]` タグで新規追加した（直さず記録のみ）。その前は M19（Windows の自動アップデート）完了時点（2026-08-19）。「挙動の穴」に自動再起動の有無・SmartScreen の有無が未確認であることを `[M19]` タグで2件追加、「次に手を付ける候補」に M19 の実機確認が未実施であること・未署名配布そのもの（mac の Gatekeeper・Windows の SmartScreen）が未解決であることを2件追加した（解消した項目は無し）。その前は v1.0.0 リリース前の棚卸し（2026-08-16）。**M15・M18 の実機確認は人間の手で完了した**ため、「次に手を付ける候補」から2件とも削除した（以下に残る「M18 の実機確認は未実施」「M15 のぶんは未実施のまま残る」という記述は、この更新より前の状態を指す履歴である）。残る候補は端末のキーボード復帰1件。あわせて**リリースビルドを初めて通した結果**を「挙動の穴」に反映——MSI が作れないこと（`bundle.targets` から外した）を1件追加し、`bundle.resources` の項に実測値と「glob では除外できない」という調査結果を書き足した（M15 が残した「1行で塞がる」という見通しは誤りだった）。その前は M18 最終ブランチレビューの fix wave（`readLastProjectDir` が空文字列を `null` として弾くよう直したため「テストが無い箇所」の該当項を削除、`saveLastProjectDir` が `settingsFilePath()` を使うよう直したため「小さな負債」の該当項を削除、「挙動の穴」に起動時復元中のスキャン失敗で `io` バナーが出る既知の挙動を M18 のタグで追加）。その前は M18 完了時点: 「テストが無い箇所」に `readLastProjectDir` の空文字列の項を、「小さな負債」に `settingsFilePath()` 未使用の重複の項を、M18 のタグで追加。**M18 の実機確認（`npm run tauri dev` での目視確認）は未実施**——エージェントが Tauri の GUI を操作できないため。「次に手を付ける候補」の2番目に追加した。その前は M17 完了時点: 端末ペインの M11 由来の6件を解消して削除、残る留意点3件を M17 のタグで追加、最終レビューの fix wave で「テストが無い箇所」に `pty_kill` の冪等性の1件を追加。**M17 の実機確認は人間が mac 実機で一巡して問題が出なかったため、「次に手を付ける候補」へは載せていない**——M15 のぶんは未実施のまま残る）

各項目の末尾の `[MN]` は**最初に記録されたマイルストーン**。長く開いているものほど「踏まないから残っている」のか「踏むけど後回しにしている」のかを見分ける手がかりになる。**採番は4系統**（コア・用語集・エラーカタログの `MN`、ロジックツリーの `logic-tree-mN`、シーケンスの `sequence-mN`、課題ツリーの `issue-tree-mN`）。

## 次に手を付ける候補（2026-08-15 の棚卸しより）

全項目をコード実物と突き合わせた結果からの優先順。根拠は各項目の本文にある。**Skill 関連の4項目は M15 で解消し、この一覧から消した**（[`history/m15-skill-hygiene.md`](history/m15-skill-hygiene.md) 参照）。**app-controller の interleaving 3分岐は M16 で解消し、この一覧から消した**（[`history/m16-test-homework.md`](history/m16-test-homework.md) 参照）。**M15・M18 の実機確認は 2026-08-16 に人間の手で完了し、この一覧から消した。** **issue-tree-m1（課題ツリーエディタ）の実機確認も、2026-08-22 に人間が Windows 実機で一巡して動作に問題が出なかったため、この一覧から消した**（M17・M20 と同じ扱い）。**ただし一巡の所見のうち「UI の見た目がかなり微妙」は「デザイン」節へ、未撮影のスクリーンショットは「小さな負債」節へ、それぞれ項として残してある**（[`history/issue-tree-m1-editor.md`](history/issue-tree-m1-editor.md) の「実機確認（Task 11）について」参照）。

1. **端末からキーボードで本体へ戻れない**（挙動の穴の項）——rev 2章の理念との食い違い。ただし「キーは割り当てない」は人間の裁定（設計 決定11）なので、**覆すかどうかは仕様判断**
2. **M19（Windows の自動アップデート）の実機確認が、実リリースを要する部分だけ残っている**（[`history/m19-core-auto-update.md`](history/m19-core-auto-update.md) 参照）。**開発ビルドで見える3項目（起動時の静かな失敗・手動チェックのエラートースト・「最新版です」の判定）は人間の手で完了した。** 残るのは **v1.0.1 を本物のリリースとして出さないと確かめられない6項目**——更新ボタンが「v1.0.1 に更新」に変わるか・確認ダイアログからインストールまで通るか・ダウンロード中に進捗トーストが読め「閉じる」が押せるか・UAC が出ないか・SmartScreen が出ないか・更新後にアプリが自動で戻ってくるか。ローカルの静的サーバでは代用しない（HTTPS・GitHub のリダイレクト・`releases/latest/download` の解決という経路が本番と違う）
3. **未署名配布そのものが未解決**（mac の Gatekeeper・Windows の SmartScreen）——M19 は Windows の自動更新経路を足しただけで、配布物が未署名であること自体（mac の `.app` が quarantine で Gatekeeper に弾かれる・Windows の exe/インストーラで SmartScreen が出る）は解決していない。署名（Apple Developer Program・Windows のコード署名証明書）は別の意思決定
4. **issue-tree-m2（課題ツリー登録 Skill）の実機確認が未実施**（[`history/issue-tree-m2-register-skill.md`](history/issue-tree-m2-register-skill.md) の「実機確認（Task 4）について」に、計画の9項目のチェックリストを空のまま写してある）——サブエージェントは Tauri の GUI を操作できないため。**`npm test` では原理的に届かない場所を踏むための確認である**：置かれた先で `npm install` した後の状態（sequence M4 はこの一手で欠陥を2つ連続で掘り当てた）、**「課題ツリー」と言わずに頼んだときの起動精度**（`evals` が無いのでこれが唯一の手立て）、**開発機と違う OS**（`fs` scope の glob 判定は `require_literal_leading_dot` の既定が unix と Windows で反転する）。**GUI を通さない範囲——採番・`--check` の6警告・書き出しの冪等性・`.gitignore` の効き——はエージェントが実行して確認済み。** 済んだらこの項を消すこと
5. **issue-tree-m3（課題ツリーの俯瞰 UI と語彙）の実機確認が未実施**（[`history/issue-tree-m3-overview-ui.md`](history/issue-tree-m3-overview-ui.md) の「実機確認（Task 10）について」に、計画の項目を空のチェックリストのまま写してある）——サブエージェントは Tauri の GUI を操作できないため。**このマイルストーンは「見た目」そのものが成果物なので、確認しないと成否が分からない種類である**：バッジ5語が畳まれた行で読めるか・展開した仮説のパネルが箱からはみ出さないか・抑制された配下の `ink-faint` が実機の画面で「消えた」に見えないか・ホバーで出る「見送り」のトリガーが長いタイトルに被らないか・帯のチップが押せて視点が飛ぶか・**旧版（`schemaVersion: 1`）のファイルが開き、1回編集すると 2 で書き戻るか**。**あわせて、issue-tree-m1 の一巡で出た「UI の見た目がかなり微妙」がこの作り替えで消えたかを見ること**——同じ言葉がまた出るなら、「デザイン」節へ項として書き戻す（今回は画面ごと作り替えたことを理由に消してある）。**2026-08-23、人間が一度アプリを起動して画面を見た**（`24df3b3` / `717db0c` の発端）。**この項は消さない**——出たのは3件の変更要求（判断を5語に畳む・移行を用意しない・課題の箱がバッジのぶん広がる）であって、上のチェックリストを通した記録ではない。この一巡で**明示的に片が付いたのは項目2の一部**（バッジの語が多すぎる、と読めた）と、**項目8の裏**（空のタイトルの箱でバッジが文言を食い、入力欄が 4px になっていた——`ISSUE_MIN_WIDTH` を廃して `ISSUE_TITLE_MIN_WIDTH` にした。[`history/issue-tree-m3-overview-ui.md`](history/issue-tree-m3-overview-ui.md) の追記節）だけである。**項目9は前提が変わった**——判断の語彙を5語へ畳んだとき廃止した種別（`supportedWithoutTest` / `rejectedWithoutTest` / `deferredToMainDev`）を持つ `schemaVersion: 1` のファイルは、移行後の検証で弾かれて**開けない**（互換の変換を用意しないと決めた）。置くフィクスチャは現行の4種だけを使ったものにすること。残り（ダーク・mac・チップの巡回・旧版の書き戻し・スクリーンショット）は未実施のまま。済んだらこの項を消すこと
6. **M21（役割トークン v2）の実機確認が未実施**（[`history/m21-core-design-tokens-v2.md`](history/m21-core-design-tokens-v2.md) にチェックリストを空のまま写してある）——サブエージェントは Tauri の GUI を操作できないため。**このマイルストーンは「見た目」そのものが成果物なので、確認しないと成否が分からない種類である**（issue-tree-m3 と同じ）：ライト・ダークの両方で18個のトークンが意図どおりに出るか・黄（欠落）と赤（無効）が実機で分かれて見えるか・`judge-yes` の面の上で文字が読めるか・用語集とエラーカタログのカラム名が面を失っても見出しに見えるか・削除ボタンがホバーで赤くなるか・端末の選択面が見えるか・見送りの箱が「沈んで」見えるか。**2026-08-23 に人間が一巡し、淡い面を足した**——「黄土色が欠落として読めるか」「用語集の未定義セルの破線」「ガターの未回答」の3項目で**「輪郭だけでは拾えない」**が出たため、`missing-face` / `invalid-face` / `pending-face` を足して規約2を改訂した（rev 9章）。**残りのチェック項目は再確認が要る**——面が入ったことで見え方が変わっており、一巡ぶんの所見は上の3項目に対するものである。**済んだらこの項を消すこと** `[M21]`
7. **logic-tree-m2（ロジックツリー登録 Skill）の実機確認が未実施**（[`history/logic-tree-m2-register-skill.md`](history/logic-tree-m2-register-skill.md) の「実機確認について」に、計画の10項目のチェックリストを空のまま写してある）——サブエージェントは Tauri の GUI を操作できないため。**`npm test` では原理的に届かない場所を踏むための確認である**：置かれた先で `npm install` した後の状態（sequence M4 はこの一手で欠陥を2つ連続で掘り当てた）、**「ロジックツリー」と言わずに頼んだときの起動精度**と**課題ツリーと取り違えないこと**（`evals` は5ケース定義したが**実行ハーネスに掛けていない**ので、いま起動精度を確かめる手立てはこの手動確認だけである。「テストが無い箇所」の項を参照）、**アプリで編集して自動保存させても配列順が動かないこと**（この Skill は書き出しの時点で DFS 行きがけ順に整えているので、動いたら整え方がアプリとズレている）、**開発機と違う OS**（`fs` scope の glob 判定は `require_literal_leading_dot` の既定が unix と Windows で反転する）。**GUI を通さない範囲——お手本の `--check`（ノード15件・要対応1・整合性の警告0）・`--in/--out` の往復で diff が出ないこと・スキーマ違反の exit 1——はエージェントが実行して確認済み。** 済んだらこの項を消すこと `[logic-tree-m2]`
8. **M29（表形式でコピー）の実機確認が未実施**（[`history/m29-core-table-copy.md`](history/m29-core-table-copy.md) の「実機確認」に、設計スペック §8 の10項目のチェックリストを空のまま写してある）——サブエージェントは Tauri の GUI を操作できないため。**`npm test` では原理的に届かない場所を踏むための確認である**：Excel・Google スプレッドシートに実際に貼ってセルが分かれること、**セル内改行が TSV の `"` 囲みで1セルに収まること**（上の「テストが無い箇所」の項）、メモ帳に貼って TSV が入ること、「形式を選択して貼り付け→テキスト」で書式なしになること、絞り込み中のコピーが絞り込み後の件数になりトーストが「42 件中 3 件」と出ること、絞り込み中の「Markdown を書き出す」が全件のままであること、赤のあるファイルで設定ダイアログに警告が出ること、**ダイアログを開いている間 Esc がエディタに届かないこと**（`modalOpen` の配線。fix round で Esc の無反応自体は直した——[`history/m29-core-table-copy.md`](history/m29-core-table-copy.md) 参照——が、エディタ側へ抜けないことは実機で見ていない）。済んだらこの項を消すこと `[m29]`
9. **issue-tree-m4（スキーマ v3）・issue-tree-m5（仮説の展開 UI）の実機確認が未実施**（同じ画面〔課題ツリーエディタ〕を触るので一緒に見る。[`history/issue-tree-m4-schema-v3.md`](history/issue-tree-m4-schema-v3.md) の「実機確認（Task 15）について」と [`history/issue-tree-m5-hypothesis-ui.md`](history/issue-tree-m5-hypothesis-ui.md) の「実機確認のチェックリスト（Task 12）」に、それぞれの計画のチェックリストを空のまま写してある）——サブエージェントは Tauri の GUI を操作できないため。**`npm test` では原理的に届かない場所を踏むための確認である**：お手本が v3 で開けること・帯の「FB待ち N」とチップの巡回・別枠「見送り N」「解決 N」が2つ並ぶこと・解決の旗を掲げた課題の描かれ方と押すと外れること・展開パネルが m5 で「ソリューション仮説／価値仮説／検証結果／以前の判断／FB」に組み直っており「由来」の節が無いこと（m4 時点の「判断・以前の判断・FB」の3節構成ではない）・検証結果のバッジ自体が判断のドロップダウンのトリガーになっていること（m5 Task 6。押すと判断を変えられ直後に理由が打てるか）・「根拠へ」ボタンがどこにも無くFBの欄で主修飾キー＋`Enter`が無反応なこと・判断を追加するとその日の`date`が入ること・`detail`/`value`/`asks`/FBの`by`/`sentiment`/`date`が画面のどこにも出ていないこと・仮説の追加（帯・節末のボタン）と削除（ゴミ箱アイコン）がマウスで通ること（m5 Task 7）・展開中は列全体が押し広げられ横スクロールが増えること（m5 Task 2。前提2の見え方が受け入れられるか）・判断バッジのキーボード操作（Tab到達・Enter/Spaceでの展開・矢印での選択）・**開発機と違う OS**（`fs` scope の glob 判定は `require_literal_leading_dot` の既定が unix と Windows で反転する）。**GUI を通さない範囲——お手本の書き出しスクリプト実走（課題11件／仮説6件／⚠要対応3〔仮説なし1／保留1／FB待ち1〕／見送り1／解決1）・`npm test`/`tsc`/`lint`の全件緑——はエージェントが実行して確認済み。** 済んだらこの項を消すこと `[issue-tree-m4]`

## テストが無い箇所

- **`FLUSH_MAX_ROUNDS` の打ち切りパスに直接テストが無い**（`src/core/autosave.ts`）。戻り値は安全側の false なので後回し可 `[M5]`
- **`fileExists` に専用の単体テストが無い**（`src/fs/project-fs.ts`）。`exists` への1行委譲で、意味のある挙動は `file-ops.test.ts` が押さえている `[M5]`
- **`ChoiceDialog` のオーバーレイクリックのテストが無い**。`onOpenChange` を渡さないことで構造的に担保され、同じ機構を Esc のテストが固定している `[M5]`
- **「二重に `pty_kill` しても無害」を直接踏む Rust テストが無い**（`src-tauri/src/pty.rs`）。`TerminalTab` のアンマウント時 kill（M17）はこの性質に依存しているが、保証しているのは `sessions.remove` が消えた id に対して `None` を返すという構造だけで、テストは無い `[M17]`
- **TSV の `"` 囲みが Excel で1セルに収まるかは実機でしか確かめられない**（`src/core/table-tsv.ts`）: 単体テストが見ているのは**文字列の形**（改行・タブ・`"` を含むセルだけ `"` で囲む・`"` の倍化・含まないセルは囲まない、という RFC 4180 の規則どおりに文字列を組み立てているか）だけで、**その文字列を実際に Excel・Google スプレッドシートへ貼ったときに1セルへ収まるか**は貼り付け先のパーサ次第であり、`npm test` では原理的に確かめようがない。実機確認のチェックリスト（[`history/m29-core-table-copy.md`](history/m29-core-table-copy.md)）の2番目・3番目がこれに当たる `[m29]`
- **課題ツリーの構造編集に、直接のテストを持たない関数が残っている**（`src/modules/issue-tree/commands.ts`）: `logic-tree` から移植した課題側の関数——`addSiblingIssueAfter`（ルート分岐を含む）/ `setIssueText` / `addHypothesisAfter` / `deleteHypothesis`——は、**移植元のテストが一緒には来ない**ので直接のテストを持たない（DOM テストが通る経路だけが間接的に押さえている。`grep -rn` でこの4関数への直接の呼び出しが `*.test.ts` に0件であることを確認済み）。あわせて `deleteIssueSubtree` の行き先規則（前の兄弟 → 親 → 無し）を固定するテストも無い（`commands.test.ts` の2本の `it` はデータの並び替えだけを見ており、戻り値 `.focus` は見ていない）。**この項が挙げていた `setPendingNote` / `removePendingNote` / `setHypothesisText` / `setRationale` / `promoteNote` の5関数は、issue-tree-m4 で FB のデータ形が変わった（`promoteNote` 廃止）ときまでに全て消えており、issue-tree-m5 の時点で存在しない幽霊だった**（`git show e79c40f:src/modules/issue-tree/commands.ts` でも既に0件。issue-tree-m5 のレビューが実物と突き合わせて発見し、生きている関数だけの記述に書き直した）。**移植でテストが引き継がれないのはこのリポジトリで繰り返し起きる形**なので、次に構造編集へ手を入れるときに一緒に埋める `[issue-tree-m1]`
- **`derive.ts` / `consistency.ts` の契約に穴がある**（`src/modules/issue-tree/`）: `poseQuestions` の「戻り値は入力配列と**同じ添字**で並ぶ」という契約（ID 重複ファイルのために必要）を突くテストが無く、**ID を鍵にした誤実装でも既存テストは全部緑になる**。ほかに `issueStatus` / `IssueStatus` が export されているのに一度も呼ばれていない・同じ葉に仮説を2件以上置くケースが無い（0件/1件だけ）・課題0件の空ファイルを見るテストが無い（`logic-tree` の原本には「ノード0件は正常」がある）・**5ルールの出力順を配列全体で固定するテストが無い**（全テストが `rule` で filter するので、順が変わっても同じ rule が二重に出ても捕まらない） `[issue-tree-m1]`
- **`onCloseAutoFocus` の抑止は、jsdom では原理的に守れない。実機でしか守れない**（`src/modules/issue-tree/IssueTreeEditor.tsx:272` の `KindMenu`。issue-tree-m5 で判明）: 判断を選んだ直後に根拠の欄へフォーカスを移す仕掛け（`picked` ref が Radix の「トリガーへフォーカスを戻す」既定動作を `onCloseAutoFocus` の `preventDefault` で抑止する）は、確定（`onSelect`）がその中で先に `onOpenChange(false)` を呼んで閉じる作りになっている。**jsdom ではこの経路で Radix の `FocusScope` によるフォーカス復帰そのものが走らない**ため、`onCloseAutoFocus` 側の抑止（`preventDefault`）を外す変異を入れても `IssueTreeEditor.dom.test.tsx`（42件）は赤くならない——赤くなるのは機械のもう半分（`onSelect` が先に閉じる処理）を切ったときだけである。**「まだテストを書いていない」のではなく「jsdom というテスト環境の構造上、この半分は検査しようがない」**。`npm test` が緑のままであることを根拠に、`picked` ref や `onCloseAutoFocus` の抑止を「使われていないコード」として消さないこと。Task 12（実機確認）の「検証結果のバッジを押して判断を変えられ、直後に理由が打てる」がこの機構そのものを踏む項目である。**issue-tree-m1 が同じ項に挙げていた2つ（`HypothesisPlacement | null` の契約・`pendingNotes` の矩形）は issue-tree-m3 で解消済み、「根拠へ」ボタンの reveal 機構は issue-tree-m4 で廃止済み**——いずれもこの項からは既に落としてある `[issue-tree-m1]`
- **`issue-tree-register` に evals が無い**（`.claude/skills/issue-tree-register/`）: 既存3本（`glossary-term-register` / `error-catalog-register` / `sequence-register`）は評価ハーネス（`evals/evals.json` / `grade.mjs` / `fixtures/`）を持つが、**4本目だけ持たない**（issue-tree-m2 の計画が範囲外に指定した）。**description の起動精度を測る手段が無い**ので、他の登録 Skill と誤起動し合っていても気づけない。**いま起動精度を確かめる唯一の手立ては実機確認の項目5**（「課題ツリー」と言わずに頼んで起動するか）**で、それも未実施である**（下の「次に手を付ける候補」参照）。作るときは既存3本の `grade.mjs`（M15 で自己位置解決を `import.meta.url` 起点に揃えてある）に倣えばよい `[issue-tree-m2]`
- **`src/core/reading-guide.md` の Skill 名一覧を縛るテストが無い**: 6本目の登録 Skill を足したとき、配布物である読み方ガイドの Skill 名一覧を更新し忘れても**緑のまま通る**。**logic-tree-m2（5本目）はこの穴を素通りできたが、更新自体は実際に行われた**——つまり守ったのは人の記憶であって、テストではない。`src/core/reading-guide.test.ts` は Skill 名にも `BUNDLED_SKILLS` にも触れていない（`grep "BUNDLED_SKILLS\|register" src/core/reading-guide.test.ts` は0件）ので、issue-tree-m2 の計画が確認手段としていた「`reading-guide.test.ts` が緑」は、この追従について何も担保していなかった `[issue-tree-m2]`
- **`sequence-register` が「集計結果の逐語一致も固定する」規約に従っていない**（`src/modules/sequence/skill-write.smoke.test.ts`）: issue-tree-m2 で rev 4章に**導出ロジックのコピーを持つ Skill は集計結果の逐語一致も併せて固定する**と書いたが、**先行例の `sequence-register` は未適用のまま**である。smoke テストの `it` は「アプリの整合性 message がすべて stdout に逐語で現れる」「欠陥の無いファイルは警告なしの exit 0」の2本だけで、`questions.ts` 由来の集計（問いの導出結果）を突き合わせる検査が無い。**`message` の一致はコピーが「読めた」ことしか担保しない**（整合性検証は手複製の側なので、`questions.ts` のコピーが空でも message は出る）というのが規約の理由なので、その穴は sequence 側に開いたままである。課題ツリー側の `src/modules/issue-tree/skill-write.smoke.test.ts` の「集計行がアプリの `tallyLine` と逐語で一致する」に倣えば埋まる `[issue-tree-m2]`
- **`logic-tree-register` の evals は定義しただけで、実行ハーネスに掛けていない**（`.claude/skills/logic-tree-register/evals/`）: 5ケースの `evals.json`・判定器 `grade.mjs`・fixture は揃っており、**`grade.mjs` は合成データを食わせて動作を確かめてある**が、**実際に Skill を起動させて採点する経路は一度も通していない**。したがって **description の起動精度の実測値はまだ無い**——とくに `issue-tree-register` との誤起動（ケース2・3がそのために置いてある）が実際に起きないかは未確認である。`issue-tree-register` は evals そのものが無い（上の項）のに対し、こちらは**あるのに回していない**という違い `[logic-tree-m2]`
- **`logic-tree-write.mjs` の `.gitattributes` の警告が「整合性の警告」の見出しの下に出る**（`.claude/skills/logic-tree-register/scripts/logic-tree-write.mjs:216`）: `.gitattributes` に `*.json text eol=lf` が無いときの注意を `warnings` に push しているため、`⚠ 整合性の警告（アプリでは赤表示。ファイルは開けます）` の下に並ぶ。**これは整合性の問題ではないし、アプリで赤表示にもならない**ので、見出しが嘘になっている。読む人が「アプリで赤くなる」と誤解するか、逆にこの見出しの信頼を落とす。**かつ未テストである**——smoke テストは一時ディレクトリで走り、そこに `.git` が無いのでこの分岐に入らない。直すなら警告の器を2つに分ける（整合性の警告／環境の注意） `[logic-tree-m2]`
- **`skill-write.smoke.test.ts` が子プロセスの stderr を継承している**（`src/modules/logic-tree/skill-write.smoke.test.ts:47`）: `execFileSync('node', [...], { encoding: 'utf8' })` に `stdio` を渡していないので stderr が親へ流れ、**意図的なスキーマ違反ケースのエラー出力が、緑の実行でも画面に出る**。**緑の実行が壊れて見えるのが害である**——読む人がテスト出力中のエラーを無視する癖がつき、本物の失敗を見落とす。`stdio: ['ignore', 'pipe', 'pipe']` を足せば消える（他モジュールの smoke テストが同じ書き方なら揃えること） `[logic-tree-m2]`
- **`logic-tree-write.mjs` の exit 2 の経路と、スキーマの解決順が未テスト**（`.claude/skills/logic-tree-register/scripts/logic-tree-write.mjs`）: スキーマが見つからないときの `die(2, ...)` を踏むテストが無く、**`--schema` → `FACET_LOGIC_TREE_SCHEMA` → 自動探索（同梱コピーを含む）という優先順位もどこも検査していない。** 出荷先で実際に働くのは自動探索の枝だけなので、**壊れたときに気づけるのが実機だけ**という形で残っている（同梱コピーを持つ理由は rev 4章）。smoke テストは常にリポジトリの中から走るため、自動探索がリポジトリの `schemas/` を先に拾っていても、同梱コピーの側が壊れていても緑になる `[logic-tree-m2]`
- **`ink-faint` を「アクティブな本文に使っていない」ことを機械検査していない**（`src/styles/conventions.test.ts`）: `ink-faint` は 3:1 の段で、WCAG 1.4.3 が**非アクティブ要素を免除する**範疇に収まる前提で置いた（rev 9章）。いま `text-ink-faint` / `border-ink-faint` / `bg-ink-faint` が出るのは6箇所（`src/components/badge-styles.ts:40` の `faint` variant、`HypothesisRow.tsx:131` / `:133` / `:161` / `:186`、`IssueBox.tsx:85`。M21 で1つ目が共通部品へ移り、行番号もずれた）で、**すべて `suppressed`（祖先の見送りで抑制された配下）の分岐の中**だが、**それを縛っているのはレビューだけである**——読ませる本文に当てても `npm test` は緑のまま通り、本文の 4.5:1 を静かに割る。検査を書くなら「`text-ink-faint` の出現は抑制・非アクティブの文脈に限る」形になるが、**文脈をソースの走査で判定する検査は誤検知しやすい**ので、2箇所目の用途（フォーカスモード等）が出て「抑制以外にも使う」が確定した時点で形を決めるのがよい `[issue-tree-m3]`
- **`schemaVersion` の移行を、読み込み時以外の経路で見るテストが無い**（`src/core/load.ts` / `src/core/autosave.ts` / `src/modules/issue-tree/module.test.ts`）: issue-tree の 1 → 2 がこのリポジトリ初の版上げで、`load.ts` の移行フックにも `migrate.test.ts` にもテストは付いた。**空いているのは2つ**——(1) rev 5章が定める「**ファイルへの反映は最初の編集保存時**」、つまり旧版で開いたファイルを1回編集すると現行版で書き戻されることを見るテストが無い（保存経路は `autosave.test.ts` の管轄で、そこに版の観点が無い）、(2) `load.ts` の移行フックを通す唯一のテストは**偽のモジュール**を登録した `src/core/load.test.ts` で、**実物の `issueTreeModule` を v1 のファイルで `classifyFile` に通す経路はどこにも無い**（`module.test.ts` はお手本＝v2 を通しており、配線は見ているが移行は踏んでいない）。どちらも実機確認の項目に入れてある `[issue-tree-m3]`
- **`invalid`（無効）の見せ方に DOM テストが無い**（`src/modules/issue-tree/HypothesisRow.tsx:190` の `outline-invalid` / `IssueBox.tsx:83` の `border-invalid`）: 整合性検証が無効と判定した仮説の行の輪郭と、課題の箱の赤枠は、**どちらも `invalid` を渡したときの見え方を固定するテストを持たない**（`derive.ts` / `consistency.ts` 側の「無効と判定すること」は検査されているが、そこから先の描画は素通りする）。**これは M21 で入った欠落ではなく、issue-tree-m3 から続く既存の穴である**——M21 は色の名前を `warning` から `invalid` へ替えただけで、テストの有無は変えていない。`HypothesisRow.dom.test.tsx` にチップの面（実線・破線）を見るテストがあるので、同じ形で足せる。**M22 で `invalid` が欠落（`missing`）に勝つことを見る DOM テストがロジックツリーの `NodeBox`（`LogicTreeEditor.dom.test.tsx`）とシーケンスのアクターヘッダ・参照ボタン（`SequenceEditor.dom.test.tsx` / `ActorRefCell.dom.test.tsx`）に新設され、そちらの穴は埋まった**——ただしこの項が指す `HypothesisRow.tsx` / `IssueBox.tsx` の赤枠そのものは変わらず未検査なので、残る対象をこの2ファイルに絞る `[M21]`
- **`<Button>` の variant を見る検査は、属性値に `>` を含むと早期に閉じる**（`src/styles/conventions.test.ts:164` の `/<Button[^>]*>/g`）: 正規表現でタグを切り出しているので、`title={a > b ? …}` のような属性値があるとそこでマッチが終わり、**その後ろに書かれた `variant` が切り出した範囲に入らない**。結果は**誤検出**（false positive）で、`variant="outline"` を正しく書いてあるボタンを違反として挙げる——見落とし（塗りの `default` の素通り）ではない。**現状そのような書き方は1件も無い**ので実害はゼロだが、**ソースを正規表現で走査する検査の一般的な限界**として記録する（`conventions.test.ts` の他の検査も同じ性質を持つ）。塞ぐなら AST で読むことになるが、この1件のために構文解析器を持ち込む値打ちは今は無い `[M21]`
- **欠落の帯そのものに DOM テストが無い3モジュールがある**（`src/modules/glossary/GlossaryEditor.tsx` / `src/modules/error-catalog/ErrorCatalogEditor.tsx` / `src/modules/logic-tree/LogicTreeEditor.tsx` の `jumpToMissing` と `MissingTally`）: `sequence`・`issue-tree` は `SequenceEditor.dom.test.tsx`／`IssueTreeEditor.dom.test.tsx` に「要対応 N」「未回答 N」等のテキストのアサートと、チップ押下→`document.activeElement` の一致（フォーカス移動）を見るテストが実在するが（`SequenceEditor.dom.test.tsx` の「未回答のチップは未回答のスロットだけを巡る」等、`IssueTreeEditor.dom.test.tsx` の「帯のチップを押すと、その種類の次の要対応へフォーカスが移る」）、**残る3モジュールの `*.dom.test.tsx` には `要対応` の文字列アサートもチップの `getByRole('button', ...)` も一件も無い**（`grep` で確認済み。件数の正しさは各 `missing.test.ts` の単体テストが検査しているが、それが画面の帯に**描かれること**・チップを**押すとフォーカスが動くこと**はどちらも未検査） `[M22]`
- **エクスプローラからのドロップにテストが1本も無い**（`src/App.tsx` のドロップ受け口 / `src/fs/drag-drop.ts`）: ペインの中かどうかの判定が `getBoundingClientRect` と `window.devicePixelRatio` の換算に依存しており、**jsdom にはレイアウトが無い**ため実体を持てない（M11 の教訓。設計 §6.2）。畳んだペインでは矩形が0になり必ず「外」と判定されること・複数ファイルを一度に落としたときの連結・`over` 中の枠の強調は、いずれも Task 10 の実機確認でしか確かめていない `[M28]`
- **右クリックで OS の既定メニューが実際に消えることを守っているのは実機確認だけ**（`src/components/TerminalTab.tsx` の `handleContextMenu`）: DOM テストが見ているのは `event.preventDefault()` が呼ばれたことだけで、**WebView2 が実際にメニューを出さないこと自体はテストで観測できない**。壊れても `npm test` は緑のまま通る `[M28]`
- **`src/core/load.test.ts` の「旧版 → editable」の `it` が空配列フィクスチャのまま**（「既知 type × 旧 schemaVersion は module.migrate で移してから検証し、editable になる」）: フィクスチャは `issues: []` / `hypotheses: []` なので、v3 で増えた必須キー（`title` / `detail` / `value` / `asks` / `feedbacks` / `date` 等）の要不要は要素の中身に一度も現れず、この `it` は実質そこを検証していない。同じ主張（1 → 3 の移行がスキーマ検証を通る）は `migrate.test.ts` の最終 `it` が実データを使って別途担保しているので実害は無い `[issue-tree-m4]`
- **`src/core/today.test.ts` の「引数を省略すると『いま』を返す」が、`todayString()` と `todayString(new Date())` を別々の時刻で評価している**（`const now = new Date(); expect(todayString()).toBe(todayString(now))`）: `todayString()` の内部呼び出しは `now` を作った瞬間より後なので、日付が変わる瞬間をまたげば2つの文字列は食い違い、この `it` は理論上落ちうる。実務上のフレーク率は無視できる水準（日付境界の数十ms） `[issue-tree-m4]`
- **`ensureVisible` / `panIntoView` を見るテストが課題ツリーに1本も無い**（`src/modules/issue-tree/IssueTreeEditor.tsx` / `src/core/canvas/`）: 帯のチップやショートカットで巡回した先が実際に画面へ寄るかは、`detail` / `value` / `feedback` / `event` / `ask` のどのセルについても未検査。jsdom にはレイアウトが無く矩形が測れないため、`ensureVisible` を直接踏む単体テストは書けても「実際に視界に入るか」は実機でしか確かめられない（既存の穴で、issue-tree-m5 が問いのセル（`ask`）を巡回対象に加えたときに気づいた） `[issue-tree-m5]`
- **判断バッジのトリガーのキーボード経路に DOM テストが無い**（`src/modules/issue-tree/IssueTreeEditor.tsx` の `KindMenu`）: Tab で `<DropdownMenuTrigger>` に到達すること、Enter/Space でメニューが展開すること、矢印で項目を選べることは、いずれも Radix の既定動作に委ねているだけで、課題ツリー側にこれを固定するテストが無い。マウスでの選択（「判断を選ぶとイベントが追記される」）はテストがあるが、キーボード経路は素通り `[issue-tree-m5]`

## 将来の機能を作った瞬間に踏むもの

- **`ensureFileOfType` は将来のインライン登録から呼ぶと二択と競合する**（`src/core/app-controller.ts`）: 内部で `rescan()` を回すので、`ask` が出た直後に `selectFile` で選択を移してしまい、回答が「選択が変わったため書き戻しませんでした」に倒れる。今日は「用語集を作る」ボタンが空状態（未選択時）にしか出ないので到達不能だが、**インライン登録を実装した時点で踏む**（[`history/m4-core-file-operations.md`](history/m4-core-file-operations.md) の `ensureFileOfType` に関する項と併せて読むこと） `[M6]`
- **モジュール規約8（表記ゆれ検知の対象フィールドパス宣言）が `ToolModule` に無い**（`src/core/registry.ts`）: rev 6章は8点セットと書いているが、コードは7点＋`createEmpty`。**検知エンジン自体もコアに無い**ため、宣言だけ足しても読み手のいない死んだコードになる。エンジンを作る時点で両方を足す `[M9]`
- **`scripts/gen-types.mjs` は `schemas/*.schema.json` が減っても対応する `src/types/*.ts` を消さない**: スキーマを走査して書き出すだけで、消えたスキーマの古い型ファイルは掃除しない。M9 で `.gitignore` を `src/types/glossary.ts` から `src/types/*.ts` に広げたため、取り残された型ファイルは `git status` に現れず `tsc` の対象にだけ残る。**2本目以降のツールでスキーマを作り直す／消す時点で踏む** `[M9]`（M10 はスキーマを1本足しただけで、作り直し・削除は発生していないため未解消のまま）
- **エッジに矢印を描いていない**（`src/modules/logic-tree/TreeEdges.tsx`）: [`logic-tree/logic-tree-canvas-tech-notes.md`](logic-tree/logic-tree-canvas-tech-notes.md) 論点3 は「曲線と矢印」としているが、横向きで親が必ず左にあるため向きは曖昧でなく、M1 では描いていない。**エッジの種類を増やす（点線・色分け）ときに再検討する**——種類が増えた瞬間「線の意味」を線自体が語る必要が出るため `[logic-tree-m1]`
- **`KeyHints` の `key={hint.keys}` は同じ `keys` が2件あると衝突する**（`src/components/KeyHints.tsx`）: React の `key` に `hint.keys` 文字列をそのまま使っており、一意性の保証は呼び出し側任せ。現行の `SEQ_HINTS` / `TREE_HINTS` / `ISSUE_TREE_HINTS`（issue-tree-m1 で生えた3本目。5件）はいずれもキー表記が一意なので今は無害だが、**同じキー表記を再利用すると踏む**。**3本目は実際に生えたが踏んでいない**——`ISSUE_TREE_HINTS` の5件（`Enter` / `Tab` / `$mod+Enter` / `←→` / `$alt+↑↓`）は集合の中で重複していない（`key` の一意性はヒント集合ごとに閉じているので、他の集合と表記が被っても問題は起きない） `[M14]`
- **ゾーンは sequence M5 以降**（`schemas/sequence.schema.json` / `src/modules/sequence/`）: design-notes 論点12 の M3+ 候補のうち、M3 はマウス操作と出力を、M4 は登録 Skill を採った。ゾーンは `schemaVersion` 改訂＋マイグレータを伴う唯一の候補で、`questions.ts` / `consistency.ts` / `layout.ts` / ガター集計と縦に全層を貫く。データ形式（step ID のペアか所属宣言か）も未確定。design-notes が付けた条件「**同じ答えを何度も書いた実感を得てから**」を満たしたかを先に確かめること `[sequence-m3]`
- **フォーカスモードと選択ハイライト（D8）が未実装**（`src/modules/issue-tree/`）: [`issue-tree/仮説検証モジュール-設計ノート.md`](issue-tree/仮説検証モジュール-設計ノート.md) の D8 は「いまどの課題を説明中か」の共有を**純粋なビュー状態**（選択ハイライト＋選択サブツリー以外を薄くする）で実現すると決めたが、issue-tree-m1 のスコープ IN には入っていない。**レビューで実際に困ってから作る。** 作るときの制約は D8 が既に置いてある——**データに保存しない**（消し忘れフラグ＝嘘データの温床。座標を保存しないのと同じ判断）／**色は使わない**（赤＝未定義の意味論を汚染しない）。「薄くする」を `opacity` で実装すると検算したコントラストを割る点は、抑制の見せ方で既に踏んでいる（地の色に落とす形を採った） `[issue-tree-m1]`
- **課題ツリーに Markdown 出力が無い**（`src/modules/issue-tree/module.ts` の `outputs: []`）: 設計ノートの OUT。**本当に必要になるのは PoC 終盤**（検証結果を意思決定の場に持ち込むとき）で、それまでが観察期間という条件が付いている。規約上 `outputs: []` は正しい状態で（rev 6章）、額縁は書き出し・コピーの両ボタンを `disabled` にする。作るときは `describeIssueEffect`（整合性エラーがあるまま出力しようとしたとき何が起きるかの1文）も一緒に決めること `[issue-tree-m1]`

## 挙動の穴（実害は小さいが残っている）

- **`closeCurrentFile` の尻尾が古いまま実行される残余 interleaving**（`src/core/app-controller.ts`）: flush 待ちの間に `handleSelectedGone` が選択を落とし、続けて新しい `selectFile` が完了して新しい `saver` が張られた場合、遅れて再開した古い `closeCurrentFile` は（M16 の修正により新しい `saver` には触らなくなったが）`setSelected(null)`/`host.setDocument(null)` を無条件に実行する——新しい `saver` が生きたまま選択と編集画面だけが閉じる。`closeCurrentFile` は `selectSeq` トークンを持たないため、塞ぐには契約の変更が要る。M16 修正前は同じ経路で新しい `saver` ごと dispose して pending 編集を黙って捨てていたので、現状は「破壊は減ったが穴は残る」状態 `[M16]`
- **削除の着地前に飛んだ flush が消えたファイルを復活させうる**（`src/core/app-controller.ts`）: `handleSelectedGone` は意図して flush しない（書き戻すと復活するため）が、その検知より前に `closeCurrentFile` が飛ばした flush は既に in-flight であり、着地すれば外部で消えたファイルを書き戻す。M16 の修正はこの窓を広げても狭めてもいない（flush を呼ぶタイミングは不変） `[M16]`
- **ロジックツリーにマウスだけでノードを増やす動線が無い**（`src/modules/logic-tree/LogicTreeEditor.tsx`）: 追加は `Enter`（兄弟）／`Tab`（子）のキーだけで、帯のボタンは**0件のときにルートを作る**1本しか無い。M14 は「フォーカス中のノードを持つ」という新しい状態を作らずに済ませるため、意図的に手を付けなかった。シーケンスは「ステップを追加」「アクターを追加」を常設しており、**2本のキャンバスでマウス動線の厚みが揃っていない** `[M14]`
- **サイドメニューを畳むとフォルダのパスが読めない**（`src/components/FileList.tsx`）: M14 でパスを額縁の帯からファイル一覧の直上へ移した（帯の幅を食う最大の要素だったため）。畳んだ状態でどのフォルダを開いているかを確かめる手段が画面に無い `[M14]`
- **サイドメニューのパスをコピーすると先頭に不可視文字が付く**（`src/components/FileList.tsx`）: 頭を省いて末尾（フォルダ名）を残すために `dir="rtl"` を当てているが、それだけでは POSIX パスの先頭 `/` が右端へ回る（bidi の N2）。U+200E を先頭に置いて防いでいるので**表示は正しい**が、その文字は DOM のテキストに入るため、選択してコピーした文字列はそのままではシェルに渡せない。`title` は生のパスなので、そちらからは正しく取れる。JS で頭を省く実装に替えれば消えるが、その場合は読み上げに届く文字列が切り詰まる `[M14]`
- **`layout.totalHeight` が図の実際の一番下を表さない**（`src/modules/sequence/layout.ts` / `SequenceEditor.tsx`）: M14 で追加した「末尾にステップを追加」ボタンは `top: layout.totalHeight + ROW_GAP` に置かれるため、実際に描かれる一番下は `totalHeight` より下（ボタンの高さぶん）にある。右端の `totalWidth`/`GhostSlot` の帳簿ずれ（下記）と同種の欠陥で、**図の下端を扱う変更をするとき必ず踏む** `[M14]`
- **`replyTo`（応答と呼出の対応）が無く、`reply` 行の説明が一般文言である**（`schemas/sequence.schema.json` / `src/modules/sequence/`）: 「この応答はどの呼出への応答か」をデータが持たないため、行の説明は誰に対しても同じ文になる。design-notes 論点3 は**意図的に持たない**と決めた（対応の明示が要ると分かってから入れる）ので欠陥ではないが、**呼出が入れ子になるシナリオを実際に書いたときに読めるか**は実使用でしか分からない。ホバーで対の呼出をハイライトする等を検討するなら、まずこのフィールドが要る `[sequence-m1]`
- **`reply` 行の一般文言の高さが行高に入らない**（`src/modules/sequence/layout.ts` / `SequenceEditor.tsx`）: ガターの行高は「答えスロット群と問いラベルの高い方」から導くので、`answers` も `ghosts` も無い `reply` 行に出す一般文言（`─ 応答が返らないときは呼出側の「結果がわからなかったら？」に書く`）と、問いが立たない行の `─ 問いは立たない` は、**どちらも高さを勘定に入れていない**。折り返した瞬間に次の行へ食い込む。sequence-m5 は**文言を1行に収まる長さへ短縮して回避した**が、`QUESTION_LABEL_WIDTH` を縮めるか文言を伸ばせば再発する（同定数のコメントに注記済み）。根治は一般文言も測って行高に足すことだが、それは「行高はガターのスロット群が決める」という導出を1本増やす変更になる `[sequence-m5]`
- **`GhostSlot` の ✕ が `layout.totalWidth` の外に約26px はみ出す**（`gap-1` ぶんも帳簿外）（`src/modules/sequence/GhostSlot.tsx` / `layout.ts`）: 図の幅の帳簿（`layout.totalWidth`）と実描画がずれている——ghost の削除ボタンは `layout.totalWidth` を考慮せず配置されるため、右端に固定幅ぶんはみ出す。sequence M2 の実機確認では崩れとして問題視されなかったが、**構造的なずれなのでガターに列を足す等、図の右端を扱う変更をするとき必ず踏む** `[sequence-m2]`
- **起動時の自動復元中にフォルダ走査が失敗すると `io` バナーが出る**（`src/core/app-controller.ts`）: 復元は「設定読み込み・scope 付与・存在確認」の3つの失敗点だけを静かに握りつぶす設計（spec スコープ節）だが、そこを通過した後の `openProject` 内のフォルダ走査自体が失敗した場合（読めないファイルがある等）は、手動でフォルダを開いたときと同じ `host.setBanner('io', ...)` が起きる。意図的な例外——ユーザーが選んでいないフォルダについてのバナーが起動直後に出うる、という認識共有の記録 `[M18]`
- **`resolveCommand` の細かい非対称**（`src/core/keyboard/keymap.ts`）: macOS の `Ctrl+Backspace`（主修飾キーは Cmd なので素の Backspace として通る）と `Alt+Shift+↑↓`（`altKey` を先に見るため並び替えになる） `[M3]`
- **ID が重複しているファイルでは、その ID を親に指すノードが先頭の1つにだけ付く**（`src/core/canvas/flat-tree.ts`）: 挙動は決めてあるが、**画面に出るのは「ID が重複しています」だけ**で、木の形が想定と違って見える理由が読み手に繋がらない。ID 重複を直せば解消するので実害は小さいが、原因の説明が要る `[logic-tree-m1]`
- **ドラッグ中にアンマウントすると d3 が window に張ったリスナーが残る**（`src/core/canvas/use-viewport.ts`）: 体感とは無関係に成立し、実機確認を終えても解消しない `[logic-tree-m1]`
- **`FOLLOW_MARGIN`(48) > `CANVAS_MARGIN`(40) で初回の追従が 8px ずれる**（`src/core/canvas/use-viewport.ts` / `viewport.ts`）: 完全に見えているノードでも初回の追従だけ 8px 余分に動く。**実機確認の前に載せておく必要がある**——載せておかないと、実機で「1回だけカクッと動く」を見た人が I-1（二重スクロール）の再発と誤診する `[logic-tree-m1]`
- **一度端末をクリックするとキーボードだけでは本体へ戻れない**（`src/components/TerminalTab.tsx` の `attachCustomKeyEventHandler`——`Shift+Enter` 以外はすべて xterm へ渡す）: xterm が `Tab` を消費するため。ペインの開閉にショートカットを割り当てない判断（設計 決定11。**人間が「キーは割り当てない」と裁定**）の帰結で、マウスで本体をクリックする必要がある。**facet は入力速度最優先（rev 2章）を掲げているので、体験としては未達である** `[M11]`
- **ペインが壁に当たった状態でさらに広げようとすると、記憶している幅が縮む**（`src/core/column-resize.ts` / `src/components/PaneSplitter.tsx`）: `upper` に達している状態でドラッグすると、クランプ後の値が「意図」として書き戻される。ウィンドウを広げても元の幅に戻らない（ダブルクリック／`Home` で復帰可能）。**表エディタと共有しているモジュールなので、直すなら両方の挙動を見る必要がある** `[M11]`
- **`pty_write` は同期コマンドのままなので、本当に詰まると UI ごと止まる**（`src-tauri/src/pty.rs`）: M17 で `sessions` の錠前は外した（書き手を別の `Arc<Mutex<…>>` へ隔離し、`pty_kill` が錠前待ちにならないようにした）が、**Tauri の同期コマンドは main thread で走る**（`tauri-macros` の `ExecutionContext::Blocking`。既定は `kind = "sync"`）ため、書き込みが返らなければ後続の IPC も処理されない。根治は「セッションごとの書き込みスレッド＋エラーを `Channel` で返す」だが、設計 決定13（書き込み失敗をタブの中に出す）の同期的なエラー経路を作り替える変更になる。**`#[tauri::command(async)]` は採ってはいけない**——複数の `pty_write` が待たれずに撃たれるので**打鍵が並び替わる** `[M17]`
- **起動待ちの入力の待ち行列に上限が無い**（`src/components/TerminalTab.tsx` の `pendingRef`）: 溜まるのは `spawn` が解決するまでの約1秒ぶんで、上限を設けると「どこから捨てるか」という新しい判断が要るため**意図的に持たない**。`spawn` が永遠に解決しない経路ができたときに踏む `[M17]`
- **端末の ANSI 16色は xterm の既定のまま**（`src/core/terminal/theme.ts`）: facet の役割トークン（19個。M21 で 12 → 15 → 18 になり、M27 の `rule-muted` で 19 になったが、ANSI の16色に対応物が無い事情は変わらない）に対応物が無いため**持たない判断**で、流し込むのは面・文字・カーソル・選択の4系統だけ。ライトの面での読みやすさは xterm の `minimumContrastRatio`（`TERMINAL_MIN_CONTRAST = 4.5`）に任せている。**実機で「色が寄りすぎて区別が付かない」と感じたら、値を下げるか16色を持つかを決め直す** `[M17]`
- **`describeSequenceIssueEffect` の `to-mismatch` 分岐が2条件を1つの説明文に束ねている**（`src/modules/sequence/markdown.ts`）: `to-mismatch` は「`self` に余分な `to`」と「非 `self` に `to` が無い」の2条件を1つの rule 名に束ねており、確認文はどちらでも「図には『（未解決）』というアクターが立ち、宛先を引けない矢印はそこへ向きます」と言う。**前者だけのファイルでは実際は（未解決）は1つも出ない。** また `missing-actor` が `from` 側だけのときも同じ文言が「宛先」と言うが、実際に（未解決）になるのは**送り手**の方である。到達性は低い（`setStepShape` が self 化のとき `to` を落とすため UI からは作れず、外部編集ファイル限定）。根治は rule を2つに割るか `describeIssueEffect` に data を渡す設計変更で、`markdown.test.ts` の「to-mismatch でも同じ説明になる」がこの粗い近似を固定している。M3 のスコープ外として繰り越し `[sequence-m3]`
- **一覧の並び順が、漢字の `title` では画面から説明できない**（`src/core/file-grouping.ts` の `sorted`）: グループ内は `displayTitle` の `localeCompare('ja')` 順で、**これは五十音順ではない**——漢字は ICU の照合順（部首・画数）で並ぶ（実測: 受注 → 返品 → 問合せ。読みなら 受注 → 問合せ → 返品）。読みはデータに無いので求めようがなく、順は決定的なので挙動は確定として据えた。ただし**M13 設計スペック決定5 が並び順の根拠に挙げた「画面に大きく出ているもので並ぶので、順の根拠が見て分かる」は、漢字の `title` については達成されていない**（実運用の名前はこちらが多い）。読み仮名を持たせる／手動の並び順を持たせる案は、必要が出てから検討する `[M13]`
- **読み方ガイド（`README-for-AI.md`）はプロジェクト固有に聞こえない質問には効かない**（`src/core/reading-guide.md`）: ガイドは**AI がフォルダを読みに行った場合にだけ**効く。素読み比較実験（M12 Task 4）で「『応募者』とはどういう意味？」と聞くと、AI はファイルを一切読まずに一般知識で答え、ガイドの有無に関係なく用語集に到達しなかった。文面の改善では直らない（読まれないファイルはどう書いても読まれない）ため、人間の裁定で「限界として記録」に確定した。対応候補として facet がユーザーの `CLAUDE.md` にも誘導を書く案があるが、ユーザーのファイルを触る侵襲性があるため、今回は実装せず将来の検討候補に留める `[M12]`
- **同期は「消せない要素があっても続ける」ので、置いた Skill が同梱物と一致している保証が best-effort に落ちた**（`src/core/skill-sync.ts`）: 置き直しの削除は直下の要素ごとに `try/catch` で握りつぶす（1つ消せないだけで Skill 全体を失う——「読む前に消す」と同型の永続破壊——を避けるため。mac の `.DS_Store` で実際に起きる）。代償として、**新しい版の Skill が消したはずの古いファイルが残り続ける経路ができた**。残骸は同名なら次の書き込みが上書きするが、名前が変わった旧スクリプトは残り、AI が古い手順書・古いスクリプトを読む余地がある。`console.warn` に「消せませんでした」と出るだけで**画面には何も出ない**（トーストに上げない判断は意図的——`.DS_Store` が消せないのは mac では正常なので、実バグと同じ文言で驚かせない）。**Skill からファイルを消す／改名する改訂を出すときに踏む** `[sequence-m4]`
- **`bundle.resources` は `evals/` も `node_modules` も除外できていない**（`src-tauri/tauri.conf.json`）: `collect()` の読み出し側は M15 で `node_modules` へ降りないよう塞いだ（下記「小さな負債」参照）ので実行時の性能・throw は解消済みだが、**同梱物そのものには入り続ける。** v1.0.0 のビルドで実測: `target/release/skills/` は **564 ファイル・1.39MB** で、うち **529 ファイル（約 1.2MB）が `glossary-term-register/node_modules`**（ajv）。サイズは小さいが、**ファイル数がインストーラのコンポーネント数を膨らませる。**
  - **M15 が残した「bundler がパターン除外に対応していれば1行で塞がる」という見通しは誤りだった**（v1.0.0 のリリース作業で `tauri-utils-2.9.3/src/resources.rs` の実物を読んで確認）。除外パターンは無く、**`*` を含むパターン（glob 経路）は target が `dest.join(path.file_name())` になってディレクトリ構造が潰れる**（同ファイルの `ResourcePathsInnerIter::Glob` 分岐。`*` を含まないディレクトリ指定だけが `WalkDir` で構造を保つ）。`collect()` は `$RESOURCE/skills/<skill>/scripts/...` を辿るので、glob で書き換えると読み出し側が壊れる。**塞ぐなら「必要なディレクトリを1つずつ列挙する」か「ビルド前に配布用の skills を組み立てて `resources` はそれを指す」のどちらか**で、前者は `shouldSyncSkillFile` が意図して採っている除外リスト方式（開発時点で名前を知らないファイルも既定で同梱する）と逆行する `[sequence-m4]`
- **MSI は作れない。v1.0.0 で `bundle.targets` から外した**（`src-tauri/tauri.conf.json`）: WiX の `light.exe` が **コードページ 1252 に無い文字を含む文字列を拒否する**（`LGHT0311`）ため、同梱 Skill の `evals/fixtures/` にある日本語ファイル名4本——`用語集.json` / `エラーカタログ.json` / `エラーメッセージ一覧.md` / `注文確定.json`——でリンクが落ちる。**利用者には配られない評価用 fixture が**（`shouldSyncSkillFile` が `evals/` を除外する）**バンドルにだけ入って MSI を止めている。** NSIS にこの制約は無く、そちらは通る。**MSI が要るのは GPO でのソフトウェア配布や、情シスが MSI しか受け付けない運用のときだけ**で、GitHub Releases から手で落とす配布では要らない。必要になったら上の項の「ビルド前に配布用の skills を組み立てる」を実装すれば同時に塞がる（`evals/` と `node_modules` が両方落ちる）。**その場合も「Skill 本体のファイル名が ASCII であること」は前提のまま**——`scripts/` や `references/` に日本語名が入れば再発する `[v1.0.0]`
- **`sequence` スキーマに `notes` 相当が無く、`failures` を空にした理由がファイルに残らない**（`schemas/sequence.schema.json`）: エラーカタログは「なぜ空にしたか」を `notes` に逃がせるが、シーケンスは逃がせない。登録 Skill は**報告文で伝える**運用にしたので、**会話が流れた時点で理由は失われる**——後からファイルを見た人には「未定義」と「未定義でよいと判断した」の区別が付かない。スキーマ改訂＋マイグレータを伴うため、設計スペックは「不便が実感されてから」と条件を付けている `[sequence-m4]`
- **更新後にアプリが自動で再起動するかが未確認**（`src/fs/updater.ts` の `install`）: Windows ではインストールの実行時に OS がプロセスを落とすため、成功しても `relaunch()` に到達しない見込み。`plugins.updater.windows.installMode: "passive"`（`src-tauri/tauri.conf.json`）では NSIS 側が再起動する想定だが未確認。**戻ってこなかった場合でも不具合として追わない**——確認ダイアログの文面（「更新後に facet を開き直してください」）が吸収する設計。実機で戻ってくることが確認できたら、その一文を削って `App.tsx` を直し、テストも合わせて直すこと `[M19]`
- **未署名の exe を updater が起動したときに SmartScreen が出るかが未確認**（`src-tauri/tauri.conf.json` の `plugins.updater.windows.installMode: "passive"`）: updater がダウンロードして起動するインストーラには mark-of-the-web が付かない見込みだが未確認 `[M19]`
- **課題ノードの見送りにキーボード経路が無い**（`src/modules/issue-tree/IssueTreeEditor.tsx`）: 見送り（`deferred`。issue-tree-m3 の後半で `deferredToMainDev` が廃止され、課題に付く判断はこの1種になった）はノードのボタンからしか付けられない。選定会議で数回だけ使う操作なので、キーを増やす側を優先しなかった——が、これは rev 10章の「**キーでしか到達できない意味を残さない**」の裏返しの穴である（マウスでしか到達できない意味が1つある）。**この項は当初「主修飾キー＋`Enter` は仮説の追加に割り当てたのでキーが空いていない」と書いていたが、その前提はもう成り立たない**——issue-tree-m5 Task 3 でこの写像は消え（`runIssueCommand` の `case 'toggle-item-state': return false`）、**課題ツリーで主修飾キー＋`Enter` は未割り当てである。** 塞ぎたければ空いているキーがある。ただし**そこへ割り当てるかどうかは決めていない**——課題の旗は見送り・解決の2種類あるのに割り当てられるキーは1つで、片方だけに与えると非対称になる（issue-tree-m5 の計画が「やらないこと」として明記した判断。rev 10章にも同じ理由が書いてある） `[issue-tree-m1]`
- **仮説を別の課題へ付け替える手段が無い**（`src/modules/issue-tree/commands.ts` の `moveHypothesis`）: `moveHypothesis` は同じ課題の中でしか動かず、課題をまたがない。**ただしこの項が述べているのは `commands.ts` の関数の制約であって、画面からできることの範囲ではない**——issue-tree-m5 以降、その `moveHypothesis` 自体がどの動線からも呼ばれていないので、**いまは同じ課題の中ですら並び替えられない**（別項「仮説と FB の並び替え、および『押した位置の次に挿す』手段が無い」を参照）。関数の制約だけを読んで「同じ課題の中なら並び替えられる」と受け取らないこと。移したくなったら消して作り直すしかない（`rationale` とイベント列は打ち直しになる）。**課題の並び替え（`moveIssueSibling`）が兄弟の中だけで動くのと同じ制約**だが、あちらは部分木ごと動くのに対し、仮説は持ち主が変わる操作なので `issueId` の書き換えと `hypotheses` 配列の正規化が要る `[issue-tree-m1]`
- **ID が重複しているファイルで、同じ仮説の行がどちらの箱にも描かれる**（`src/modules/issue-tree/layout.ts` の `rowsOf`）: 仮説の引き当ては **id 鍵**（`rowsOf.get(node.id)`）なので、同じ id の課題が2つあると**両方の箱が同じ行の一覧を受け取る**。**これは `src/core/canvas/flat-tree.ts` の「ID が重複していると、その ID を親に指すノードが**先頭の1つ**にだけ付く」とも、`derive.ts` の `suppressedIssueIds`（先に現れた方を採る）とも違う3つ目の慣習である。**issue-tree-m1 のときは `cardsOf` が key 鍵の Map を上書きしていたので「最後に歩いた方だけ」だった**が、issue-tree-m3 で仮説がカードから箱の中の行になり、**上書きではなく重複描画**に変わった（挙動は決定的で、純粋性も保たれている）。ID 重複のファイルを見た人には、木の寄り方と仮説の寄り方が食い違って見える。揃えるなら「先に現れた箱だけに付ける」側が既存に合う `[issue-tree-m1]`
- **「課題を追加」ボタンが DFS 末尾のノードを狙う**（`src/modules/issue-tree/IssueTreeEditor.tsx`）: 帯の「仮説を追加」は**最後にフォーカスがあった課題**（`lastCell`（`IssueTreeEditor.tsx:339`）から `lastIssueFocus()` で導く。issue-tree-m3 で `lastIssueKey` から改名・一般化された）に足すのに、「課題を追加」は末尾の課題の兄弟を足す。木が深いと**マウスだけの利用者には兄弟が木の奥に生まれる**ので、押した結果がどこに出るか予測しづらい。両方 `lastCell` 由来に揃える方が説明しやすい。上の「ロジックツリーにマウスだけでノードを増やす動線が無い `[M14]`」と同系の問題（あちらは動線が無い、こちらは動線の行き先が読めない） `[issue-tree-m1]`
  - **循環を含むファイルでは、このボタンが「描かれない課題」を作る。** 狙い先は `data.issues.length - 1`（配列の末尾）だが、**読み込んだファイルは正規化されない**——`orderFlatNodes` は根から到達できないノードを末尾へ寄せるので、循環を含むファイルの末尾は循環の一員である。`buildTree` は到達できないノードの `parents[i]` を `null` にするため `addSiblingIssueAfter` のルート分岐が発火し、**新しい課題は循環ノードを親として作られる**（＝根から到達できず、図に一度も出ない）。作られて保存はされるので、押した人には「ボタンが効かない」ように見える。**フォーカスは新しい課題を指す**（`d94c4a3` の参照同一性による引き直し）ので他人の文言を潰すことは無く、実害は「作った覚えのない課題がファイルに増える」ことに留まる。**直すのは上と同じ `lastCell` 由来（`lastIssueFocus()`）への切り替えで足りる**（フォーカスのある課題は必ず図に描かれているノードなので、末尾を狙うより安全）。ただし切り替え自体が後続マイルストーンの判断なので、ここでは記録だけ残す `[issue-tree-m1]`
- **`issue-tree-register` の書き出しスクリプトが配列順を整えない。そして「アプリが後で整える」も、開くだけでは起きない**（`.claude/skills/issue-tree-register/scripts/issue-tree-write.mjs`）: アプリ側の `normalizeOrder`（`issues` を DFS 行きがけ順、`hypotheses` をぶら下がり先の課題順に並べる）は値 import を持つのでバイト一致コピーにできず、手複製すると追従漏れがテストに映らないため、**スクリプトは配列順を触らない**（キー順・インデント・改行は正規形で書く）。**Skill が書いた直後のファイルは、配列順だけが正規形になっていない。** 整うのは**アプリで編集して自動保存が走ったとき**だけで——`normalizeOrder` は `src/modules/issue-tree/commands.ts` の編集コマンドからしか呼ばれず、読み込み経路（`module.ts` の `migrate: migrateIssueTree` が並びを触らない。issue-tree-m3 で 1 → 2 の変換が入ったが、変えるのは `schemaVersion` の値だけである）には無い——**開くだけでは並びは整わない。** 帰結は、乱れた順がディスクに残り続け、**利用者が後で1箇所を直した瞬間に配列全体が並び替わって大きな無意味 diff が出る**こと（Git diff を仕様の変更履歴として読めなくする。rev 5章）。**当座は SKILL.md が「DFS 行きがけ順で書け」と勧めることで抑えている**（要求ではなく勧め）。塞ぐなら「`normalizeOrder` を値 import 無しに書き直してコピー対象に加える」か「アプリの読み込み経路でも並べ替える」のどちらかで、後者は「読み込み・閲覧では書き戻さない」原則（rev 5章）と正面から衝突する `[issue-tree-m2]`
- **判断の誤操作（同じ種別を続けて付ける・最新以外を消せない）を塞いでいない**（`src/modules/issue-tree/commands.ts` の `appendJudgement`）: 仮説の `events` は追記専用なので、同じ判断を2度選べば同じイベントが2つ積まれ、**取り消せるのは最新の1件だけ**（根拠を編集できるのも最新の1件だけ）。**課題側は対象外になった**——見送りがトグルになり（`toggleDeferral`）、押し直せば最新の1件が消えるので、誤って付けた見送りはその場で戻せる。issue-tree-m3 の批評でも挙がったが、**FB（`pendingNotes`）の枠組みが入って意味が変わった**——「判断を変えない FB は何件あってもよい」ので、積み重なった同種のイベントが本当に邪魔なのかは実機で見ないと分からない。**直す前に一巡して観察すること**（推測で弾くと、正当な「同じ判断を再確認した」記録まで塞ぐ） `[issue-tree-m3]`
- **`listOpenTargets` が「図に描かれない課題」を行き先から外していない**（`src/modules/issue-tree/open-targets.ts`）: ぶら下がり先の課題が無い仮説（参照切れ）は意図して列から外してある——描かれていない物へ飛んでも視点が動かず、動かないと `lastCell` が変わらないので**その種類のチップが以後ずっと同じ答えを返して巡回が止まる**ため。ところが**課題の側に同じ穴が開いている**: 循環に入っていて根から到達できない課題は `layout.issues[i] === null`（＝描かれない）だが、そこからは「仮説なし」の行き先がそのまま出る。実害は壊れたファイルに限られ（整合性検証が赤くする）、症状は「チップを押しても何も起きない」。**仮説側のコメントにこの非対称が書かれていない**ので、直すときはコメントも揃えること `[issue-tree-m3]`
- **`load.ts` が「整数でない／1未満の `schemaVersion`」を既知の旧版として移行経路に入れる**（`src/core/load.ts:99-104`）: 判定は `version > module.schemaVersion` で新しすぎるものを弾いた後、残り全部を旧版として `module.migrate` に渡す。したがって `0` や `1.5` や `-1` も移行される。**issue-tree の 1 → 2 は `schemaVersion` を 2 に書き換えるだけ**なので、これらは移行後にスキーマ検証を通ってしまい、**壊れた版番号のファイルが黙って開く**（`NaN` は `typeof number` だが比較が false になり、こちらは安全側に落ちる）。issue-tree-m3 でこの経路に初めて実装が入るまでは踏みようがなかった穴である。`Number.isInteger(version) && version >= 1` の1行で塞がる `[issue-tree-m3]`
- **バナー（`IssueBanner`）から該当行へのジャンプが無い**（`src/App.tsx` / `src/components/IssueBanner.tsx`）: メッセージは `#N`（`rowRef`）で行を指すが、押しても飛ばない。`IssueBanner` は App 側にあり、各モジュールのエディタが持つ `jumpToMissing` 相当の配線が要る（M22 設計スペック 決定4） `[M22]`
- **欠落ジャンプは4モジュールが巡回 ref で、フォーカス位置を起点にしない**（`src/modules/glossary/GlossaryEditor.tsx` / `src/modules/error-catalog/ErrorCatalogEditor.tsx` / `src/modules/logic-tree/LogicTreeEditor.tsx` / `src/modules/sequence/SequenceEditor.tsx` の `jumpAt`）: `useRef` で「前回どこまで巡ったか」を種類（`kind`）ごとに覚えて次を返す形なので、**ユーザーが手でフォーカスを別のセルへ移しても巡回はそこから再開しない**（`SequenceEditor.tsx` の `jumpToMissing` 直上のコメントが「フォーカス位置は起点にせず巡回 ref で数える（用語集と同じ。課題ツリーの `nextOpenTarget` とは違う）」と明記している）。**課題ツリーだけ方式が違う**——`IssueTreeEditor.tsx` の `goToNextOpen` は `nextOpenTarget(listOpenTargets(...), kind, lastFocus)` を呼び、`lastFocus` は `onFocusCapture` で更新される実際のフォーカス位置なので、**フォーカス位置を起点に「次」を導く**（`open-targets.ts` の `nextOpenTarget` が `current`＝`lastFocus` から `findIndex` で位置を特定してから +1 する）。物足りなければ、他4モジュールも課題ツリーの方式へ揃える `[M22]`
- **ドロップダウンの「名前が空のアクター」は面だけの空チップで示す**（`src/modules/sequence/ActorRefCell.tsx` の `DropdownMenuItem`）: `aria-label="名前が空のアクター"` で読み上げには対応したが、見た目は `h-4 w-16` の破線＋淡い面だけの矩形で、文字が無い。実機で押しにくい・見つけにくいと出たら再考する `[M22]`
- **エラーカタログの集計はプロファイル非依存だが、ジャンプは表示中のセルだけに飛ぶ**（`src/modules/error-catalog/ErrorCatalogEditor.tsx` の `jumpToMissing` / `TALLIED_FIELDS`）: `tallyMissing` は全 `errors` を対応レベルに関わらず数えるが、`jumpToMissing` は `TALLIED_FIELDS` の中で `isMissingCell` が真になる最初のフィールドへ `focusCell` するだけで、**現在のプロファイル（サポート向け／開発者向け）に表示されていない列（例: サポート向け表示中の `causeForSpec`）は押しても実際には見えない場所へフォーカスしようとする** `[M22]`
- **エラーカタログの読み手（プロファイル）が3箇所で別々に選ばれる**（`src/modules/error-catalog/ErrorCatalogEditor.tsx` の表示プロファイル切り替え・`ExportMenu` のドロップダウン・`TableCopyDialog` の「読み手」選択）: 画面に出す列・Markdown 出力・表形式コピーがそれぞれ独立に「サポート向け／開発向け」を持っており、3箇所の選択は連動しない。**M29 は絞り込みの範囲（見た目のまま）を3つの口で揃えたが、読み手はまだ揃えていない**——画面をサポート向けで見ながら開発向けの表をコピーする、といった食い違いがそのまま起きる `[m29]`
- **Miro 書き出しのノード幅は概算で、フォントが変われば折り返す**（`src/modules/logic-tree/miro-export.ts` の `EM_WIDTH`(16) / `EN_WIDTH`(9) / `PADDING_X`(28)）: 全角・半角ごとの固定幅を文字数ぶん積算するだけの近似で、実フォントを測っていない（キャンバス本体の測定層とは別系統）。11回の貼り付け実験のうち実験8→9でこの係数を上げて折り返しを解消した経緯があり（[`../superpowers/plans/2026-08-29-logic-tree-m3-miro-clipboard-design.md`](../superpowers/plans/2026-08-29-logic-tree-m3-miro-clipboard-design.md)）、Miro 側の既定フォントが変わって再び折り返しが出たら同じ要領で係数を上げる `[logic-tree-m3]`
- **循環で根から到達できないノードは、Miro 出力・Markdown 出力の両方から警告なく落ちる**（`src/modules/logic-tree/miro-export.ts` の `nodesToMiroPayload` と `src/modules/logic-tree/markdown.ts` の `logicTreeToMarkdown` が、どちらも `buildTree` の `roots` からしか辿らないため）: **参照切れ（親 ID が存在しない）は落ちない**——`buildTree` は参照切れノードをルートへ繰り上げる（`missingParent` に記録するだけで、到達不能を表す `unreachable` には入れない）ので、両出力にそのまま現れる。**落ちるのは循環（`unreachable`）だけ**。facet 側のデータは無傷で、整合性検証の赤表示は既に出ているが、Miro へコピー・書き出しした結果にも、Markdown 出力の図・箇条書きにも、循環に含まれるノードが1つも現れない。壊れたファイル（外部編集で作られた循環）でのみ起こり、アプリの通常操作では作れない。レビューで指摘されたが、spec にも計画にも要求が無いため logic-tree M3 では直さなかった `[logic-tree-m3]`
  - **M29 の表形式コピー（`src/modules/logic-tree/table.ts`）にも同じ穴がある。** `logicTreeToTable` も `buildTree(data.nodes).roots` からしか辿らないので、循環に含まれるノードは表からも警告なく落ちる。**新しく作った穴ではなく、既存2つの出力と挙動を揃えた結果**——3つの出力（Miro・Markdown・表）のうちどれかだけが循環を拾う／拾わないという食い違いを作らない判断で、直すなら3箇所同時になる `[m29]`
- **表に貼ると、`=`／`+`／`-`／`@` で始まるセルが数式として実行されうる**（`src/core/table-tsv.ts` / `src/core/table-html.ts`）: セルの先頭文字がこの4つのいずれかだと、TSV・HTML どちらの経路で貼っても Excel・Google スプレッドシートがそのテキストを数式として評価する（いわゆる CSV/TSV インジェクション）。RFC 4180 の引用符付けはセル区切りの混入を防ぐだけで、この種類の実行は防がない。データの出所は利用者自身の仕様 JSON だが、それらは会話の書き起こしから Skill が書くものであり、**`table-tsv.ts` 自身の JSDoc が採っている「書き手を信用せず」escape する姿勢と地続きの入力**である。**意図して直していない**——よくある対策（先頭に `'` や半角スペースを足す）は貼り付けた文字列そのものを汚し、設計の狙い（「貼り先の書式に馴染ませる」）と正面から矛盾するため。直すなら、黙った書き換えにせず、利用者に見える選択にすること `[m29]`
- **`readClipboardText` は「クリップボードが空」と「読み取りに失敗した」を区別せず、どちらも空文字に潰す**（`src/fs/clipboard.ts`）: プラグインの `readText()` が投げる例外は、メッセージ文字列を解析しないと両者を区別できない（**非公開の文言への依存は新しい負債になる**ので見分ける改修はしていない）ため、`try/catch` で一律に空文字へ倒す。結果、`TerminalTab` の貼り付け失敗の経路（`onError` → トースト。設計 §7.3）は**本番では到達不能**になっている——`clipboard-manager:allow-read-text` が capabilities から外れると、右クリック貼り付けは**画面に何も出さないまま無反応になる**。**無反応を見たら、まず capabilities の権限を疑うこと** `[M28]`
- **起動時（および起動中の会話差し込み）は「bracketed paste mode が有効になった（xterm が解析した `term.modes.bracketedPasteMode`）、かつ出力が `INSERTION_QUIET_MS`（500ms）途切れた」の両方を待ってから流す**（`src/components/TerminalTab.tsx`。上限 `INSERTION_MAX_WAIT_MS` は8秒）: 当初は「2004（bracketed paste mode）の到来だけを待てば足りる」としていたが、`claude` は端末を raw mode にする初期化の一環として **REPL の入力欄を作るより前に** bracketed paste を有効にするため、2004 単独は「貼り付けを受け付ける準備ができた」の必要条件ではあっても十分条件ではなかった（実機の診断トーストで確定。設計 §2.1、詳細は [`history/m28-core-claude-pane-handoff.md`](history/m28-core-claude-pane-handoff.md)）。**`INSERTION_QUIET_MS`（500ms）は実機の1環境でしか確かめていない**——`claude` の起動が異常に遅い環境や、出力が断続的に続く環境では、**500ms の静穏が早すぎて `@` の補完にまだ食われる**か、**上限8秒に当たって静穏を待たず従来どおり差し込まれ末尾のスペースが食われる**症状のどちらかに戻りうる。**症状が戻ったら、まずこの2定数を疑うこと** `[M28]`
- **存在しない `ask` を指す FB を、整合性検証が見ていない**（`src/modules/issue-tree/consistency.ts`）: `feedbacks[].askId` が実在しない問い（`ask_...`）を指していてもレベル1・レベル2のどちらも通る——スキーマは `askId` を「文字列か null」としか制約せず（`schema.test.ts` の「循環・多重ルート・参照切れのファイルはスキーマ検証を通る」がこれも意図して許している）、`consistency.ts` も突き合わせをしていない。`feedbacks` は id を持たず添字で扱う設計（`asks` だけが参照される側として id を持つ）なので参照は片方向だけだが、**参照切れの FB は「答えたはずの問いがいつまでも FB待ちのまま」という形で静かに要対応を残す**——`poseQuestions` の FB待ちは「文言のある問いに紐づく FB が0件か」を見るので、`askId` が消えた問いを指していても、その問いには変わらず FB待ちが立つ。仮説の `issueId` の参照切れ（`missing-issue`）と同じ形で `dangling-ask` のようなルールを足せば塞がる `[issue-tree-m4]`
- **`date` が「いつ言われたか」を保証しない**（`src/core/today.ts` / `src/modules/issue-tree/commands.ts`）: アプリが刻むのは**追記した日**（`todayString()` の既定引数）であり、会議の日が別なら食い違う。登録 Skill は会話から実際の日付を取れるが、アプリには手で直す欄が無い——**意図的な設計**（手入力の欄は更新忘れで嘘をつく、というミュータブルなステータス欄を捨てたのと同じ理屈）。過去の日付を記録したい要望が出たら、そのとき形を決める `[issue-tree-m4]`
- **仮説と FB の並び替え、および「押した位置の次に挿す」手段が無い**（`src/modules/issue-tree/commands.ts` / `IssueTreeEditor.tsx`）: issue-tree-m5 でキーボードから仮説側の編集操作を全廃したとき、**4本の関数がまとめて到達不能になった**——`moveHypothesis`（仮説の並び替え。旧 `Alt+↑↓`）／`moveFeedback`（**FB の並び替え**。同上）／`addHypothesisAfter`（仮説を押した位置の次に挿す。旧 `Ctrl+Enter`）／`addFeedbackAfter`（**FB を押した位置の次に挿す**。同上）。代わりのマウス動線を作ったのは**追加と削除だけ**で、しかも追加は必ず末尾に付く（節末の「聞きたいことを追加」「FBを追加」・ノード末尾の「＋仮説を追加」）。結果、**仮説も FB も追記順のまま並び、間に挿すことも入れ替えることもできない。** 4関数は `commands.ts` に残してある（将来の並び替え UI で戻る操作なので消していない。テストも `commands.test.ts` に残る）——`grep -rn --include=*.ts --include=*.tsx -E "\b(addHypothesisAfter|addFeedbackAfter|moveHypothesis|moveFeedback)\b" src/ | grep -v "\.test\." | grep -v "^src/modules/issue-tree/commands.ts:"` が0件であること（定義とテストを除く呼び出しが無いこと）で確かめられる。**`-E` と `\b` を外さないこと**——`"…\|moveFeedback"` の形だと `removeFeedback`（生きている関数）に部分一致して、無関係な行が出る。要望が出てから、どの操作（ドラッグ／ボタン）で並び替えるかを決める `[issue-tree-m5]`
- **仮説0本の課題には、末尾の「＋仮説を追加」ボタンが出ない**（`src/modules/issue-tree/IssueBox.tsx` の `placement.addHypothesis`）: issue-tree-m5 Task 2 の修正で「仮説を1本も持たない課題は展開しない」ことにしたため、開けない箱の中に置く末尾ボタンの置き場所が無い。**その課題に仮説を足す道は、帯の「仮説を追加」ボタン（最後に触った課題に足す）だけが担う**——課題のタイトルに触ってから帯のボタンを押せば足せるので、動線そのものは残っている `[issue-tree-m5]`
- **別種のチップを押すときの起点が、問いの欄にいるときだけ列の先頭へ落ちる**（`src/modules/issue-tree/IssueTreeEditor.tsx` の `LastCell` / `lastFocus`）: issue-tree-m5 Task 8 で問いの欄（`ask`）を巡回できるようにしたとき、起点 `lastFocus` を導く `LastCell` に `{cell:'ask'}` を新設した。`detail` / `value` / `feedback` / `event` はどれも「仮説の行」（`{cell:'hypothesis'}`）へ潰れて他種のチップと `cell` が一致するのに対し、**`ask` だけは潰れない**ため、問いの欄にフォーカスがある状態で「未決」「保留」など仮説行ベースのチップを押すと `findIndex` が -1 を返し、列の先頭へ戻ってから進む。**一巡の不変条件は壊れない**（2回目以降は正しく進む）が、意図した設計の帰結であることの註もテストも無い `[issue-tree-m5]`

## 性能

- **編集1打鍵ごとに全ファイルの `checkConsistency` を再実行している**: 数百レコード規模では問題ないが、規模が増えたら選択中ファイルだけの差分計算に変える `[M2]`
- **textarea の高さ計算が `rows=1` に戻して `scrollHeight` を読む強制リフローを起こす**（`src/components/CellInput.tsx`）: `multiline` のセル（定義・備考）はこの測定を初回マウント時の `useLayoutEffect` で行い、さらに `ResizeObserver` が幅の変化のたびにも呼ぶ（測定点は初回だけではない）。コストは行数に比例し、上限は `MAX_ROWS = 8`（M10 で 5→8）。M10 の実機確認では体感は出ていない。数百行規模で体感が出たら、上の `checkConsistency` と同じく差分計算へ `[M8]`
- **ノードの測定結果のキャッシュが上限で全消しになる**（`src/modules/logic-tree/LogicTreeEditor.tsx` の `MEASURE_CACHE_LIMIT`）: LRU ではなく `cache.clear()` なので、2000件に達した直後の1フレームだけ**全ノードを測り直す**。木が大きいほど、その1フレームだけ引っかかる。フォント変更でも作り直す設計なので、上限に達するのは長い編集セッションの後になる `[logic-tree-m1]`
- **`lastCell` が state なので、セルにフォーカスが移るたびにエディタ全体が再描画される**（`src/modules/issue-tree/IssueTreeEditor.tsx:339`。issue-tree-m3 で `lastIssueKey` から改名・一般化され、帯の「仮説を追加」とチップの「次へ」の**2つの動線がここ1つから出る**ようになった）: 保持しているのは「最後にフォーカスがあったセル」だけで、**読むのはハンドラの中だけ**（`lastIssueFocus()` ← `addHypothesisFromBanner`、および `goToNextOpen`。`grep -n "lastFocus" src/modules/issue-tree/IssueTreeEditor.tsx` の読み手は2箇所ともハンドラの中）なので `ref` で足りる——ただし派生値の `lastFocus` はレンダ本体で作っているので、`ref` へ移すならその導出もハンドラの中へ動かすことになる。木が大きいほど1打鍵ごとの再描画が重くなる（測定器のキャッシュは効くので測り直しは起きない） `[issue-tree-m1]`

## アクセシビリティ

- **列幅ハンドル（`role="separator"`）が `aria-valuenow` / `aria-valuemin` / `aria-valuemax` を持たない**（`src/core/column-resize.ts` の `HandleProps`）: ハンドルは `tabIndex={0}` で Tab 順に入り ←→ で動く（＝WAI-ARIA の window splitter）が、**現在の幅がスクリーンリーダーに伝わらない**ので、操作しても何が起きたか分からない。値は store が持っているので、`getHandleProps` が返す props に3つ足すだけで塞がる `[M8]`
- **帯のチップの `aria-label` が、見えているラベルを含んでいない**（`src/modules/issue-tree/IssueTreeEditor.tsx` の「次の◯◯へ」）: チップの見た目は「未決 2」だが、アクセシブル名は「次の未決へ」で、**可視の文字列（とくに件数）が名前に入っていない**。WCAG 2.5.3（Label in Name）に照らすと、音声入力で見えているとおりに言っても当たらない。**計画がこの文言を指定しているので実装の逸脱ではない**が、他のチップ状の操作（`KeyHints` の帯など）と足並みを揃えるかは決めていない `[issue-tree-m3]`
- **シーケンスの答えスロットの `aria-label` に状態（考慮不要／未回答）の語が含まれない**（`src/modules/sequence/GutterSlot.tsx` / `SequenceEditor.tsx`）: `aria-label` は「ステップNの答え: 問いの文言」の形で、**画面に出る「考慮不要」の接頭は `aria-hidden="true"` の `<span>`**（`GutterSlot.tsx:59`）にしか無く、名前には現れない。スクリーンリーダーの利用者は、未回答か考慮不要かを面（見えない）や語（読み上げられない）のどちらからも判別できない `[M22]`

## デザイン

- **方眼背景がキャンバスのズームに追従しない**（`src/modules/logic-tree/LogicTreeEditor.tsx`）: `bg-grid-paper` はビューポート（transform を当てる要素）の**外側**に敷いてあるので、拡大しても升目の大きさが変わらない。**rev 9章「地は方眼、作業する面は無地」の意図には合っている**（方眼は地の模様であって図の一部ではない）が、キャンバスとしては**倍率の手がかりを失っている**——どれだけ拡大しているかが画面から読めない。倍率表示を出すか方眼を内側へ移すかは、実機で「今どれくらいの倍率か分からない」と感じたときに決める `[logic-tree-m1]`
- **空欄 `Backspace` のステップ削除に誤爆の不安があると実機で観察された**（`src/modules/sequence/SequenceEditor.tsx`）: 設計は「消しても `Undo` が受け皿」で確定済みだが、体感として不安が先に立つ場面があった。確定設計を変える理由にはならないが、体感と設計判断の差として記録する。詳細は [`history/sequence-m1-keyboard-editor.md`](history/sequence-m1-keyboard-editor.md) の「実機確認で出た観察」② `[sequence-m1]`
- **「図を作る」→「失敗考慮を打つ」の二段階ワークフローと `Tab` の2ゾーン化**（`src/modules/sequence/SequenceEditor.tsx`）: 骨格（from/to/種別/ラベル）を打ち切ってからガターの答えを埋め直す使い方が多く観察された。`Tab` を骨格ゾーン→答えゾーンの2ゾーンに分けられると自然という提案。**答えへのキーボード到達は必須のまま崩さないこと。** 詳細は同「観察」⑤ `[sequence-m1]`
- **UI ノートの D（レイアウト固定）は幅だけが閉じた**（同 D3）。**ノード幅の固定は M24 で実施した**——課題ツリー・ロジックツリーとも全ノードが 320px 固定になり、右上のバッジは列ごとに縦一列に揃う（`ISSUE_MAX_WIDTH` / `ISSUE_TITLE_MIN_WIDTH` / `NODE_MIN_WIDTH` は消えた）。**rev.3 が優先度を引き上げた根拠（スキャン性）は、これで満たされている。** `[M24]`
- **テーブル（用語集・エラーカタログ）の行高固定＋2行省略は、M24 で「変えない」と決めた**（同 D3 の前半。人間の裁定）。`CellInput` の `MAX_ROWS = 8` に付いた「行の高さが揃わなくなるが、読めないよりよい」（M10 決定17）を反転させることになり、かつ「省略した全文をどこで読むか」の答えを新設する必要があるため。**これは先送りではなく判断である**——蒸し返すなら、まずその2点に答えを持ってくること `[M24]`
- **ダークの意味色まわりは実機確認待ち**（`src/styles/palette.css` の `.dark` ブロック）: 机上の吟味（`invalid-face` L 0.28・`judge-yes` C 0.08）と、実機確認の一巡目——無彩色の面の序列の作り直し（`surface` 0.24／`surface-muted` 0.13／`grid` 0.20）と `rule-muted` の分離——は済んだ（UI ノート D20）が、**意味色系の項目**——「暗い面で黄土色の破線が付箋に見えるか」「`judge-yes` が暗い地で浮きすぎないか（canvas 比 10.6:1）」「`invalid`（L 0.68）と `missing`（L 0.82）が実機で分かれて見えるか」「淡い面3つが `surface` 0.24 の上でも浮いて見えるか」「端末」「ライトに戻して崩れが無いか」——は、面の序列が変わった状態で改めて見る必要がある。チェックリストは [`history/m27-core-dark-palette.md`](history/m27-core-dark-palette.md)。**浮きすぎと出た場合の調整は識別との綱引きになる**（D20 の知見——`judge-yes` の L は彩度を保ったまま動かせないので、fg 反転まで含めた構図の再設計になる） `[M27]`
- **`invalid` がライトで暗い赤（L 0.38）なのは、D型色覚で `missing` と分けるためである**（`src/styles/palette.css`）: 黄（`missing` L 0.49 / H 85）と赤（H 30）は **D型では色相がほぼ重なる**ので、同じ明度に置くと OKLab の色差が 0.013 しか出ない（`contrast.ts` の `simulate` で実測）。明度を一段落として `DISTINCT_MIN` の 0.10 を満たしている。**実機で「黒っぽくて赤に見えない」と出たら、`DISTINCT_MIN` を 0.08 に下げて L 0.42 前後へ戻す**——そのときは `palette-requirements.ts` の定数の隣に実測値と理由を書くこと（閾値を黙って消さない） `[M21]`
- **展開中の仮説が課題の列全体を押し広げるため、深い木では横スクロールが増える**（`src/modules/issue-tree/layout.ts`。展開中は箱が 360px → 780px に広がる。**畳んだ幅は m5 の実機確認で 320 → 360 に伸びた**ので、押し広げの幅は 460px から 420px にわずかに縮んだ）: 仮説の展開を「行の高さだけでなく箱の幅も変える」設計にした帰結で、木が深いほど、遠くの列で仮説を開くたびに全体の横幅が伸びる。issue-tree-m5 の計画段階で挙がっていた前提2の受け入れ済みトレードオフであり、直すなら展開パネルを別レイヤ（オーバーレイ等）に持ち上げる設計変更になる。実機で体感が悪ければそのとき検討する `[issue-tree-m5]`

## 小さな負債

- **`src/components/button-styles.ts` の `buttonBase` はまだ `disabled:opacity-50` を持っている**（`src/components/button-styles.ts`）: M29 のフォローアップで額縁の出力ボタン5本（`ExportMenu`・表形式でコピー・Miro へコピー／から取り込む）は `disabled` 属性をやめ、`aria-disabled` ＋ `text-ink-faint` / `border-rule-muted` の見た目に切り替えた（`disabled` はポインタイベントを丸ごと落とすので、押せない理由を説明する `title` のツールチップが出なくなるため）。**`buttonBase` を土台にする側（チップ・一覧の行・トーストの操作）は今も `disabled:opacity-50` のまま**——押せない状態の見た目が額縁の出力ボタンとそれ以外で2種類並立している。M29 が変えたのは「理由を説明する価値がある」出力ボタンだけで、残りは意図して手を付けていない `[m29]`
- **配布物（インストーラ）にサードパーティのライセンス表記が同梱されていない**（[`../THIRD-PARTY-NOTICES.md`](../THIRD-PARTY-NOTICES.md) / `src-tauri/tauri.conf.json` の `bundle.resources`）: OFL のフォントは **M7 でラテン書体をバンドルした時点から配布物に含まれており**、M26 で書体が IBM Plex 3書体に替わっても形は同じ——**表記の実体はリポジトリにしか無く、インストーラで受け取った人はライセンス文に到達できない。** OFL-1.1 は埋め込みを許諾するが、**同梱物の表示を求める形の条項を持つ**ので、リポジトリを見ない配布経路では満たしているとは言いにくい。M26 で新しく生まれた欠陥ではなく、**書体を替えたときに目に入っただけ**である。直すなら `bundle.resources` に `THIRD-PARTY-NOTICES.md` を足す1行と、アプリから開く導線（あるいはインストール先に置くだけ）を決めるだけ `[M26]`
- **用語集の別名バッジが `Badge` に寄せられていない**（`src/modules/glossary/AliasCell.tsx:231` の `border-grid` の `<span>`）: M21 で状態のバッジは `src/components/Badge.tsx` に一本化したが、**別名のタグだけは寄せていない**——これは「未決」「無効」のような**状態**ではなく**データの値**そのものなので、`BadgeVariant` の8語（`open` / `hold` / `invalid` / `pending` / `yes` / `no` / `deferred` / `faint`）のどれにも当てはまらない。無理に当てると「意味を variant で受ける」という部品の契約が壊れる。**`Badge` に `neutral`（意味を持たない値のタグ）を足すかは、同じ形が2件目に出たときに決める** `[M21]`
- **表記ゆれの「指摘（suggestion）」の波線下線は、規約に書いたが実装が無い**（rev 9章 無効軸）: 「無効は実線、指摘は波線下線（弱形）」という強度差は M7 から予約されていて M21 も規約として書いたが、**そもそも suggestion の検出自体がまだ無い**（表記ゆれ検知エンジンがコアに無い。「将来の機能を作った瞬間に踏むもの」のモジュール規約8の項を参照）。**予約は消さない**——検知を作る人が「無効と同じ実線で出す」を選ぶと、レベル2の赤表示との強度差が失われるため `[M21]`
- **`FileList.dom.test.tsx:45` のコメントが古い**（`src/components/FileList.dom.test.tsx`）: 「ボタンのラベルは『＋ 用語集を新規作成』なので部分一致で引く」と書いているが、M14 で「＋」は文字ではなくアイコン（`Plus`）に変わった。テストの可読性のみの実害 `[M14]`
- **`docs/README.md` の履歴表に M12 の行が無い**（M11 → M13 と飛ぶ。sequence-m2 / sequence-m3 は追記済み——2026-08-15 の棚卸しで確認）: 表は完了処理で手で足す運用（[`../CLAUDE.md`](../CLAUDE.md)「マイルストーン完了時に触る3箇所」は `history/` の新規作成までしか義務化しておらず、表への追記は README 側の慣習）なので、**落ちても誰も気づかない**。`history/` にファイルはあるので記録は消えていないが、**表だけを見た人にはそのマイルストーンが存在しないように見える。** 気づいたマイルストーンが自分の行と一緒に足すか、`history/` の一覧から表を生成する形にするか `[M13]`
- **`TerminalTab` の根 div が `flex` と `hidden` を同時に出している**（`src/components/TerminalTab.tsx`）: `className={`flex min-h-0 flex-1 flex-col ${hidden ? 'hidden' : ''}`}` は非表示時に `flex` と `hidden` の両クラスを同時に持つ。`src/App.tsx` は同じ問題を三項（`` `${paneOpen ? 'flex' : 'hidden'} ...` ``）で避けており、「display は排他なので三項で切り替える（`hidden` と `flex` を並べてもどちらが勝つかは出力順まかせになる）」というコメントまで書いてある。`TerminalTab` はその警告どおりの形になっており、いまは Tailwind の出力順で `hidden` が勝って動いているだけ `[M11]`
- **用語テーブルの `<th>` に `sticky` と `relative` が同時に付いている**（`src/modules/glossary/GlossaryEditor.tsx`）: どちらも `position` なので、カラム名が固定されているのは Tailwind が `sticky` を `relative` より後に出力しているからにすぎない。`sticky` 自体が絶対配置の包含ブロックになる（列幅ハンドルはそれに乗っている）ので `relative` は不要。**出力順が変わると固定が静かに外れ、原因は読み手に自明でない** `[M8]`
- **エディタのキー処理が用語集とエラーカタログで二重化している**（`GlossaryEditor.tsx` / `ErrorCatalogEditor.tsx`）: `runCommand` の switch・`onCellKeyDown`・`textFieldContext`・セルの面のクラス定数（計 約80行）がほぼ同一。M10 は意図的に複製した——いま抽象を決めても、3本目（ロジックツリーは列を持たない図系）が必要とする形と一致する保証がないため（M9 決定1が万能フックを退けたのと同じ理由）。**3本目が列を持つツール（状態遷移の遷移表など）だったら、その時点で引き上げる。** 判断材料は「2本の差が3点（プロファイルトグル・列幅ストア2本・吸収列）に収まっているか」——なお 2026-08-15 の棚卸し時点で4点目の差が既にある（描画構造: ErrorCatalog は `cellNode` 1関数で列を回すが、Glossary は各列を手書きで並べる） `[M10]`
- **`focusSibling` が `@/core/canvas/flat-tree.ts` の `siblingsOf` と同一の式**（`src/modules/logic-tree/LogicTreeEditor.tsx`）: 「次の写経で3本目が生える」。M20 の移設で `siblingsOf` は `@/core/canvas/flat-tree.ts` へ引き上げられ、**既に export 済みになった**——あとは `LogicTreeEditor.tsx` が `focusSibling` の代わりにそれを呼び替えるだけで潰せる `[logic-tree-m1]`
- **登録5 Skill は整合性検証の警告文言・計上規則を、アプリと独立に複製している——現在は実行 smoke テストで縛られている**（`.claude/skills/{glossary-term-register,error-catalog-register,sequence-register,issue-tree-register,logic-tree-register}/scripts/*-write.mjs`）: `consistency.ts` は値 import と `@/` エイリアスを持つため `canonical.ts` のようにバイト一致コピーへ寄せられず、各 Skill は判定ロジック・文言を手で複製している。**M15 で当時の3本とも `src/modules/<tool>/skill-write.smoke.test.ts` を新設し**、`*-write.mjs --check` を実際に spawn して、アプリの `checkXxxConsistency` が返す `message` がその stdout に逐語で現れることを検査するようにした——ズレた瞬間にテストが赤くなる。エラーカタログ・用語集の重複ルールは M15 時点で実際にズレていた（文言・計上規則とも）ものを追従させ、sequence は元々一致していたことを実測して固定した。**issue-tree-m2 で足した4本目（`issue-tree-register`）も同じ smoke テストで縛ってある**（`src/modules/issue-tree/skill-write.smoke.test.ts`）——こちらは `rule` 名の集合（5種類）に加えて **`message` が6件であること**も固定している（課題ツリーの `duplicate-id` は課題側と仮説側の2箇所で出るので、集合だけだと片方を落としても緑のままになる）。**logic-tree-m2 で足した5本目（`logic-tree-register`）も同じ smoke テストで縛ってある**（`src/modules/logic-tree/skill-write.smoke.test.ts`）。**ただしロジックツリーだけは複製の量が少ない**——`checkLogicTreeConsistency` の4ルールのうち3つ（`cyclic-parent` / `missing-parent` / `multiple-root`）の**判定そのものは `buildTree` の戻り値であり、その `buildTree` は `flat-tree-core.ts` のバイト一致コピーが持っている**（rev 4章）。したがってこの Skill が手で複製しているのは**警告の文言と、集計行の組み立てだけ**である。**縛られたのは「一致していること」であって「複製そのもの」ではない**——`consistency.ts` のルール・文言を改訂するたびに5スクリプトも書き替える必要がある点は変わらないが、追従漏れは次の `npm test` で検知される `[Skill]`

- **`palette-fit.mjs` が Node の型ストリップに依存している**（`.claude/skills/palette-retheme/scripts/`）: `.mjs` から `src/styles/contrast.ts` を直接 import しており、型ストリップが unflagged な Node が要る（22.18+ / 23.6+ / 24+。23.0〜23.5 はフラグ無しでは動かない。検証したのは v22.20.0）。また `contrast.ts` に `enum` やコンストラクタのパラメータプロパティを書くと**消去できない構文**として落ちる（型注釈・`interface`・`type` は問題ない）。ロジックを複製しないための選択で、複製との比較では正しいが、**依存が Node のバージョンと構文の制約という見えにくい形で残っている**。**同じ依存が登録5 Skill すべてにある**（`sequence-register` は `questions.ts` / `canonical.ts` を、`issue-tree-register` は `derive.ts` / `canonical.ts` を、**`logic-tree-register` は `flat-tree-core.ts` / `canonical.ts`** を、`glossary-term-register` / `error-catalog-register` は `canonical.ts` のみを同ディレクトリから import する。M15 で旧2本も `canonical.ts` をこの形に揃え、issue-tree-m2 で4本目、logic-tree-m2 で5本目が加わった）。ただし**いずれも機械検査が付いている**——コピーがアプリの原本とバイト一致していること・値 import を持たないことを、sequence 側は `src/modules/sequence/skill-copy.test.ts` が、課題ツリー側は `src/modules/issue-tree/skill-copy.test.ts` が、ロジックツリー側は `src/modules/logic-tree/skill-copy.test.ts` が、旧2本は `src/core/skill-canonical-copy.test.ts` が検査する（判定ヘルパは issue-tree-m2 で `src/core/import-analysis.ts` へ切り出され、3モジュールが共有している）。**なお `canonical.ts` のコピーについては、logic-tree-m2 で `src/core/skill-canonical-copy.test.ts` に網羅アサーションを置き、検査の所在を1箇所へ集約した**（それまでは検査が3ファイルに散り、網羅を縛るものがどこにも無かった。rev 4章）。したがって、**コピーが元とズレたときも、元に消去できない構文（`enum` 等）が生えたときも、テストが赤くなる。** `palette-fit.mjs` 側も `src/styles/palette-fit.smoke.test.ts` が実際に spawn して import 経路と終了コードを検査するようになった（`contrast.ts` に `enum` が生えれば赤くなる）ので、残っているのは **Node のバージョン制約（22.18+ / 23.6+ / 24+）が見えにくい依存として存在する**という記録のみ `[Skill]`
- **課題ツリーのスクリーンショットが未撮影で、`README.md` の画像の行がコメントの中に入ったままである**（`docs/images/issue-tree-editor.png` / `README.md`）: 他ツールの節は `<!-- SCREENSHOT: ... -->` のコメントの直後に `![...](docs/images/....png)` を生かしているが、課題ツリーだけは PNG がまだ無いため、**`![課題ツリーエディタ](docs/images/issue-tree-editor.png)` の1行を意図的にコメントの中へ入れてある**（生かすと、リポジトリで最も人目に付く文書に壊れた画像アイコンが出る）。**撮って置いたら、その1行をコメントの外へ戻すこと**——撮り方はコメント自身に書いてある。2026-08-22 の実機確認では撮られなかった（計画 Task 11 の項目13） `[issue-tree-m1]`
- **`layoutIssueTree` の doc に不変条件が書かれていない**（`src/modules/issue-tree/layout.ts`）: 「`posed` は同じ `data` に対する `poseQuestions(data)` の結果でなければならない」という前提があり（配列の添字で引き当てるため）、**引数の型からは読めない**ので、呼び出しを増やす人が踏む。**issue-tree-m3 で症状が悪くなった**——以前は食い違うと `q.result` で throw していたが、いまは `posed.issueNeedsHypothesis[i]` を読むだけなので、**別の木の答えで「仮説なし」のバッジが立ったり立たなかったりするだけで、例外は飛ばない** `[issue-tree-m1]`
- **`schemas/sequence.schema.json` の `failures` の description が「キーの欠落＝未定義」のままで、画面・Skill の語彙（M22 で「未回答」に変わった）と揃っていない**（`schemas/sequence.schema.json:87`）: `"description": "『失敗したら？』の答え。キーの欠落＝未定義（warning 表示）。正規化で欠落キーを補完しない。"` は M22 前の語彙（`未定義` / `warning`）のまま残っている。**`.claude/skills/sequence-register/schemas/sequence.schema.json` は `src/core/skill-schema-copy.test.ts` がバイト一致を強制する追跡済みコピー**（`app-controller.ts` が実体を共有しているわけではない）なので、**スキーマ改訂時は `schemas/sequence.schema.json` と Skill 側コピーの両方を同時に直す必要がある**——片方だけ直すと `skill-schema-copy.test.ts` が赤くなる `[M22]`
- **シーケンスの「未記入」判定（`=== ''`）が `missing.ts`・アクターヘッダ・ラベルセル・`ActorRefCell` の4箇所に散在している**（`src/modules/sequence/missing.ts` / `SequenceEditor.tsx`（アクターヘッダ・ステップのラベルセル） / `ActorRefCell.tsx`）: いずれも `actor.name === ''` または `step.label === ''` を直接書いており、`missing.ts` の `isMissingCell` 相当の一元化（用語集・エラーカタログにある関数）が無い。判定が1つに増えるたびに4箇所を揃える必要がある。glossary の `isMissingCell` のような形へ寄せるのは将来の整理 `[M22]`
- **幾何の primitive が2ファイルに分かれ、互いに無関係**（`src/core/canvas/viewport.ts` の `Rect { x, y, width, height }` と `src/core/canvas/tree-layout.ts` の `Point { x, y }` / `Size { width, height }`）: `Rect` は `Point & Size` として定義されていない。結果として `TreeEdges.tsx` は `edgePath({ ...from, ...fromSize }, { ...to, ...toSize })` とスプレッドで組み立てる形になっており、3本目のキャンバスツール（課題ツリー）も同じ組み立てを書くことになる。また `edges.ts` が `viewport.ts` に依存する（`import type` のみなので実行時結合はゼロ）読み筋も生まれている。M20 は「挙動を1つも変えない」移設のマイルストーンだったため、型の再編はスコープ外として見送った。**課題ツリーを書き始める前に、1ファイルへ寄せるか `Rect = Point & Size` にするかを決める値打ちがある** `[M20]`
- **Miro のクリップボード形式は非公開で、Miro 側の変更で壊れうる**（`src/modules/logic-tree/miro-codec.ts` 等）: 設計文書（[`../superpowers/plans/2026-08-29-logic-tree-m3-miro-clipboard-design.md`](../superpowers/plans/2026-08-29-logic-tree-m3-miro-clipboard-design.md)の「[付録] Miro のクリップボード形式」）は実データの解析と11回の貼り付け実験だけを根拠にした**この時点の観測結果**であり、公開仕様ではない。壊れたときに最初に落ちるのは `miro-codec.test.ts` の原本バイト照合（実機の貼り付けで取得したフィクスチャとの一致検査）で、そこが赤くなったら設計文書の「実験の記録」を辿って何が変わったかを調べ直す `[logic-tree-m3]`
- **`src/modules/sequence/mermaid.ts` の `export { escapeMermaidLabel }`（re-export）を参照している本番コードが無い**（`escapeMermaidLabel` は logic-tree-m3 で `@/core/mermaid.ts` へ引き上げ済み）: 本体は `sequence/mermaid.ts` 内で `sequenceToMermaid` が使うために import しているだけで、re-export の唯一の参照者は `src/modules/sequence/mermaid.test.ts`（`import { escapeMermaidLabel, sequenceToMermaid } from './mermaid'`）である。テストを `@/core/mermaid` から直接 import する形へ移せば、この re-export と `sequence/mermaid.ts` の import 1行は消せる `[logic-tree-m3]`
- **端末の配色が、`palette.css` の `.dark` が「素のクラスセレクタ」であることに依存している**（`src/components/TerminalTab.tsx` の `buildDarkTerminalTheme`）: 非表示の使い捨て要素に `className = 'dark'` を付けて `getComputedStyle` で読むことで、アプリがライトでもダーク側の値を取っている（M28。9章）。この読み方は `.dark { … }` という**素のクラスセレクタ**の形を前提にしており、`:root.dark` のような詳細度の高い形へ変えると、`document.body` の子に付けた `.dark` はもうトークンを再定義しないので、**端末の配色が黙ってライトへ戻る**（`palette.test.ts` はこの経路を検査しておらず、テストでは検出できない） `[M28]`
- **`（未定義）` の文言が4箇所で複製されている**（`src/modules/glossary/markdown.ts` の `UNDEFINED_DEFINITION` / `src/modules/error-catalog/markdown.ts` の `UNDEFINED_VALUE` / `src/modules/logic-tree/markdown.ts` の `UNDEFINED_TEXT`（いずれもモジュール内のローカル定数）、そして M29 が新設した `src/core/table-export.ts` の `UNDEFINED_TEXT`（表側3ツールが参照する共通の定数）): **表側だけが core の1本に一元化され、Markdown 側の3つは畳んでいない。** 文言そのものは3箇所とも `'（未定義）'` で一致しているので今すぐ表示がずれる心配は無いが、どちらか一方を直して他方を直し忘れると「Markdown では（未定義）だが表では別の文言」という食い違いが黙って生まれる経路が残る。**M29 のスコープでは意図して畳んでいない**——表形式コピーを作るのが目的で、既存の Markdown 出力3本に手を入れるのは範囲外と判断した `[m29]`
- **`src/modules/issue-tree/derive.ts` の `suppressedIssueIds` の JSDoc に、v2 の前提が残っている**——「課題ノードのイベントは見送り（deferred）しか無い」と書いてあるが、v3 では旗が2種（`deferred` / `resolved`）になり、判定（`latestKind(node.events) !== null`）は最初からどちらのイベントでも抑制する形で書かれている。挙動は正しく実害は無いが、**心臓部のコメントであり、同梱 Skill へバイト一致でコピーされる**（`.claude/skills/issue-tree-register/scripts/derive.ts`）ので、直すときは `cp` のやり直しが要る `[issue-tree-m4]`
- **「どの問いにも紐づかない FB」ブロックの固定文が編集できない**（`src/modules/issue-tree/AskBlock.tsx` の `null` 問い用の文言）: 問いを指さない FB（`askId: null`）をまとめる見出しの文言はコード中に固定で置いてあり、意図した設計（この枠自体が「宙に浮いた FB」を示す固定の器であって、利用者が名付ける対象ではないため）だが、**文言はソースコードにしか存在しない**——用語集・仕様書のどこにも書き起こしていないので、文言を変えたくなったときにここへ辿り着く手がかりが画面以外に無い `[issue-tree-m5]`
- **アクセシブル名の動詞が仮説だけ「削除」、FB・問いは「消す」で不揃い**（`src/modules/issue-tree/HypothesisPanel.tsx:199` の `` `${label}を削除` ``、`AskBlock.tsx:208` / `:260` の `` `${askLabel}を消す` `` / `` `${name}を消す` ``）: issue-tree-m5 Task 7 の brief が仮説側の文言を名指しで `仮説{N}を削除` と指定していたため、他の削除操作（FB・問い）の「消す」と語が割れた。同じ「取り除く」操作に2つの語があるだけで、機能上の実害は無い `[issue-tree-m5]`
- **`.add`（節末の追加ボタン）の面クラスが2ファイルに逐語の写しで存在する**（`src/modules/issue-tree/HypothesisPanel.tsx` の `addButtonClass` と `src/modules/issue-tree/IssueTreeEditor.tsx` の帯の「仮説を追加」ボタン）: 2つのクラス文字列は `${ink}` を使うか `text-ink` と直書きするかの1語を除いて同一。**片方の面を直すと、もう片方が古い面のまま取り残される**——`AskBlock.tsx` の削除ボタンは `deleteButtonClass` に1本化されているのに、こちらは揃えられていない。検査は通るので気づきにくい `[issue-tree-m5]`
- **`solutionLabelH = max(labelH, TRASH_ICON_SIZE)` の分岐が現在の書体では選ばれない**（`src/modules/issue-tree/layout.ts:598`）: ソリューション仮説の節見出しの高さは、現行の書体サイズでは常に `labelH`（文字の高さ）が `TRASH_ICON_SIZE`（ゴミ箱アイコン）を上回るため、`Math.max` の右側が選ばれる入力を作れない。**欠陥ではなく将来の書体変更に対する備え**である。**この項は当初「選ばれない分岐には番人を付けようがない」と書いていたが、それは誤りだった**——`layoutIssueTree(data, posed, fonts, index)` は `fonts` を**引数で受け取る**ので、`{...fonts, small: {…, lineHeight: 12}}` を渡せば `labelH(12) < TRASH_ICON_SIZE(16)` で右側が選ばれる。`layout.test.ts` に番人を置いた（「節見出しの帯はゴミ箱より低くならない（書体が縮んだときの分岐）」。`Math.max` を落とす変異で赤くなることを確認済み）。**残件として開いているのは「実際の書体では選ばれない分岐が1つある」という事実の記録だけ**で、無防備ではない `[issue-tree-m5]`
